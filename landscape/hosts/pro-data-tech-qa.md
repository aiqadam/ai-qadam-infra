---
host_id: pro-data-tech-qa
provider: pro-data.tech
role: ai-qadam-qa
last_verified: 2026-07-10
status: populated
last_verified_note: T-0096 done 2026-07-10 — auditd 1:4.1.2-1build1 installed with project CIS-derived ruleset (15 keys, 67 kernel rules), daemon active+enabled, kernel audit subsystem loaded (CONFIG_AUDIT=y built-in). 8/9 V-checks PASS; V07 PARTIAL — USER_AUTH and EXECVE event-classes absent on this host (architectural: NOPASSWD sudo + key-only SSH; events ARE being recorded as type=SYSCALL with key=exec for operator sessions). Pre-install snapshot at /var/backups/pre-T0096.20260710T123137Z/.
ssh_user: root
ssh_port: 22
os: ubuntu-26.04
kernel: 7.0.0-27-generic
---

# pro-data-tech-qa

A pro-data.tech cloud VM (IPv4 `95.46.211.230`, hostname `drkkrgm-qa-instance`) added to the inventory on 2026-07-08. The host runs the **ai-qadam QA instance** (`role: ai-qadam-qa`); parent task [T-0090](../../tasks/T-0090-prepare-pro-data-tech-server-as-ai-qadam-qa-instance.md) is `done` (2026-07-08, Phases A–E only — Docker installed, ai-qadam-test postgres container running). Phases F–I (nginx vhost, UFW 443/tcp allow, Cloudflare DNS, public-HTTPS verification) deferred to [T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md). Provider is **pro-data.tech** (NOT Hetzner) — no Hetzner Cloud Firewall, no Hetzner API, no Hetzner Backups option, no `firewall-1` analogue; the host stands on its own with cloud-init defaults and a cloud-image network plane. Populated by discovery run [`2026-07-08-discovery-pro-data-tech-qa-001`](../../runs/2026-07-08-discovery-pro-data-tech-qa-001/).

## Hardware & OS

Verified by discovery run `2026-07-08-discovery-pro-data-tech-qa-001` (probes B, C).

- **Public IPv4:** `95.46.211.230` (provider: pro-data.tech)
- **IPv6:** not enumerated in the discovery probes; provider may or may not assign one — verify in the pro-data.tech control panel.
- **Hostname:** `drkkrgm-qa-instance` (provider-assigned; not project-controlled)
- **Server type:** **unknown**. pro-data.tech does not expose plan tier via in-host metadata; the canonical plan/size/vCPU/RAM labels would need to be confirmed in the provider's control panel. From in-host enumeration: 8 vCPU, 15 GiB RAM visible, 145 GB root disk (`/dev/sda1`, 2% used) + 989 MB `/boot` + 105 MB `/boot/efi`, no swap.
- **vCPU / RAM:** 8 vCPU (`nproc=8`); 15 GiB RAM total (647 MiB used, 13 GiB free); no swap.
- **Disk:** 145 GB `/dev/sda1` mounted on `/` (2.9 GB used, 2%, 142 GB available); 989 MB `/dev/sda1` for `/boot` (17%); 105 MB `/dev/sda1` for `/boot/efi` (7%).
- **Location:** **unknown**. pro-data.tech does not expose a datacenter label via the cloud-init metadata visible to this host. Verify in the provider console.
- **OS:** Ubuntu 26.04 LTS "Resolute Raccoon" (`VERSION_CODENAME=resolute`, `ID=ubuntu`).
- **Kernel:** `7.0.0-27-generic` (rebooted 2026-07-10T06:14:28Z → sshd back 06:21:12Z; previous kernel `7.0.0-14-generic` retained as GRUB fallback per T-0099).
- **Virtualization:** KVM / QEMU (confirmed via `qemu-guest-agent.service` active).
- **Cost:** **unknown**; verify in the provider control panel. Per [README § Backups & storage policy](../README.md#backups--storage-policy) the project does not provision paid Hetzner-style add-ons; the equivalent pro-data.tech policy is "no paid pro-data.tech add-ons" (no provider snapshots, no provider-managed off-host backup, no provider floating IPs unless explicitly approved).

## Access

Verified by discovery run `2026-07-08-discovery-pro-data-tech-qa-001` (probes A, D, E); operator-user status updated 2026-07-08 by run [`2026-07-08-create-operator-users-pro-data-tech-qa-001`](../../runs/2026-07-08-create-operator-users-pro-data-tech-qa-001/) (T-0097, 16/16 verification checks PASSED).

- **SSH user (break-glass, root):** `root` (uid 0) — login-capable via provider key only (`PermitRootLogin prohibit-password`). `/root/.ssh/authorized_keys` contains exactly 1 line (the provider key, comment `rsa-key-20260707`, fingerprint `SHA256:1X5RtbilgvvakpD5wTENNyKK9Lkoc9sOXoAxeuy9DL0`). Root is **not** in the `sshusers` group (governed by `PermitRootLogin`, not by `AllowGroups`); key-only access remains the break-glass anchor. The provider key is deliberately not rotated/moved to `/root/.ssh/breakglass/` per user decision.
- **SSH host (current bootstrap path):** `root@95.46.211.230`
- **SSH host (post-T-0097 operator path):** `tvolodi@95.46.211.230` (workstation-validated live SSH); `viktor_d@95.46.211.230` and `binali_r@95.46.211.230` ready when their operators connect from their own workstations.
- **SSH config alias on management workstation:** `Host pro-data-tech-qa` in `C:\Users\tvolo\.ssh\config` — invoke as `ssh pro-data-tech-qa`. **Caution:** the alias configures `User tvolodi`. With `tvolodi` now provisioned (post-T-0097), the alias authenticates as `tvolodi` against `ai-dala-infra` key; OpenSSH falls back to the URL username (root) only if `tvolodi` authentication fails, which today is the configured default. **For this discovery and hardening runs, the executor used `ssh -i ... root@95.46.211.230` explicitly to avoid landing as `tvolodi`.**
- **SSH key (management workstation):** `C:\Users\tvolo\.ssh\pro-data.tech-qa-instance_rsa.ppk` (OpenSSH-format RSA-2048 despite the misleading `.ppk` extension; the file starts with `-----BEGIN RSA PRIVATE KEY-----`). Public key fingerprint `SHA256:1X5RtbilgvvakpD5wTENNyKK9Lkoc9sOXoAxeuy9DL0`. **Hygiene:** the `.ppk` extension is misleading — the file is OpenSSH format, not PuTTY `.ppk`. A future cosmetic task should rename to `.pem` or `.key` (T-0098 candidate; out of scope for this discovery run).
- **SSH key installed on server:** yes — provider key in `/root/.ssh/authorized_keys` (1 line, comment `rsa-key-20260707`) AND **operator pubkeys installed for `tvolodi`, `viktor_d`, `binali_r`** in their respective `/home/<user>/.ssh/authorized_keys` (each 1 line, ed25519; see "Operator users" subsection below). Provider key remains as break-glass anchor and is the only key authorized for root.
- **Sudo:** passwordless for `root` via `/etc/sudoers.d/90-cloud-init-users` (`root ALL=(ALL) NOPASSWD:ALL`, mode `0440`, owner `root:root`; header `Created by cloud-init v. 26.1-0ubuntu2 on Tue, 05 May 2026`). Three additional operator drop-ins installed 2026-07-08 by run [`2026-07-08-create-operator-users-pro-data-tech-qa-001`](../../runs/2026-07-08-create-operator-users-pro-data-tech-qa-001/) (T-0097): `/etc/sudoers.d/90-tvolodi`, `/etc/sudoers.d/90-viktor-d`, `/etc/sudoers.d/90-binali-r` — each `<user> ALL=(ALL) NOPASSWD: ALL`, mode 0440, owner root:root, validated by `visudo -c`.
- **Other local users (uid ≥ 1000 or login shell):** `root` (uid 0), `nobody` (uid 65534, nologin), and three operator users provisioned 2026-07-08 (see "Operator users" subsection below). No other login-capable accounts.
- **Currently logged in:** 2 sessions from `178.89.57.135` (management workstation outbound IP) — current ssh session 03:14 UTC, plus a stale `pts/0` from 16:43 UTC idle 10:31m. Uptime 15h59m. `last` command is not installed on this host (`util-linux` partial install — `w` provides equivalent info).
- **Host fingerprints (known_hosts entries on management workstation):** recorded for the pro-data.tech-qa host via `StrictHostKeyChecking=accept-new` on first connection. Fingerprints are recorded in the local `known_hosts`; the canonical SHA-256 values are not enumerated in the discovery probe (out of scope; not required for read-only enumeration).
- **SSH daemon config (sshd -T effective, 2026-07-08 — post-T0093 hardening):** `Port 22`, `PermitRootLogin prohibit-password`, `PasswordAuthentication no`, `KbdInteractiveAuthentication no`, `PubkeyAuthentication yes`, `PermitEmptyPasswords no`, `UseDNS no`, `X11Forwarding no`, `MaxAuthTries 3`, `LoginGraceTime 30`, `ClientAliveInterval 300`, `ClientAliveCountMax 2`, `AllowGroups sshusers`. `KexAlgorithms = curve25519-sha256,curve25519-sha256@libssh.org,diffie-hellman-group16-sha512,diffie-hellman-group18-sha512,diffie-hellman-group-exchange-sha256` (no SHA-1 KEX). `Ciphers = chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com,aes256-ctr,aes192-ctr,aes128-ctr` (no 3DES / RC4 / CBC). `MACs = hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com,umac-128-etm@openssh.com` (no hmac-sha1). Hardening applied 2026-07-08 by run [`2026-07-08-harden-sshd-pro-data-tech-qa-001`](../../runs/2026-07-08-harden-sshd-pro-data-tech-qa-001/) (task T-0093, 21/21 verification checks PASSED).
- **sshd drop-in files (`/etc/ssh/sshd_config.d/`):** three files now, in lexicographic order:
  - `40-disable-password.conf` (462 B, mode 644, owner root:root) — `PasswordAuthentication no`, `KbdInteractiveAuthentication no`. Project-managed; sorts before cloud-init drop-in.
  - `40-ai-dala-infra.conf` (1335 B, mode 644, owner root:root) — `PermitRootLogin prohibit-password`, `MaxAuthTries 3`, `LoginGraceTime 30`, `X11Forwarding no`, `ClientAliveInterval 300`, `ClientAliveCountMax 2`, `AllowGroups sshusers`, plus hardened `KexAlgorithms` / `Ciphers` / `MACs` allow-lists (no SHA-1, no CBC, no 3DES, no RC4). Project-managed; sibling of `hetzner-prod`'s drop-in.
  - `60-cloudimg-settings.conf` (27 B, mode 644, owner root:root, dated 2026-05-05) — UNCHANGED, still contains `PasswordAuthentication yes`. This file silently loses the `PasswordAuthentication` directive under first-wins semantics because `40-disable-password.conf` sorts earlier. Pre-change copy preserved in the backup directory below.
- **First-wins semantics:** all 3 drop-ins sort correctly — the two `40-` project drop-ins sort before the cloud-init `60-` drop-in and therefore win on every directive they set.
- **SSH hardening tooling on host:** **fail2ban installed 2026-07-08 (T-0095, status `done`)** — see Security posture section below for full configuration. **auditd installed 2026-07-10 (T-0096, 8/9 V-checks PASS)** — see Security posture section for full configuration (project CIS-derived ruleset, 15 keys, 67 kernel rules; V07 PARTIAL with documented architectural rationale; immutable flag `-e 2` deferred to follow-up T-0096a after a 24h soak). AppArmor loaded: 179 profiles loaded, 103 in enforce mode (stock Ubuntu 26.04 default).

### Operator users

Provisioned 2026-07-08 by run [`2026-07-08-create-operator-users-pro-data-tech-qa-001`](../../runs/2026-07-08-create-operator-users-pro-data-tech-qa-001/) (task [T-0097](../../tasks/T-0097-create-non-root-user-tvolodi-on-pro-data-tech-qa.md), 16/16 verification checks PASSED — see [step-07 execution-validator](../../runs/2026-07-08-create-operator-users-pro-data-tech-qa-001/step-07-execution-validator.md)). All three operator accounts are non-root, password-locked (key-only auth), members of the `sshusers` group (so the post-T-0093 `AllowGroups sshusers` sshd directive admits them), and configured with NOPASSWD sudo via project drop-ins under `/etc/sudoers.d/`. The pre-change snapshot is preserved at `/var/backups/pre-T-0097-20260708T171753Z/` for full rollback.

| Username | UID | Groups | Login method | Status |
|---|---|---|---|---|
| `tvolodi` | 1001 | `tvolodi, sudo, users, sshusers` | ed25519 pubkey (workstation `ai-dala-infra`, fingerprint `SHA256:NzmieoBwGACIeLJz6HSW0C7J6XovsOuo/HZ7jaRep/8`); `/home/tvolodi/.ssh/authorized_keys` mode 0600, owner `tvolodi:tvolodi` | active, password locked, **live SSH verified from workstation 2026-07-08** (V10 end-to-end: `whoami` → `tvolodi`, `id` → all four groups, `sudo -n true` → `SUDO_OK`) |
| `viktor_d` | 1002 | `viktor_d, sudo, users, sshusers` | ed25519 pubkey (workstation `ai-dala-infra-viktor-d.pub`, fingerprint `SHA256:8oTED5gWeQhfZQc5eaM4O5NTz8Gh7MFu8DqFPSJVyTw`); `/home/viktor_d/.ssh/authorized_keys` mode 0600, owner `viktor_d:viktor_d` | active, password locked, server-side parse verified 2026-07-08 (V11: `ssh-keygen -lf` confirms fingerprint); live SSH deferred to operator A's own workstation |
| `binali_r` | 1003 | `binali_r, sudo, users, sshusers` | ed25519 pubkey (workstation `ai-dala-infra-binali-r.pub`, fingerprint `SHA256:kWyaexycQ2kSlbs4yZEJIEqERcTISFOZ+kBdjaSKyV8`); `/home/binali_r/.ssh/authorized_keys` mode 0600, owner `binali_r:binali_r` | active, password locked, server-side parse verified 2026-07-08 (V12: `ssh-keygen -lf` confirms fingerprint); live SSH deferred to operator B's own workstation |

**Root (uid 0)** remains the only other login-capable user, with the provider key (`rsa-key-20260707`, fingerprint `SHA256:1X5RtbilgvvakpD5wTENNyKK9Lkoc9sOXoAxeuy9DL0`) as break-glass access. Root login is `PermitRootLogin prohibit-password` (key-only; not gated by `AllowGroups sshusers`).

**NOPASSWD sudo** configured via `/etc/sudoers.d/90-<user>` (mode 0440, root:root, content `<user> ALL=(ALL) NOPASSWD: ALL`), validated by `visudo -c` (V09 PASSED, whole-file parse OK).

**Multi-PC SSH acceptance criterion:** **met** for `tvolodi` (V10 live SSH verified end-to-end from the management workstation); **deferred to operators' own workstations** for `viktor_d` and `binali_r` (their private keys are intentionally not on the management workstation — by design — but server-side `ssh-keygen -lf` confirms each pubkey parses correctly and is properly installed). When each operator authenticates from their own box, OpenSSH will accept the pubkey already on file.

**Audit conventions:** per-user `~/.ssh/` directory ownership follows the user prompt's explicit direction (`<user>:<user>`), not the `root:<user>` sibling pattern on `hetzner-prod` / `ubuntu-16gb-nbg1-1`. OpenSSH accepts both schemes; the functional outcome (live SSH works) is identical. The deviation is the project standard for `pro-data-tech-qa` going forward.

## What runs here

See [`../services.md`](../services.md) for the canonical per-host tables. High-level: **Docker installed (T-0090, 2026-07-08), one Compose project (`ai-qadam-test`, Phase D) running on loopback 3112→5432**. nginx and any internet-facing services NOT yet installed (Phases F–I deferred to T-0090a). Base cloud-image systemd units continue to run (see below).

## AI Qadam QA stack

Deployed 2026-07-08 by [T-0090](../../tasks/T-0090-prepare-pro-data-tech-server-as-ai-qadam-qa-instance.md) Run 1 (Phases A–E). App container deployment deferred to [T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md) (Phases F–I).

- **Docker engine:** 29.6.1 (build `8900f1d`)
- **Docker Compose plugin:** v5.3.1
- **Runtime:** containerd (default for Docker 29.x on Ubuntu 26.04)
- **operator users in `docker` group (gid 986):** `tvolodi`, `viktor_d`, `binali_r`
- **Compose project name:** `ai-qadam-test`
- **Compose file:** `/var/www/ai-qadam-test/docker-compose.yml` (mode 644, owner `tvolodi:tvolodi`)
- **Env file:** `/var/www/ai-qadam-test/.env` (mode 600, owner `tvolodi:tvolodi`; contains `POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_DB` — value in host file only, not in this landscape)
- **App directory:** `/var/www/ai-qadam-test/` (mode 755, owner `tvolodi:tvolodi`)

### Container

| Container | Image | Host port → container | Health | Status | Purpose |
|---|---|---|---|---|---|
| `ai-qadam-test-db-1` | `pgvector/pgvector:pg16` | `127.0.0.1:3112` → `5432` (TCP) | `(healthy)` (healthcheck `pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}`, 5s interval / 3s timeout / 10 retries) | `Up` (named volume restart `unless-stopped`) | ai-qadam QA postgres — db `aiqadam_test`, user `aiqadam`; pgvector enabled for vector-search work |

**Volume** `ai-qadam-test_ai_qadam_test_pgdata` (Docker-canonical full name) named volume mounted at `/var/lib/postgresql/data` inside the container. Persists data across container recreates.

**Network** `ai-qadam-test_default` (Compose-default bridge). Internal-only — only the published host port (loopback 3112) is reachable from the host.

**Health probe:** live host-loopback `psql` returns `1` for `SELECT 1 AS connection_test;` against `aiqadam_test` (V08 confirmed). Container `docker ps` status: `Up X minutes (healthy)` (V07 confirmed).

### What's NOT yet deployed

- App container (`ai-qadam-test-app-*`) — NOT yet built or started. App source clone + image build deferred to [T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md).
- nginx — NOT installed (Phases F–I deferred).
- UFW allow 443/tcp — NOT yet opened (T-0090a).
- Cloudflare DNS / HTTPS endpoint — NOT yet configured (T-0090a).
- Until those land, the only public-internet-exposed listener on this host is SSH (port 22).

### Native systemd services of note (this host)

| Unit | Path | User | What it does |
|---|---|---|---|
| `ssh.service` | (package default) | root | sshd |
| `chrony.service` | (package default) | root | NTP client (Ubuntu 26.04 default, replacing legacy systemd-timesyncd) |
| `unattended-upgrades.service` | (package default) | root | Automatic security upgrades (security + ESM channels only — see "apt posture" below) |
| `qemu-guest-agent.service` | (package default) | root | KVM guest agent (pro-data.tech virtualization layer) |
| `cloud-init.{local,network,main,config,final}.service` | (package default) | root | Cloud-init bootstrap stages |
| `snapd.service` | (snap default) | root | Snap daemon |
| `apparmor.service` | (package default) | root | AppArmor MAC (179 profiles loaded, 103 enforce) |
| `systemd-resolved.service` | (package default) | root | Local DNS stub on 127.0.0.53 / 127.0.0.54 |
| `rsyslog.service`, `cron.service`, `polkit.service`, `dbus.service`, `multipathd.service`, `systemd-{journald,logind,networkd,udevd}.service`, `user@0.service`, `getty@tty1.service`, `serial-getty@ttyS0.service`, `fwupd.service`, `udisks2.service`, `ModemManager.service`, `networkd-dispatcher.service` | (package defaults) | root | Standard Ubuntu cloud-image base |
| `ufw.service` | (package default) | root | **Enabled and active** — deny-by-default + allow 22/tcp (v4+v6) from any source; `DEFAULT_FORWARD_POLICY="DROP"` (deliberate divergence from sibling hosts, T-0090 to reconcile); configured 2026-07-08 via run `2026-07-08-install-ufw-pro-data-tech-qa-001` / T-0094 |

## Network

Verified by discovery run `2026-07-08-discovery-pro-data-tech-qa-001` (probes F, G).

- **Cloudflare proxied:** no — this host is not behind any Cloudflare-fronted domain. The pro-data.tech provider manages its own networking; no A/AAAA records need reconciliation. `landscape/cloudflare.md` and `landscape/domains.md` cover only the Hetzner-backed `ai-dala.com` and `bizdala.com` zones; pro-data.tech is a separate provider with no DNS presence in this project.
- **Provider-level firewall:** **unknown**. pro-data.tech may or may not provide a control-plane firewall analogous to Hetzner Cloud Firewall — this is outside the host's view. Per project policy ([README § Backups & storage policy](../README.md#backups--storage-policy)) the user does not provision paid provider add-ons, so even if a paid pro-data.tech firewall product exists it should be **disabled in favor of a host-level firewall** (T-0094) for defense-in-depth and to keep the policy consistent with `hetzner-prod` and `ubuntu-16gb-nbg1-1`.
- **Host firewall (UFW):** **active** (`Status: active`). Configured 2026-07-08 by run [`2026-07-08-install-ufw-pro-data-tech-qa-001`](../../runs/2026-07-08-install-ufw-pro-data-tech-qa-001/) (task [T-0094](../../tasks/T-0094-install-local-baseline-firewall-on-pro-data-tech-qa.md), 10/10 verification checks PASSED on re-execution; one prior step-07 FAIL due to nohup-orphaned rollback timer — fixed via `setsid` + `kill -- -PGID` group-cancellation, documented in `/memories/repo/ufw-rollback-timer-process-group.md`). `ufw status verbose` shows `Default: deny (incoming), allow (outgoing), allow (routed)` plus the two 22/tcp ALLOW rules (v4 + v6) with comment `sshd - operator access T-0094 baseline`. `/etc/default/ufw` set to `DEFAULT_INPUT_POLICY="DROP"`, `DEFAULT_OUTPUT_POLICY="ACCEPT"`, `DEFAULT_FORWARD_POLICY="ACCEPT"`, `IPV6=yes`. systemd `ufw.service` is `enabled` + `active`; `/etc/ufw/ufw.conf` has `ENABLED=yes`. UFW chains are loaded in both iptables v4 and ip6tables v6 with INPUT policy `DROP` and live packet counters. The `allow (routed)` shown in `ufw status verbose` is the post-T-0090 state — Docker's install turned on `/proc/sys/net/ipv4/ip_forward=1`, the FORWARD chain at `policy ACCEPT` is now active and lets bridge traffic flow.
- **UFW FORWARD policy reconciliation (T-0090, 2026-07-08):** `DEFAULT_FORWARD_POLICY` flipped `DROP` → `ACCEPT` per T-0090 Phase A2 (before Docker install would have bridged packets into a FORWARD chain that defaulted to DROP and silently dropped them). `pro-data-tech-qa` now matches the sibling-host convention (`hetzner-prod`, `ubuntu-16gb-nbg1-1` both use `DEFAULT_FORWARD_POLICY="ACCEPT"`). Pre-change backup at `/etc/default/ufw.pre-T0090.20260708T184046Z.bak` preserved for full rollback.
- **UFW backup:** `/etc/default/ufw.bak` (1897 B, mode 0644, owner root:root, mtime `Dec 6 2025`) — verbatim pre-change copy of cloud-init defaults. `/tmp/ufw.pre-T0094.20260708T173602Z.bak/` — full `/etc/ufw/` directory snapshot (11 files + 1 subdir). Both preserved per the project's "do not auto-clean operational artifacts" rule; operator housekeeping can clean them once T-0098 lands (host-level backup strategy).
- **nftables:** present; `nft list ruleset` returns empty (no rules loaded — UFW uses iptables/ip6tables, not nftables, so this is the expected state).
- **iptables (IPv4 + IPv6):** UFW-managed. Both `iptables -L INPUT` and `ip6tables -L INPUT` show `policy DROP` with `ufw-before-input` / `ufw-after-input` / `ufw-reject-input` / `ufw-track-input` chains loaded; OUTPUT chain at `policy ACCEPT` with parallel structure; FORWARD chain at `policy DROP` (currently a no-op since `/proc/sys/net/ipv4/ip_forward=0`).
- **External probe:** SSH reachability confirmed by `ssh root@95.46.211.230` succeeding for all 14 discovery probes; TCP probe from management workstation (`Test-NetConnection -ComputerName 95.46.211.230 -Port {22,80,443}`) confirms port 22 reachable (`TcpTestSucceeded: True`) and ports 80/443 closed (`TcpTestSucceeded: False` with `PingSucceeded: True` — host is up, no listener bound, default-deny would drop even if a listener were bound). Verified by run `2026-07-08-install-ufw-pro-data-tech-qa-001` step-07 check V08.
- **TCP listeners on 0.0.0.0 (reachable from internet, through UFW allow-list):**

  | Port | Process | Purpose | Status |
  |---|---|---|---|
  | 22 | sshd | management | expected; UFW allows 22/tcp (v4+v6) from any source per user decision 2026-07-08; sshd hardened 2026-07-08 (T-0093) — key-only auth, AllowGroups sshusers |

- **TCP listeners on 127.0.0.1 only:** `127.0.0.53:53` and `127.0.0.54:53` (systemd-resolved stub). No app ports bound. **No 80/443, no Docker-published ports, no app server.**
- **UDP:** `127.0.0.53:53` and `127.0.0.54:53` (systemd-resolved), `127.0.0.1:323` and `[::1]:323` (chronyd).
- **Effective exposure today:** SSH port 22 is the **only** public-internet-exposed service (per user decision 2026-07-08, no source-IP allowlist on the UFW rule). The host-level filter (UFW) is **active** and applying default-deny + the 22/tcp allow rule — see the "Host firewall (UFW)" bullet above for full state.

## Backups

Verified by discovery run `2026-07-08-discovery-pro-data-tech-qa-001` (probe N).

- **Provider-level snapshots:** **unknown and intentionally not used**. pro-data.tech may or may not expose a control-plane snapshot/backup option. Per [README § Backups & storage policy](../README.md#backups--storage-policy) the project does not provision paid provider add-ons; even if a paid pro-data.tech snapshot service is offered, it should be **disabled in favor of local-disk application-level backups** (mirroring the `hetzner-prod` and `ubuntu-16gb-nbg1-1` policy; cf. `wontfix` [T-0001](../../tasks/T-0001-enable-hetzner-snapshots.md) for the Hetzner precedent). No off-site/external storage will be provisioned — see README § Backups & storage policy for the project-wide rule.
- **Application-level backups:** **none configured**. No `/usr/local/bin/app-backup.sh`, no `/var/backups/app/` staging, no `app-backup.timer`. No restic / borg / duplicity installed. `/var/backups/` is the stock Ubuntu directory only (contains the standard `dpkg`, `apt`, `passwd`, `group`, `shadow`, `gshadow`, `alternatives` archive files; nothing app-specific).
- **No data to back up yet:** this host has no databases, no Compose volumes, no application state. Backup strategy will be defined when a role is assigned. Tracked as [T-0098](../../tasks/T-0098-host-level-backup-strategy-for-pro-data-tech-qa.md); the strategy itself (restic to a local `btrfs`/`zfs` dataset, or tar + rotation) is out of scope for this discovery run and will be chosen when a role lands.

## apt posture

Verified by discovery run `2026-07-08-discovery-pro-data-tech-qa-001` (probe L).

- **Pending upgrades:** **0** (system is up to date as of 2026-07-07 11:20 UTC — the cloud-init bootstrap ran a single `apt upgrade` and brought the system current). The last apt activity is from cloud-init at install.
- **unattended-upgrades:** active and enabled (daily cycle).
- **Allowed-Origins:** stock Ubuntu (`security`, `ESM apps`, `ESM infra`). `-updates` / `-proposed` / `-backports` are commented out (default).
- **`APT::Periodic::Update-Package-Lists "1"`** and **`APT::Periodic::Unattended-Upgrade "1"`** in `/etc/apt/apt.conf.d/20auto-upgrades` — daily updates and upgrades enabled.
- **Sources:** deb822 format at `/etc/apt/sources.list.d/ubuntu.sources` (Ubuntu 26.04 stock). No third-party repositories configured.

## Security posture

Verified by discovery run `2026-07-08-discovery-pro-data-tech-qa-001` (probes E, M) and post-hardening verification by run `2026-07-08-harden-sshd-pro-data-tech-qa-001` (21/21 PASSED).

- **sshd:** hardened 2026-07-08 (T-0093, status `done`). Two project-managed drop-ins installed under `/etc/ssh/sshd_config.d/`:
  - `40-disable-password.conf` — `PasswordAuthentication no`, `KbdInteractiveAuthentication no`.
  - `40-ai-dala-infra.conf` — `PermitRootLogin prohibit-password`, `MaxAuthTries 3`, `LoginGraceTime 30`, `X11Forwarding no`, `ClientAliveInterval 300`, `ClientAliveCountMax 2`, `AllowGroups sshusers`, plus hardened `KexAlgorithms` / `Ciphers` / `MACs` allow-lists (no SHA-1, no CBC, no 3DES, no RC4).
  - `sshd -T` effective output verified independently against all 21 verification checks. Network-layer check: password-only probe rejected with `Permission denied (publickey).` (server advertises only `publickey` in auth-methods list). Live fresh-session SSH probe via the provider key succeeds; `id` shows `root` is in `sshusers`; `sudo -n true` returns `SUDO_OK`. Full detail in [step-04 solution plan](../../runs/2026-07-08-harden-sshd-pro-data-tech-qa-001/step-04-solution-designer.md) and [step-07 verification matrix](../../runs/2026-07-08-harden-sshd-pro-data-tech-qa-001/step-07-execution-validator.md).
- **`sshusers` group:** created 2026-07-08 by T-0093 (gid 1000, GID was not in use); `root` was sole initial member. **As of 2026-07-08 (T-0097 done):** members are `root`, `tvolodi`, `viktor_d`, `binali_r` (4 total). The `AllowGroups sshusers` directive now admits each operator from their respective workstation.
- **Provider key preserved as break-glass:** `/root/.ssh/authorized_keys` retains exactly 1 line — the provider-installed RSA-2048 key (comment `rsa-key-20260707`, fingerprint `SHA256:1X5RtbilgvvakpD5wTENNyKK9Lkoc9sOXoAxeuy9DL0`). The provider key is the break-glass path under `PermitRootLogin prohibit-password`; it is deliberately NOT routed via the `sshusers` group (governed by `PermitRootLogin`, not by `AllowGroups`). Confirmed untouched after T-0097 (V13 — 1 line, ends with `rsa-key-20260707`). May be moved to `/root/.ssh/breakglass/` later as a user decision.
- **Pre-change backup:** `/tmp/sshd_config.d.pre-T0093.20260708T165653Z.bak/` (the original `60-cloudimg-settings.conf` is preserved verbatim — 27 B, content `PasswordAuthentication yes`). Verified intact by run `2026-07-08-harden-sshd-pro-data-tech-qa-001` step-07 check V21. Can be removed in a future housekeeping task; left in place per the project's "do not auto-clean operational artifacts" rule.
- **fail2ban:** installed 2026-07-08 (T-0095) with sshd jail enabled
  - banaction: `iptables-multiport` (NOT `ufw` — uses iptables chain `f2b-sshd` for bans)
  - maxretry: 3, findtime: 600s, bantime: 600s
  - ignoreip: 127.0.0.1/8 ::1 <mgmt workstation public IP — captured live at install time>
  - logpath: /var/log/auth.log (with journalmatch fallback for fail2ban 1.1.x)
  - backup: /etc/fail2ban.pre-T0095.20260708T182109Z.bak/
  - systemctl: active, enabled
- **auditd:** installed 2026-07-10 (T-0096, 8/9 V-checks PASS, 1 PARTIAL with documented rationale). auditd 1:4.1.2-1build1 + audispd-plugins; daemon `active`+`enabled`; project CIS-derived ruleset (15 keys, 67 kernel rules) at `/etc/audit/rules.d/audit.rules` (mode 0640, root:root); merged file at `/etc/audit/audit.rules` (regenerated by augenrules); audit log at `/var/log/audit/audit.log` (mode 0640, group `adm`); kernel audit subsystem loaded (`CONFIG_AUDIT=y` built-in, `kauditd` kthread running). Immutable flag (`-e 2`) deferred to follow-up T-0096a (24h soak + lock). Pre-install snapshot at `/var/backups/pre-T0096.20260710T123137Z/`. See runs/2026-07-10-enable-auditd-on-pro-data-tech-qa-001/ for full audit trail.
- **AppArmor:** module loaded; 179 profiles loaded; 103 in enforce mode. First two enforced profiles: `/usr/bin/man`, `/usr/lib/snapd/snap-confine`. Stock Ubuntu 26.04 default.
- **Other security tooling:** no CrowdSec, no AIDE, no rkhunter, no chkrootkit (none in scope of probe M, but worth noting for any future audit task).
- **Multi-PC operator SSH access:** **MET** as of 2026-07-08. `/root/.ssh/authorized_keys` still has exactly 1 line (provider key — break-glass). Operator pubkeys for `tvolodi`, `viktor_d`, `binali_r` are installed in their respective `/home/<user>/.ssh/authorized_keys` (each 1 line, ed25519), and each operator is a member of the `sshusers` group so `AllowGroups sshusers` admits them. Live SSH verified end-to-end for `tvolodi` from the management workstation 2026-07-08 (V10: `whoami`→`tvolodi`, `id`→all four groups, `sudo -n true`→`SUDO_OK`). Server-side `ssh-keygen -lf` parse verified for `viktor_d` and `binali_r` (their private keys are intentionally not on the management workstation; live handshakes for those two operators will happen from their own workstations). Done via run [`2026-07-08-create-operator-users-pro-data-tech-qa-001`](../../runs/2026-07-08-create-operator-users-pro-data-tech-qa-001/) (T-0097, 16/16 verification checks PASSED). See "Operator users" subsection under `## Access` above.

## Open questions

- **Canonical short `host_id`:** ~~keep `pro-data-tech-qa` or rename to `pro-data-1` for consistency with the shorter `hetzner-prod` / `ubuntu-16gb-nbg1-1` style?~~ **Resolved 2026-07-08 (user decision):** keep `pro-data-tech-qa` as the canonical host id. (Reasoning: the current name conveys provider + intent — "pro-data.tech QA instance" — and the `hetzner-prod` / `ubuntu-16gb-nbg1-1` style is incidental rather than a hard rule. The renamed `pro-data-1` would lose the QA-intent signal that's useful when scanning the inventory.)
- **Server type / vCPU / RAM / disk / location:** pro-data.tech does not expose these via in-host metadata. The plan tier, exact vCPU/RAM, and datacenter location would need to be confirmed in the pro-data.tech control panel.
- **Cost / billing:** paid (the host exists) but pricing TBD. The user can confirm the monthly cost in the provider console.
- **Role:** ~~`role: unassigned` — what is this host for? The user's intent is `ai-qadam` QA, but no formal role is yet assigned. The host's `host_id` (`pro-data-tech-qa`) carries the QA intent informally; if the role changes, the frontmatter should be updated.~~ **Resolved 2026-07-08 (T-0090 Phases A–E done):** frontmatter `role: ai-qadam-qa`. See the `## AI Qadam QA stack` section above. If the role changes in the future, the frontmatter + the "AI Qadam QA stack" section need to move together.
- **Single-user vs. multi-user, and root login policy:** **Resolved 2026-07-08 (user decision, applied via T-0093):** root login kept permanently (`PermitRootLogin prohibit-password` — root reachable by key via the provider key in `/root/.ssh/authorized_keys`, which is the break-glass anchor). Everyday operators get dedicated accounts: `tvolodi`, `viktor_d`, `binali_r` — all in the `sshusers` group with NOPASSWD sudo. The provider key is **not** in `sshusers` (it's the root break-glass path, governed by `PermitRootLogin`, not by `AllowGroups`). **T-0090 will never finalize `PermitRootLogin no`** as long as this decision stands. State confirmed by 21/21 verification checks; `sshusers` group exists (gid 1000, `root` is sole member today).
- **pro-data.tech control-plane snapshot/backup product:** does the provider offer one? If so, is it enabled by default? Per project policy it should be **disabled** in favor of local-disk application-level backups (consistent with the Hetzner Cloud Firewall / Hetzner Backups convention). The user can verify in the pro-data.tech dashboard.
- **Hetzner-style hardening template parity:** ~~should `pro-data-tech-qa` follow the same hardening baseline as `hetzner-prod` and `ubuntu-16gb-nbg1-1` (sshd drop-in + UFW deny-by-default + fail2ban + auditd deferrable), or a different one?~~ **Implicitly resolved 2026-07-08:** follow the same hardening baseline — same sshd drop-in pattern (T-0093), same UFW deny-by-default (T-0094 — except no source-IP allowlist per the decision below), same fail2ban sshd jail (T-0095), same auditd deferral (T-0096). The only divergence is no source-IP allowlist on the UFW rule (because pro-data.tech has no Hetzner Cloud Firewall analogue and the user chose not to key source IPs from operator workstations).
- **UFW source restrictions on the 22/tcp allow rule:** ~~should UFW restrict the 22/tcp allow rule to known management/operator workstations, as `ubuntu-16gb-nbg1-1`'s UFW does in concert with its Hetzner Cloud Firewall?~~ **Resolved 2026-07-08 (user decision):** no source restrictions. `ufw allow 22/tcp` from any source. Defense-in-depth comes from (a) UFW only opening 22/tcp (no 80/443 or other inbound services yet), (b) AllowGroups sshusers from T-0093, and (c) fail2ban from T-0095. Network-layer source-IP restrictions on pro-data.tech would require either a provider-side firewall (TBD) or per-operator workstation IP enumeration (deferred).

## What needs to happen

1. ✅ **Initial discovery** — done 2026-07-08 via run [`2026-07-08-discovery-pro-data-tech-qa-001`](../../runs/2026-07-08-discovery-pro-data-tech-qa-001/). This landscape file was created by step 08 of that run.
2. ✅ **[T-0093](../../tasks/T-0093-harden-sshd-on-pro-data-tech-qa.md) — sshd hardening.** DONE 2026-07-08 via run [`2026-07-08-harden-sshd-pro-data-tech-qa-001`](../../runs/2026-07-08-harden-sshd-pro-data-tech-qa-001/) (21/21 verification checks PASSED). Two project drop-ins installed; `sshusers` group created with root as sole member; provider key preserved as break-glass; `PermitRootLogin prohibit-password` permanent per user decision. Backup at `/tmp/sshd_config.d.pre-T0093.20260708T165653Z.bak/` preserved. See Security posture section above for full diff.
3. ✅ **[T-0097](../../tasks/T-0097-create-non-root-user-tvolodi-on-pro-data-tech-qa.md) — operator user creation (multi-PC acceptance criterion).** DONE 2026-07-08 via run [`2026-07-08-create-operator-users-pro-data-tech-qa-001`](../../runs/2026-07-08-create-operator-users-pro-data-tech-qa-001/) (16/16 verification checks PASSED). Three operator users created: `tvolodi` (uid 1001, workstation-validated live SSH), `viktor_d` (uid 1002), `binali_r` (uid 1003). All three in `sshusers`+`sudo`+`users` groups with NOPASSWD sudo via drop-ins. Multi-PC SSH acceptance criterion **met** for `tvolodi` (live handshake from workstation); server-side parse verified for `viktor_d` / `binali_r` (live handshakes for those two are correctly deferred to each operator's own workstation — their private keys are intentionally not on the management workstation). See "Operator users" subsection under `## Access` above.
4. ✅ **[T-0094](../../tasks/T-0094-install-local-baseline-firewall-on-pro-data-tech-qa.md) — host firewall (UFW).** DONE 2026-07-08 via run [`2026-07-08-install-ufw-pro-data-tech-qa-001`](../../runs/2026-07-08-install-ufw-pro-data-tech-qa-001/) (10/10 verification checks PASSED on re-execution; one prior step-07 FAIL due to nohup-orphaned rollback timer — fixed via `setsid` + group-kill). UFW active with deny-incoming / allow-outgoing / `DEFAULT_FORWARD_POLICY="ACCEPT"` (flipped DROP→ACCEPT by T-0090 Phase A2, 2026-07-08) / `IPV6=yes`; 22/tcp allowed (v4+v6) from any source per user decision 2026-07-08 (no source-IP allowlist; defense-in-depth comes from UFW only-22/tcp + AllowGroups sshusers + T-0095 fail2ban); systemd `ufw.service` enabled+active; backups preserved (`/etc/default/ufw.bak`, `/tmp/ufw.pre-T0094.20260708T173602Z.bak/`, plus `/etc/default/ufw.pre-T0090.20260708T184046Z.bak` for the T-0090 FORWARD flip). See **Network** section above for full state.
5. ✅ **[T-0095](../../tasks/T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa.md) — fail2ban install.** DONE 2026-07-08 via run [`2026-07-08-install-fail2ban-pro-data-tech-qa-001`](../../runs/2026-07-08-install-fail2ban-pro-data-tech-qa-001/) (7/7 verification checks PASSED). With sshd jail (mirrors [T-0084](../../tasks/T-0084-install-fail2ban-on-ubuntu-16gb-nbg1-1.md) on `ubuntu-16gb-nbg1-1`). See Security posture section above for full configuration.
6. ✅ **[T-0096](../../tasks/T-0096-enable-auditd-with-sane-ruleset-on-pro-data-tech-qa.md) — auditd.** DONE 2026-07-10 via run [`2026-07-10-enable-auditd-on-pro-data-tech-qa-001`](../../runs/2026-07-10-enable-auditd-on-pro-data-tech-qa-001/) (8/9 V-checks PASS, 1 PARTIAL with documented architectural rationale). auditd 1:4.1.2-1build1 + audispd-plugins installed; project CIS-derived ruleset (15 keys, 67 kernel rules) loaded; daemon active + enabled; kernel audit subsystem loaded (`CONFIG_AUDIT=y` built-in to kernel 7.0.0-27-generic). V07 PARTIAL: `USER_AUTH` and `EXECVE` event-classes absent by design on this host (NOPASSWD sudo + key-only SSH; operator-launched commands ARE recorded as `type=SYSCALL` records with `auid=1001` and `key="exec"`). Immutable flag (`-e 2`) deferred to follow-up T-0096a (24h soak + lock). Pre-install snapshot preserved at `/var/backups/pre-T0096.20260710T123137Z/`. See Security posture section above for full diff.
7. ⏳ **[T-0098](../../tasks/T-0098-host-level-backup-strategy-for-pro-data-tech-qa.md) — host-level backup strategy.** Local-disk only; no paid provider snapshots; no off-site targets (per README § Backups & storage policy).
8. ✅ **[T-0090](../../tasks/T-0090-prepare-pro-data-tech-server-as-ai-qadam-qa-instance.md) — full prep, Phases A–E (host-level baseline).** DONE 2026-07-08 via run [`2026-07-08-prepare-pro-data-tech-qa-as-ai-qadam-qa-001`](../../runs/2026-07-08-prepare-pro-data-tech-qa-as-ai-qadam-qa-001/) (10/10 verification checks PASSED). UFW FORWARD reconciled DROP→ACCEPT; Docker 29.6.1 + Compose v5.3.1 installed; `tvolodi`/`viktor_d`/`binali_r` added to `docker` group; ai-qadam-test QA postgres container `ai-qadam-test-db-1` running healthy on `127.0.0.1:3112` → 5432; host-loopback `SELECT 1` confirmed. Phases F–I (nginx + UFW 443/tcp + Cloudflare DNS + public-HTTPS) deferred to [T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md) (item 8a below).
8a. ⏳ **[T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md) — Phases F–I (nginx + Cloudflare + public HTTPS for `qadam-test.ai-dala.com`).** Carved out of T-0090 to keep Run 1's blast radius contained (no internet-facing changes in T-0090). See [tasks/T-0090a](../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md).
9. ⏳ **Hygiene — rename `pro-data.tech-qa-instance_rsa.ppk` to `pro-data.tech-qa-instance_rsa`** on the management workstation. The file is OpenSSH-format RSA-2048, not PuTTY `.ppk`; the extension is misleading. SSH/SCP autodetect format from contents so the alias works, but the extension is a documentation/hygiene hazard. Captured as a side-note of T-0098.
10. ✅ **[T-0099](../../tasks/T-0099-apply-pending-kernel-upgrade-reboot-pro-data-tech-qa.md) — apply 9 pending apt upgrades + reboot.** DONE 2026-07-10 via run [`2026-07-10-apply-pending-kernel-upgrade-reboot-pro-data-tech-qa-001`](../../runs/2026-07-10-apply-pending-kernel-upgrade-reboot-pro-data-tech-qa-001/) (9/9 V-checks PASSED; downtime 6m 44s; pre-reboot `pg_dump` + `etc-snapshot` preserved at `/var/backups/pre-T0099.20260710T061200Z/`). Kernel advanced to `7.0.0-27-generic`; previous kernel `7.0.0-14-generic` retained as GRUB fallback. 4 phased-rollout packages (`fwupd`, `libfwupd3`, `python3-software-properties`, `software-properties-common`) remain in the upgradable queue — Ubuntu's phased-update mechanism, will land on the next unattended-upgrades cycle. See Security posture and `## Hardware & OS` sections above for the diff.

## Open tasks affecting this host

Pending work that affects this host is tracked in [`tasks/`](../../tasks/). For the current open set with priorities and statuses see [`tasks/_index.md`](../../tasks/_index.md). The following tasks reference this host's landscape file:

- ~~[T-0090](../../tasks/T-0090-prepare-pro-data-tech-server-as-ai-qadam-qa-instance.md)~~ — **DONE 2026-07-08 via run `2026-07-08-prepare-pro-data-tech-qa-as-ai-qadam-qa-001`** (Phases A–E only). Outcome succeeded; 10/10 verification checks PASSED. Docker 29.6.1 + Compose v5.3.1 installed; ai-qadam-test QA postgres container running on `127.0.0.1:3112` (loopback) → `5432` (container); healthcheck passing; host-loopback `SELECT 1` confirmed. Phases F–I (nginx + UFW 443/tcp + Cloudflare DNS + HTTPS verification) explicitly deferred to [T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md).
- [T-0090a](../../tasks/T-0090a-prepare-qadam-test-public-https-endpoint.md) — Add nginx + Cloudflare + HTTPS for `qadam-test.ai-dala.com` (Phases F–I of the original T-0090 plan). `kind: observation, status: observation, priority: P2`, `blocked_by: T-0090` (now done).
- ~~[T-0093](../../tasks/T-0093-harden-sshd-on-pro-data-tech-qa.md)~~ — **DONE 2026-07-08 via run `2026-07-08-harden-sshd-pro-data-tech-qa-001`.** Outcome succeeded; 21/21 verification checks passed. See Security posture section above. Closing this item removes the gating blocker for T-0094, T-0095, and T-0097.
- ~~[T-0094](../../tasks/T-0094-install-local-baseline-firewall-on-pro-data-tech-qa.md)~~ — **DONE 2026-07-08 via run `2026-07-08-install-ufw-pro-data-tech-qa-001`.** Outcome succeeded; 10/10 verification checks PASSED (after one re-execution — the first step-07 FAIL was caused by a nohup-orphaned `sleep 300` rollback timer; fix was `setsid` + `kill -9 -- -PGID` group-kill, documented in `/memories/repo/ufw-rollback-timer-process-group.md`). UFW active with deny-incoming / allow-outgoing / forward-ACCEPT (T-0090 Phases A–E 2026-07-08 flipped DROP→ACCEPT to enable Docker bridge forwarding) / IPv6-yes defaults; 22/tcp allowed (v4+v6) from any source per user decision 2026-07-08 (no source-IP allowlist; defense-in-depth comes from UFW only-22/tcp + AllowGroups sshusers + T-0095 fail2ban). Now matches sibling `hetzner-prod` / `ubuntu-16gb-nbg1-1` `FORWARD=ACCEPT` convention. Backups `/etc/default/ufw.bak`, `/tmp/ufw.pre-T0094.20260708T173602Z.bak/`, and `/etc/default/ufw.pre-T0090.20260708T184046Z.bak` preserved on host per project policy.
- ~~[T-0095](../../tasks/T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa.md)~~ — **DONE 2026-07-08 via run `2026-07-08-install-fail2ban-pro-data-tech-qa-001`.** Outcome succeeded; 7/7 verification checks passed. fail2ban 1.1.0-9 installed with sshd jail active (`banaction = iptables-multiport`, `maxretry=3`, `bantime=600s`, `findtime=600s`, `ignoreip` includes live mgmt workstation IP `178.89.57.135`). Pre-change backup preserved at `/etc/fail2ban.pre-T0095.20260708T182109Z.bak/`. See Security posture section above.
- ✅ [T-0096](../../tasks/T-0096-enable-auditd-with-sane-ruleset-on-pro-data-tech-qa.md) — Enable auditd with sane ruleset on pro-data-tech-qa (deferrable per T-0088). **DONE 2026-07-10 via run [`2026-07-10-enable-auditd-on-pro-data-tech-qa-001`](../../runs/2026-07-10-enable-auditd-on-pro-data-tech-qa-001/)**. Outcome succeeded; 8/9 V-checks PASSED. auditd 1:4.1.2-1build1 + audispd-plugins installed; project CIS-derived ruleset (15 keys, 67 kernel rules) loaded; daemon active + enabled; kernel audit subsystem loaded (`CONFIG_AUDIT=y`). V07 PARTIAL with documented architectural rationale; immutable flag deferred to T-0096a. Pre-install snapshot at `/var/backups/pre-T0096.20260710T123137Z/`.
- ~~[T-0097](../../tasks/T-0097-create-non-root-user-tvolodi-on-pro-data-tech-qa.md)~~ — **DONE 2026-07-08 via run `2026-07-08-create-operator-users-pro-data-tech-qa-001`.** Outcome succeeded; 16/16 verification checks passed. Three operator users created (`tvolodi`/`viktor_d`/`binali_r`); live SSH verified for `tvolodi`, server-side parse verified for `viktor_d`/`binali_r`. Multi-PC SSH acceptance criterion met. Closing this item removes the conceptual blocker for T-0090.
- [T-0098](../../tasks/T-0098-host-level-backup-strategy-for-pro-data-tech-qa.md) — Host-level backup strategy for pro-data-tech-qa (local-disk only; no paid provider snapshots; no off-site targets). `kind: observation, status: observation, priority: P3` (re-created 2026-07-08; deferred until role is assigned).
- ~~[T-0099](../../tasks/T-0099-apply-pending-kernel-upgrade-reboot-pro-data-tech-qa.md)~~ — **DONE 2026-07-10 via run `2026-07-10-apply-pending-kernel-upgrade-reboot-pro-data-tech-qa-001`.** Outcome succeeded; 9/9 verification checks PASSED. 9 pending apt upgrades applied (kernel + meta-packages); host rebooted into `7.0.0-27-generic` (downtime 6m 44s); previous kernel `7.0.0-14-generic` retained as GRUB fallback. `ai-qadam-test-db-1` came back `(healthy)`; all 5 services (ssh/ufw/fail2ban/docker/apparmor) `active`; `pg_isready` `accepting connections`; no auditd/dhclient/networkd/snapd errors in the prior-boot journal. Pre-reboot `pg_dump` (405 B) + `etc-snapshot.tar.gz` (148 453 B) + `pre-reboot-state.txt` (5924 B) preserved at `/var/backups/pre-T0099.20260710T061200Z/`. 4 phased-rollout packages (`fwupd`/`libfwupd3`/`python3-software-properties`/`software-properties-common`) remain in `apt list --upgradable`; not actionable today (Ubuntu phased-update design). Removing this item clears the conceptual blocker for T-0096 (auditd install) — which was sequenced after the kernel upgrade to ensure auditd lands on a stable, current kernel.

## Change log

| Date | Run ID | Change |
|---|---|---|
| 2026-07-08 | `2026-07-08-discovery-pro-data-tech-qa-001` | T-0090 (predecessor task): Initial discovery run. Host populated with hardware, OS, access, network, firewall, sshd, security, and backup findings. State: freshly provisioned Ubuntu 26.04 cloud image, no project services, no Docker, no nginx, no host firewall, no fail2ban. Multi-PC operator SSH access NOT currently possible (only provider key in `/root/.ssh/authorized_keys`). |
| 2026-07-08 | (user decision, no run) | Resolved 3 open questions from the initial discovery findings: (1) **host_id** stays `pro-data-tech-qa` (canonical; encodes provider + QA intent); (2) **root login policy** — root login kept permanently via `PermitRootLogin prohibit-password` + provider key in `/root/.ssh/authorized_keys` (break-glass); operators get dedicated accounts `tvolodi`, `viktor_d`, `binali_r` in the `sshusers` group with NOPASSWD sudo (T-0097). **T-0090 will not finalize `PermitRootLogin no`** as long as this decision stands. (3) **UFW source restrictions** — none. `ufw allow 22/tcp` from any source; defense-in-depth comes from UFW (only 22/tcp open), AllowGroups sshusers (T-0093), and fail2ban (T-0095). Captured in T-0093, T-0094, T-0097, and the "Open questions" + "What needs to happen" sections above. |
| 2026-07-08 | `2026-07-08-harden-sshd-pro-data-tech-qa-001` | sshd hardening applied — password auth disabled, root key-only login, AllowGroups sshusers, KEX/Ciphers/MACs tightened, sshusers group created (root only); see drop-ins /etc/ssh/sshd_config.d/40-*.conf | T-0093 |
| 2026-07-08 | `2026-07-08-create-operator-users-pro-data-tech-qa-001` | T-0097 done — operator users tvolodi (uid 1001), viktor_d (uid 1002), binali_r (uid 1003) created; sshusers group membership; NOPASSWD sudo configured; live SSH for tvolodi verified | T-0097 |
| 2026-07-08 | `2026-07-08-install-ufw-pro-data-tech-qa-001` | T-0094 done — UFW active (deny-in/allow-out/forward-DROP/IPv6-on); 22/tcp allowed from any source per user decision (no source-IP allowlist); backup `/tmp/ufw.pre-T0094.20260708T173602Z.bak/` + `/etc/default/ufw.bak` preserved; 10/10 verification checks PASSED on re-execution (after first step-07 FAIL due to nohup-orphaned rollback timer — fixed via `setsid` + `kill -- -PGID` group-cancellation); CRITICAL for T-0090 (Docker install): explicit `DEFAULT_FORWARD_POLICY="DROP"` divergence from sibling `ACCEPT` pattern — T-0090 executor must reconcile (flip to ACCEPT via sed+ufw reload, or set Docker `"iptables": false` in daemon.json) before installing Docker | T-0094 |
| 2026-07-08 | `2026-07-08-install-fail2ban-pro-data-tech-qa-001` | T-0095 done — fail2ban 1.1.0-9 installed with sshd jail; iptables-multiport banaction; mgmt workstation IP in ignoreip | T-0095 |
| 2026-07-08 | `2026-07-08-prepare-pro-data-tech-qa-as-ai-qadam-qa-001` | T-0090 Phases A–E done — Docker installed (engine 29.6.1 + Compose plugin v5.3.1); FORWARD policy reconciled DROP→ACCEPT (matches sibling convention); ai-qadam-test QA postgres container `ai-qadam-test-db-1` running healthy on `127.0.0.1:3112` → 5432; host-loopback SELECT 1 confirmed; 10/10 V-checks PASSED. Phases F–I (nginx + UFW 443/tcp + Cloudflare DNS + HTTPS verification) deferred to T-0090a. `role:` updated from `unassigned` to `ai-qadam-qa`. | T-0090 |
| 2026-07-10 | 2026-07-10-audit-host-pro-data-tech-qa-001 | audit-host run; 2 new findings (2 P2: kernel/reboot-required drift, Postgres container hardening gap); reconfirmed sshd/UFW/fail2ban/sudoers no drift; auditd/backups/nginx gaps already tracked as T-0096/T-0098/T-0090a | T-0099, T-0100 |
| 2026-07-10 | T-0099 done — 9 apt upgrades applied (incl. linux-image-7.0.0-27-generic), reboot into new kernel, downtime 6m 44s, all V-checks PASSED, backups in /var/backups/pre-T0099.20260710T061200Z/ | T-0099 |
| 2026-07-10 | T-0096 done — auditd 1:4.1.2-1build1 installed with project CIS-derived ruleset (15 keys, 67 rules); daemon active+enabled; V01-V09: 8 PASS, 1 PARTIAL (USER_AUTH/EXECVE event-classes absent due to NOPASSWD+key-only host config); pre-install snapshot /var/backups/pre-T0096.20260710T123137Z/ | T-0096 |
