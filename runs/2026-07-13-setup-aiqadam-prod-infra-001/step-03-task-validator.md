---
run_id: 2026-07-13-setup-aiqadam-prod-infra-001
step: 03
agent: task-validator
verdict: PASS
created: 2026-07-13T00:00:00Z
task_id: T-0111-setup-aiqadam-prod-deploy-infra-pro-data-tech-prod
inputs_read:
  - runs/2026-07-13-setup-aiqadam-prod-infra-001/step-01-task-reader.md
  - runs/2026-07-13-setup-aiqadam-prod-infra-001/step-02-landscape-reader.md
  - tasks/T-0111-setup-aiqadam-prod-deploy-infra-pro-data-tech-prod.md
  - workflows/infrastructure.md
artifacts_changed: []
next_step_hint: solution-designer must (1) treat the apex A-record change as a repoint of a live, shared, third-party-owned record — not a fresh create — and capture its current value (212.20.151.29, proxied true, record ID bf1113199732117bd147ebd87d6e356d) as a rollback reference per workflow rule 2 before mutating it; (2) resolve the apex tenant-routing question by reading apps/api/src/modules/tenants/tenant.middleware.ts from the cloned checkout as part of plan formation, mirroring how T-0110 resolved its OIDC/tenant findings during design rather than validation — do not defer this to execution or assume the qa-uz analogy holds; (3) confirm dedicated prod Postgres (new volume/container, no QA reuse), a free host port distinct from 9001, and additive-only nginx/Compose artifacts that cannot collide with /opt/penpot/ or its vhost file; (4) surface the git-ref choice and www-scope question as explicit NEEDS_APPROVAL asks, consistent with step 01/02's flags — this task is very unlikely to qualify for auto-approve PASS given estimated_blast_radius: high plus a shared-zone DNS mutation.
---

## Summary
T-0111 is validated and ready for the solution-designer: it is well-formed, in-scope for the infrastructure workflow, not already done, and does not conflict with any landscape fact requiring a different outcome — the apex-record repoint and the unconfirmed tenant-routing behavior are real risk items but are resolvable design-time gaps (source-read + user confirmation), not validation blockers.

## Details
### Validation results
1. **Well-formed: PASS** — The task names a concrete, verifiable end state: a specific git checkout path/repo, a specific dedicated-Postgres constraint, a specific host/port pairing to avoid (9001), a specific domain (`aiqadam.org` apex) and target IP (`95.46.211.224`), and a 13-item, fully itemized "what done looks like" checklist with an explicit external verification command (`curl -I https://aiqadam.org` → 200, plus a no-regression check on `penpot.aiqadam.org`). This is not a vague intent — it is a fully specified, testable target state.

2. **In-scope: PASS** — The task is Docker/Compose provisioning, nginx vhost configuration, Cloudflare DNS, and TLS certificate issuance, all on a host already in this repo's inventory (`landscape/hosts/pro-data-tech-prod.md`). Every item in the acceptance checklist maps directly to `workflows/infrastructure.md`'s "When this workflow applies" list (Docker/Compose changes, nginx config changes, Cloudflare DNS, TLS operations). No part of the task falls outside the infrastructure workflow's remit.

3. **Not already done: PASS** — Per the step-02 landscape summary: no `/opt/apps/` directory or app Compose project exists on `pro-data-tech-prod` yet (only the pre-existing `penpot` project); no app-registry "Production environment" section exists; `landscape/domains.md` has no apex-domain row; no TLS cert exists yet for bare `aiqadam.org`. The one DNS artifact that does exist (the apex A record) points at an unrelated third-party host, not at this task's target — so the task's specific end state (`aiqadam.org` → `95.46.211.224`, serving the AiQadam app) is unambiguously not yet in place.

4. **No conflict with current state: PASS (flagged)** — The apex `aiqadam.org` A record already existing and pointing at `212.20.151.29` (an undocumented third-party Coolify-like PaaS, confirmed defunct/unrelated to this repo's inventory per T-0110's investigation) is not a landscape fact that contradicts this task's goal — nothing in the landscape asserts "the apex must keep pointing at 212.20.151.29" as a requirement this repo must honor. It is, however, a materially different *type* of operation than T-0110's fresh `qa-uz` record create: this is a **repoint of a live, shared, third-party-owned record**, not additive DNS. This does not block validation, but it must be surfaced prominently to the solution-designer as a scope/risk item distinct from the task's already-HIGH blast-radius rating (which step 01/02 attribute primarily to Penpot co-residency) — the DNS step carries its own, separately-reasoned risk: mutating a record that an undocumented external party may still depend on for routing or mail-adjacent behavior, even though the current target host is confirmed dead. The designer should treat this as shared-resource surgery per `landscape/cloudflare.md`'s explicit warning, capture the pre-change record value as a rollback reference (workflow rule 2: "backup before destructive changes" — an A-record value overwrite is exactly this kind of destructive change to existing config), and consider recommending `NEEDS_APPROVAL` framing that names this repoint explicitly rather than folding it silently into the general high-blast-radius framing.

5. **Discoverable scope: PASS** — Nearly all open items (exact git ref, `www.aiqadam.org` scope, DB provisioning, free port choice, app-slice scope) are exactly the kind of gaps this workflow expects to resolve at design time (via landscape facts, user confirmation at plan-approval, or executor's documented judgment) — none of them represent a missing landscape fact that make the task undesignable. The one open technical unknown — whether the tenant-resolution middleware's leftmost-label parsing handles a bare 2-label apex Host header the same way it handled `qa-uz`'s 5-character label (both falling through to `DEFAULT_TENANT_CODE='uz'`) — is unconfirmed against source, but this is precisely the same category of app-behavior fact that T-0110 successfully resolved *during the design step* by reading `tenant.middleware.ts` directly from the cloned checkout, not during validation. Recommending the same path here: the solution-designer should read the middleware source (once the repo is cloned per acceptance criterion 1, or via a lightweight pre-clone/sparse-checkout if needed before finalizing the DNS/nginx plan) and resolve this as part of plan formation, not treat it as a validation-blocking critical unknown. This is a design-step source-verification task, not a missing-landscape-fact problem — the landscape reader could not have resolved it (it requires reading application source, not infrastructure state), and step 02 correctly flagged it as a task for downstream design rather than claiming it blocks landscape completeness.

6. **Workflow-specific rules respected: PASS** — (a) *Idempotency*: the plan as scoped (new Compose project, new checkout directory, new `.env`, new Postgres container/volume, new nginx vhost file, new DNS record value) is inherently re-runnable without half-configured states, provided the designer follows the additive-only constraints already stated in the task's Notes; the one non-idempotent-feeling step (the DNS repoint) is a single atomic value change, not a multi-step destructive sequence, and its rollback is simply "restore the previous A-record value," which is capturable and satisfiable. (b) *Backup before destructive changes*: the only destructive-adjacent action is the apex DNS record value overwrite (an existing record, not a new one) — the designer must have the executor capture the current record value/proxied-status/TTL as a documented rollback reference before mutating it; this is straightforward given `landscape/cloudflare.md` already documents the pre-change value. No other step in the task touches existing config (nginx, Compose, Postgres are all net-new, additive artifacts alongside Penpot's untouched ones). (c) *Verify in two places*: acceptance criterion 11 already requires both an on-host check (implied: container health, nginx config test) and an externally-observable HTTP probe for both the new domain and the pre-existing Penpot domain (regression check) — this satisfies the workflow's two-place verification rule directly and explicitly.

## Issues / risks
- **Apex DNS repoint is a distinct, higher-scrutiny sub-operation within this task** and should be called out by name in the solution-designer's plan and risk section, separately from the general "high blast radius due to Penpot co-residency" framing already established at step 01. The current dead target host means no live traffic is disrupted today, but the record is shared, third-party-owned, proxied infrastructure — the designer should not treat it as routine additive DNS work.
- **Tenant-routing behavior for the bare apex hostname remains unconfirmed** and directly threatens acceptance criterion 11 (`curl -I https://aiqadam.org` → 200). This is not a validation blocker — it is resolvable via source-read during design, per T-0110's precedent — but the solution-designer must not proceed to a final plan/approval ask without either (a) confirming the middleware's actual behavior by reading `apps/api/src/modules/tenants/tenant.middleware.ts`, or (b) explicitly flagging the residual uncertainty as a NEEDS_APPROVAL question to the user if source cannot be read before plan-approval time.
- **Git-ref selection and `www.aiqadam.org` scope remain open** and require explicit user confirmation at plan-approval (step 05) per the task's own Notes — the solution-designer should not proceed to auto-approved PASS without addressing both; combined with the DNS repoint and high blast radius, `NEEDS_APPROVAL` is the expected (not merely possible) outcome of step 04, consistent with step 01's own assessment.
- No conflicts found regarding dedicated Postgres (task explicitly forbids QA reuse and landscape confirms no existing prod DB artifact), port collision (only port 9001 is occupied; landscape flags the absence of a reserved-range convention as something the designer must document, not a blocker), or Compose/nginx additivity (Penpot's `/opt/penpot/` directory, `penpot` Compose project name, and `penpot.aiqadam.org` vhost file are all independently named/pathed from anything this task creates).

## Open questions (optional)
None from validation itself. Carried forward for the solution-designer to resolve (not blocking task readiness):
- Exact tagged/reviewed git ref for first prod deploy (requires user confirmation at plan-approval).
- `www.aiqadam.org` in scope or bare apex only (requires user confirmation).
- Confirmed tenant-middleware behavior for a bare apex Host header (requires source-read during design).
- Prod database provisioning: fresh empty DB assumed unless the user states otherwise.
