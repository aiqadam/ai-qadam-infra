---
run_id: 2026-07-19-install-mail-server-aiqadam-001
step: 03
agent: task-validator
verdict: PASS
created: 2026-07-19T00:12:00Z
task_id: T-0117-install-mail-server-aiqadam
inputs_read:
  - tasks/T-0117-install-mail-server-aiqadam.md
  - runs/2026-07-19-install-mail-server-aiqadam-001/step-01-task-reader.md
  - runs/2026-07-19-install-mail-server-aiqadam-001/step-02-landscape-reader.md
  - workflows/infrastructure.md
  - landscape/cloudflare.md
artifacts_changed: []
next_step_hint: step-04-solution-designer should resolve host placement (pro-data-tech-prod vs ubuntu-16gb-nbg1-1) with live DNSBL checks, decide DMARC day-one policy and MTA-STS scope with the user, and treat the apex SPF TXT record and the 22-record mail block as separate-but-related edits scoped one record at a time, matching the T-0110/T-0111 precedent already documented in cloudflare.md.
---

## Summary
Task T-0117 is validated: well-formed, in-scope for the infrastructure workflow, not already done, free of conflicts with documented landscape state, sufficiently discoverable (remaining gaps are normal live-discovery items for solution-design/execution, not missing landscape facts), and satisfiable under this workflow's idempotency/backup/dual-verification rules — verdict PASS.

## Details
### Validation results

1. **Well-formed: PASS** — The task's "What done looks like" checklist names concrete, verifiable end states: specific ports reachable with valid TLS, specific DNS record types created/updated with specific content requirements (DKIM keypair, SPF authorizing the new IP, MX repointed), a named test procedure (external send/receive, mail-tester.com score), and specific landscape files to update. This is materially more concrete than a vague-intent task like "make mail better" — every bullet is independently checkable.

2. **In-scope: PASS** — Per [workflows/infrastructure.md](../../workflows/infrastructure.md), the workflow applies to "any change to a managed host," "Docker/Compose changes on the server," "Cloudflare DNS... changes," "TLS certificate operations," and "firewall rules" — this task is squarely all of those at once. Both candidate hosts (`pro-data-tech-prod`, `ubuntu-16gb-nbg1-1`) and the `aiqadam.org` Cloudflare zone are explicitly in this repo's managed scope per `CLAUDE.md`. No part of the task (install, DNS, firewall, TLS, landscape docs) falls outside what this workflow and this repo's charter cover.

3. **Not already done: PASS** — Per landscape-reader step 02, neither host candidate runs any mail-related service today: `pro-data-tech-prod` has "no mailcatch or any existing mail-related service beyond Penpot's internal dev/test mailcatcher (loopback-only, unrelated to production mail)"; `ubuntu-16gb-nbg1-1` is `role: unassigned`, blank. The only existing mail presence in the landscape is the 22 dead third-party DNS records plus the SES/Resend transactional integrations — neither of which constitutes "real, human-readable mailboxes... self-hosted on infrastructure this repo owns," which is the task's actual target state. Target state is not in place.

4. **No conflict with current state: PASS** — Checked directly against [landscape/cloudflare.md](../../landscape/cloudflare.md) rather than relying solely on the landscape-reader's summary. The mail-records table there is explicitly headed "Mail records (Stalwart mail server + Snappymail webmail — **NOT managed by this repo**)" and the zone-level note states this repo owns only 3 of 33 records. The task's plan is to replace/repoint exactly this NOT-managed mail block plus the already-mail-labeled apex SPF TXT record — it does not touch the wildcard, the 5 tunnel/pages records, or any of the 3 currently-repo-owned core web records. This is a coherent, bounded subset of the zone; nothing in the task contradicts a landscape fact. On the specific concern raised — is the "old server is dead" premise merely asserted or independently supported: cloudflare.md's own "212.20.151.29 investigation" (dated 2026-07-13, five days before the task's 2026-07-18 port probes) independently found the host serves an unrelated 302→"global.aiqadam.org" (undefined) → 503 catch-all response, "no credentials or access to that host exist in this repo," and concluded it is "entirely separate from and undocumented in this repo's hosts/". This is real corroboration at the HTTP layer from a different investigation, obtained for an unrelated reason (T-0110/T-0111 DNS work), not a circular restatement of the task's own claim. It does not independently re-confirm the 25/443/993 port-level probe cited in the task's Why section — landscape-reader flagged this precisely as a gap (see check 5) — but "HTTP dead + unrelated PaaS + no credentials in this repo" is already sufficient to establish the mail service is not something this repo can or should keep operating, regardless of whether SMTP/IMAP happen to still answer. No conflict.

5. **Discoverable scope: PASS** — Landscape-reader's step-02 handoff explicitly enumerates the live-discovery gaps (DNSBL status of both candidate IPs, exact port-level reachability of the dead host, Stalwart-vs-Mailcow footprint specifics, SES integration's live usage detail) and correctly characterizes them as "normal for a design/build task" rather than missing landscape facts. None of these are critical unknowns that block *validating* the task — they are exactly the kind of pre-execution checks the task itself already calls for (e.g., "Chosen host's public IP checked against major DNSBLs... before committing"). The landscape files needed to reason about scope (both host files, cloudflare.md, domains.md, secrets-inventory.md) are all present, current (`last_verified` within 30 days), and were read. Scope is discoverable.

6. **Workflow-specific rules respected: PASS** — Checked against [workflows/infrastructure.md](../../workflows/infrastructure.md) "Workflow-specific rules":
   - *Idempotency required*: satisfiable — DNS record creation/update, Docker Compose install, and firewall rule addition are all naturally idempotent operations (re-runnable via freshness-check-then-PATCH, `docker compose up -d`, `ufw allow` semantics), consistent with the T-0110/T-0111 precedent already used in this exact zone.
   - *Backup before destructive changes*: satisfiable — the task itself already requires a backup mechanism for mail data, and the only destructive DNS operations (deleting orphaned old-host records) are on records this repo does not own operationally but are documented pre-change in cloudflare.md, giving the executor a rollback reference.
   - *Verify in two places*: satisfiable — the task's checklist already requires both host-side verification (ports reachable, TLS valid, mailbox created) and externally-observable verification (external send/receive test, mail-tester.com score, DNS lookups), which maps directly onto this rule.
   No workflow-specific rule is unsatisfiable for this task.

### Additional checks requested by the orchestrator

- **Host-candidate feasibility (no hard blocker on either):** Confirmed via landscape-reader's findings. `pro-data-tech-prod` has ample capacity (28 GiB free RAM, 336 GB free disk), is already hardened, and has a known-and-documented risk (Docker's demonstrated ability to bind `0.0.0.0` and bypass UFW, seen with Penpot port 9001 and aiqadam-prod postgres 3114) that is a design constraint, not a blocker — the solution-designer must bind mail container ports deliberately. `ubuntu-16gb-nbg1-1` has adequate capacity (143 GB free disk, 15 GiB RAM), is currently unhardened (password auth enabled, no `AllowGroups`) and lacks Docker/nginx, but these are standard bootstrap steps this workflow already handles elsewhere (e.g., original hardening of `pro-data-tech-prod`/`pro-data-tech-qa`) — not a structural blocker, just additional plan steps (sshd hardening + Hetzner Cloud Firewall rule additions) the solution-designer must include if this host is chosen. Neither candidate has a disqualifying fact in the landscape.
- **Shared Cloudflare zone compatibility:** Confirmed feasible. The zone's own documentation already partitions records into "core web" (3 repo-owned), "mail" (22, explicitly NOT repo-owned today but exactly the target of this task), and "tunnel/pages" (5, unrelated) tables. The task's DNS surface (the 22 mail records + the apex SPF TXT) is precisely the "mail" partition plus one already mail-labeled record outside it; it does not touch any of the other 8 records. The repo has already executed two single-named-record-at-a-time operations in this same zone (T-0110 create+delete, T-0111 apex repoint) with full pre/post zone-dump diffing to prove no collateral changes — the same pattern applies directly here and cloudflare.md's own Notes section explicitly says to "coordinate before assuming exclusive control of the zone" and "scope any future Cloudflare work in this zone to single, explicitly-named record operations unless a human confirms broader changes are safe," which the task's own Notes section already commits to verbatim. No structural conflict with the shared-ownership constraint.
- **Task scope coherence (single task vs. split):** Coherent as a single task. Install, cutover, and DNS are sequentially interdependent (DNS cannot be finalized without knowing the chosen host's IP and generated DKIM key; deliverability testing cannot happen without both the server running and DNS cut over), and the task's own Notes correctly identify the MX repoint as requiring a separate, explicit approval gate within step 05 rather than being pulled into a different workflow run. Splitting into "install" and "cutover" as two separate tasks would create an artificial hard dependency between two task files for no isolation benefit, since the blast-radius-sensitive part (DNS/MX) already gets its own approval checkpoint inside this task's step 05. No split needed.

## Issues / risks
- The task's premise that the old mail server is "dead" is well-corroborated by two independent landscape investigations (HTTP 302→503/PaaS-catchall behavior, no credentials in this repo) plus the task's own 2026-07-18 port probes, but the SMTP/IMAP-level (25/443/993) unreachability is sourced only from the task file itself, not independently re-verified in any landscape file. This is not a blocking conflict (see check 4) but the solution-designer/executor should re-probe those ports live before or during execution as routine due diligence, not simply trust the task file's one-day-old claim indefinitely.
- Both host candidates carry real, non-trivial follow-on work regardless of which is chosen (Docker-UFW-bypass mitigation on `pro-data-tech-prod`; sshd hardening + dual firewall-layer coordination on `ubuntu-16gb-nbg1-1`) — the solution-designer must make this tradeoff explicit at step 04/05 rather than treating either host as a clean default.
- The `resend._domainkey.aiqadam.org` DKIM record (a second transactional-email integration, unexplained in any landscape file) should not be touched by this task; flagging forward so the solution-designer does not assume SES is the only adjacent integration to consider.

## Open questions (optional)
none — no BLOCKED-triggering gap found; all open questions in the task file itself (host placement, hostname reuse, DMARC day-one policy, MTA-STS scope) are correctly scoped as step-04/step-05 decisions, not validation blockers.
