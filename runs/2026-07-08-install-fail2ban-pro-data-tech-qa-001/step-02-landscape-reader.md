---
run_id: 2026-07-08-install-fail2ban-pro-data-tech-qa-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-07-08T00:00:00Z
task_id: T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa
inputs_read:
  - runs/2026-07-08-install-fail2ban-pro-data-tech-qa-001/step-01-task-reader.md
  - landscape/README.md
  - landscape/hosts/pro-data-tech-qa.md
  - landscape/hosts/ubuntu-16gb-nbg1-1.md
  - landscape/services.md
  - tasks/T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa.md
  - tasks/T-0084-install-fail2ban-on-ubuntu-16gb-nbg1-1.md
  - tasks/_index.md
  - runs/2026-06-27-install-fail2ban-001/step-04-solution-designer.md
  - runs/2026-06-27-install-fail2ban-001/step-07-execution-validator.md
artifacts_changed: []
next_step_hint: task-validator
---

## Summary
Host `pro-data-tech-qa` (95.46.211.230) is a freshly-provisioned Ubuntu 26.04 cloud image that has now received its full T-0090-baseline hardening: UFW active deny-by-default + 22/tcp open (T-0094 done 2026-07-08), sshd hardened with `PasswordAuthentication no` + `AllowGroups sshusers` + `PermitRootLogin prohibit-password` (T-0093 done 2026-07-08, 21/21 checks passed), and three operator users (`tvolodi`/`viktor_d`/`binali_r`) provisioned in the `sshusers` group with NOPASSWD sudo (T-0097 done 2026-07-08). **fail2ban is NOT installed** — no `fail2ban-server` binary, no `/etc/fail2ban/`, no service unit. The `fail2ban` apt package is available at version `1.1.0-9` (matching sibling `ubuntu-16gb-nbg1-1`'s install) and `/var/log/auth.log` exists and is being written (5,119,397 bytes, owner `syslog:adm`, mode 0640, freshest entry 2026-05-05 cloud-init bootstrap through today). The sibling `landscape/hosts/ubuntu-16gb-nbg1-1.md` records the proven T-0084 jail config (maxretry=3, bantime=600s, findtime=600s, `ignoreip` includes `178.89.57.135`, `banaction=iptables-multiport`) at `/etc/fail2ban/jail.d/sshd.local`. Two live-discovery gaps remain for the solution-designer (step 04): (a) the iptables backend on this host (nft shim vs legacy — drives `banaction` choice); (b) the live management-workstation outbound IP for the `ignoreip` line (must not hardcode prod's `5.250.151.158`; the sibling pattern used `178.89.57.135`, which is the same management workstation and is the most likely current value but must be re-verified). Neither gap is a step-02 blocker; the executor can resolve both at run time.

## Details
### Relevant facts (sourced from landscape)

- **Host identity:** `pro-data-tech-qa`, IPv4 `95.46.211.230`, provider pro-data.tech (NOT Hetzner — no Hetzner Cloud Firewall, no Hetzner API), OS Ubuntu 26.04 LTS "Resolute Raccoon", kernel `7.0.0-14-generic`, `role: unassigned` (intended: `ai-qadam` QA). `last_verified: 2026-07-08`, `status: populated`. — _source: `landscape/hosts/pro-data-tech-qa.md` (frontmatter)_
- **T-0093 sshd hardening DONE 2026-07-08** (21/21 verification checks PASSED, run `2026-07-08-harden-sshd-pro-data-tech-qa-001`). Effective sshd config: `Port 22`, `PermitRootLogin prohibit-password`, `PasswordAuthentication no`, `KbdInteractiveAuthentication no`, `MaxAuthTries 3`, `LoginGraceTime 30`, `X11Forwarding no`, `ClientAliveInterval 300`, `ClientAliveCountMax 2`, `AllowGroups sshusers`. Hardened KEX/Ciphers/MACs allow-lists (no SHA-1, no CBC, no 3DES, no RC4). Two project drop-ins installed at `/etc/ssh/sshd_config.d/40-disable-password.conf` and `40-ai-dala-infra.conf`; first-wins semantics confirmed (sort before cloud-init's `60-cloudimg-settings.conf`). — _source: `landscape/hosts/pro-data-tech-qa.md` § Security posture + § What needs to happen #2_
- **T-0094 UFW active DONE 2026-07-08** (10/10 verification checks PASSED on re-execution, run `2026-07-08-install-ufw-pro-data-tech-qa-001`). `Status: active`, `Default: deny (incoming), allow (outgoing)`, `DEFAULT_FORWARD_POLICY="DROP"` (deliberate divergence from sibling hosts — will be reconciled when T-0090 installs Docker), `IPV6=yes`. Two 22/tcp ALLOW rules (v4 + v6) with comment `sshd - operator access T-0094 baseline`, no source-IP allowlist per user decision 2026-07-08. iptables v4 + ip6tables v6 INPUT chain policy `DROP` with UFW chains loaded and live packet counters proving active filtering. — _source: `landscape/hosts/pro-data-tech-qa.md` § Network_
- **T-0097 operator users DONE 2026-07-08** (16/16 verification checks PASSED, run `2026-07-08-create-operator-users-pro-data-tech-qa-001`). Three operator users in the `sshusers` group: `tvolodi` (uid 1001, workstation live-SSH-verified 2026-07-08), `viktor_d` (uid 1002, server-side parse verified), `binali_r` (uid 1003, server-side parse verified). NOPASSWD sudo via `/etc/sudoers.d/90-<user>` (mode 0440, root:root, validated by `visudo -c`). Multi-PC SSH acceptance criterion **met** for `tvolodi`; deferred to operators' own workstations for `viktor_d`/`binali_r`. — _source: `landscape/hosts/pro-data-tech-qa.md` § Operator users_
- **fail2ban: NOT installed.** Per `landscape/hosts/pro-data-tech-qa.md` § Security posture: "fail2ban: NOT installed. Tracked as T-0095." Live probe confirms: `which fail2ban-server` returns exit 1 (empty), `ls -la /etc/fail2ban` returns `No such file or directory`. — _source: `landscape/hosts/pro-data-tech-qa.md` § Security posture; live probe `2026-07-08T18:1xZ`_
- **fail2ban apt package availability: confirmed.** `apt-cache policy fail2ban` returns `Candidate: 1.1.0-9` from `http://archive.ubuntu.com/ubuntu resolute/universe amd64 Packages` (version table 1.1.0-9 priority 500). Matches the version `ubuntu-16gb-nbg1-1` installed (T-0084 done 2026-06-27). — _source: live probe on 95.46.211.230, 2026-07-08_
- **Auth log path: `/var/log/auth.log` exists and is being written.** Live probe: file is 5,119,397 bytes, mode 0640, owner `syslog:adm`, mtime `2026-07-08 18:15`. Oldest entry captured: `2026-05-05T05:20:53Z` (cloud-init bootstrap). This is the canonical Ubuntu path fail2ban's stock `/etc/fail2ban/filter.d/sshd.conf` watches by default — no filter customization required. — _source: live probe on 95.46.211.230, 2026-07-08_
- **rsyslog + ssh + systemd-journald all `active`.** Live probe: `systemctl is-active rsyslog` → `active`, `systemctl is-active ssh` → `active`, `systemctl is-active systemd-journald` → `active`. The presence of rsyslog is informational — fail2ban's stock sshd filter reads `/var/log/auth.log` directly (not journald), so a journald-only host would also work, but the rsyslog+auth.log pairing is the conventional and most-compatible layout. — _source: live probe on 95.46.211.230, 2026-07-08_
- **Sibling fail2ban pattern (T-0084 on `ubuntu-16gb-nbg1-1`, done 2026-06-27):** `/etc/fail2ban/jail.d/sshd.local` created (169 bytes, mode 0644, root:root, mtime 2026-06-27 06:13) with `[sshd] enabled = true`, `maxretry = 3`, `bantime = 600`, `findtime = 600`, `ignoreip = 127.0.0.1/8 ::1 178.89.57.135` (management-workstation outbound IP, live-verified via `api.ipify.org` — distinct from prod's `5.250.151.158`), `banaction = iptables-multiport` (backend: iptables 1.8.11 / nf_tables). Service `active` + `enabled` at boot. `f2b-sshd` iptables chain present (`tcp, multiport dports 22`). 2 IPs already banned at install from journal-history import: `14.103.127.232`, `45.148.10.240`. External BatchMode SSH from management workstation succeeded, proving the workstation is not self-banned. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md` § Access + § SSH hardening tooling on host; `tasks/T-0084-install-fail2ban-on-ubuntu-16gb-nbg1-1.md` § Result; `runs/2026-06-27-install-fail2ban-001/step-07-execution-validator.md`_
- **T-0095 task frontmatter:** `kind: task, status: pending, priority: P2, blocked_by: [T-0093-harden-sshd-on-pro-data-tech-qa]`. T-0093 status is `done` (verified in `tasks/_index.md` row 63); T-0093 is the sole blocker; T-0095 is unblocked. Workflow: `infrastructure`. `estimated_blast_radius: low`. `estimated_reversibility: full`. `affects: [landscape/hosts/pro-data-tech-qa.md, landscape/services.md]`. — _source: `tasks/T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa.md` (frontmatter); `tasks/_index.md` rows 43 (T-0095) + 63 (T-0093)_
- **T-0095 acceptance criteria (verbatim from task body):** (a) fail2ban installed via `apt install fail2ban`; (b) `/etc/fail2ban/jail.d/sshd.local` with `enabled = true`, `maxretry = 3`, `bantime = 600s`, `findtime = 600s`, `ignoreip = 127.0.0.1/8 ::1 178.89.57.135`; (c) `banaction = iptables-multiport`; (d) `fail2ban-client status sshd` returns the jail active; (e) `fail2ban.service` active and enabled at boot; (f) brute-force simulation: 4 failed SSH attempts → IP banned within `findtime + bantime`; (g) `landscape/hosts/pro-data-tech-qa.md` updated (remove "fail2ban NOT installed" note, mark "What needs to happen" #5 done, update SSH hardening tooling line) and `landscape/services.md` `## pro-data-tech-qa` systemd-units table gains a `fail2ban.service` row. — _source: `tasks/T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa.md` § What done looks like_
- **User decision pre-recorded:** "just go" delegation per step-01 handoff — no approval gate needed. Task Notes section explicitly says: "Reuse the run structure and the jail config verbatim" from T-0084. — _source: `tasks/T-0095-install-fail2ban-with-sshd-jail-on-pro-data-tech-qa.md` § Notes; `runs/2026-07-08-install-fail2ban-pro-data-tech-qa-001/step-01-task-reader.md`_

### Stale or stub files encountered
None. `landscape/hosts/pro-data-tech-qa.md` is `last_verified: 2026-07-08` (today); `landscape/hosts/ubuntu-16gb-nbg1-1.md` is `last_verified: 2026-06-27` (11 days old — within the 30-day freshness window). No stub files; all status fields are `populated`.

### Gaps requiring live discovery

1. **iptables backend on pro-data-tech-qa (drives `banaction` choice).** The T-0084 design probed `iptables -V` + `update-alternatives --list iptables` to pick `iptables-multiport` (nf_tables shim) vs `nftables-multiport`. pro-data-tech-qa is also Ubuntu 26.04 and almost certainly ships the same nft shim, but the executor should not assume — it must probe and parameterize. The T-0084 design's sub-step 7a fallback (sed-swap banaction) handles the rare case where the first choice is wrong. Resolvable at executor run time; not a step-02 blocker.
2. **Live management-workstation outbound IP for the `ignoreip` line.** The T-0084 design explicitly forbids hardcoding prod's `5.250.151.158` and recorded `178.89.57.135` via `api.ipify.org` at run time. The same management workstation is being used for this run (no workstation change documented since 2026-06-27) so `178.89.57.135` is the strongly-expected value — but the executor must re-verify at run time and substitute the live value into the `ignoreip` line. The T-0095 task body even names `178.89.57.135` as the literal `ignoreip` entry, which is consistent with this assumption but should be re-verified for the same reason. Resolvable at executor run time; not a step-02 blocker.

## Issues / risks

- **T-0095 may not block brute-force as much as it would have pre-T-0093.** T-0093 already enforced `PasswordAuthentication no` + `AllowGroups sshusers`, so password-auth brute-force is not possible — fail2ban will only see key-failed attempts (typos, misconfigured `~/.ssh/`, expired/missing pubkeys, etc.) which are lower-volume than the prod-hetzner baseline saw pre-T-0005. The residual value is banning repeated key-failed attempts to reduce noise in `/var/log/auth.log` and as a baseline-consistency play with `hetzner-prod` and `ubuntu-16gb-nbg1-1`. The task correctly scopes itself to "sshd jail only, standard config"; the executor should not add additional jails (recidive, apache, postfix, etc.) unless the user re-scope. This is informational, not a step-02 blocker.
- **`banaction = iptables-multiport` vs UFW on the same host (low, pre-validated in T-0084).** T-0084's design step 8 explicitly verified the `f2b-sshd` chain is present alongside UFW's INPUT rules and the two layers coexisted cleanly on `ubuntu-16gb-nbg1-1`. pro-data-tech-qa also runs UFW (T-0094), so the same coexistence is expected. The execution-validator (step 07) is the canonical place to re-verify this on pro-data-tech-qa. Not a step-02 blocker.
- **No "just go" approval gate is recorded in the workflow file.** The step-01 handoff asserts the user pre-approved via "just go" delegation; the task body does not record this explicitly. The executor should treat the run as `NEEDS_APPROVAL`-exempt (state-changing on a managed host always requires approval per `shared/approval-protocol.md`'s "Always `NEEDS_APPROVAL` list") **only if** the user has already signaled approval out-of-band. Otherwise, the standard step-05 user approval is required. This is a step-04 / step-05 concern, not a step-02 blocker; the landscape reader just notes the ambiguity.
- **`landscape/hosts/ubuntu-16gb-nbg1-1.md` is 11 days old (2026-06-27 → 2026-07-08).** Within the 30-day freshness window — not stale per the step-02 freshness rule. Recorded for awareness only.

## Open questions
None for step 02. The two live-discovery gaps (iptables backend, management-workstation outbound IP) are flagged for the executor and are resolvable in a single SSH session at run time; they are not step-02 blockers.
