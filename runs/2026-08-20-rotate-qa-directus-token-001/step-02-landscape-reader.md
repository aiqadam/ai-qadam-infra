---
run_id: 2026-08-20-rotate-qa-directus-token-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-08-20T00:00:00Z
task_id: T-0137-rotate-qa-directus-admin-token
inputs_read:
  - runs/2026-08-20-rotate-qa-directus-token-001/step-01-task-reader.md
  - tasks/T-0137-rotate-qa-directus-admin-token.md
  - landscape/README.md
  - landscape/hosts/pro-data-tech-qa.md
  - landscape/services.md
  - shared/app-registry.md
artifacts_changed: []
next_step_hint: The landscape does not document a rotation mechanism for Directus's own admin-user credential (password/token), nor which credential the app containers use for their own Directus reads — both are real gaps step 03/04 must plan to discover live, not assume. secrets-inventory.md does not exist in this checkout (git-ignored, never committed) — step 08's later "record rotation date" update will be a first-time entry, not an edit, unless a local uncommitted copy already exists on the operator's machine.
retry_of: null
---

## Summary

The landscape documents `aiqadam-qa-directus-1` (`directus/directus:11`) as a host-networked container in the `aiqadam-qa` Compose project on `pro-data-tech-qa`, confirmed running (previously undocumented until T-0126, 2026-07-27) but with **no admin-credential provisioning detail recorded anywhere** — no mention of `DIRECTUS_TOKEN`, `DIRECTUS_ADMIN_TOKEN`, `DIRECTUS_ADMIN_PASSWORD`, no rotation mechanism, and no documented linkage between this credential and how `qa.aiqadam.org`'s own app containers (`api`, `web-next`) read from Directus. The only credential-adjacent precedent in the landscape is for a *different* system (Authentik), where the landscape explicitly states "no persisted Authentik admin credential exists in `secrets-inventory.md` for QA" and admin access instead goes through `docker exec ... ak shell`. `landscape/secrets-inventory.md` itself does not exist in this checkout — `landscape/README.md` documents it as git-ignored and never committed, to be created locally by operators — so no prior entry for this secret (or any secret) could be checked; this is expected repo state, not a gap in this read. These are real information gaps the downstream plan must close via live discovery on `pro-data-tech-qa`, not assumptions.

## Details

### Relevant facts (sourced from landscape)

- `aiqadam-qa-directus-1` runs image `directus/directus:11`, host-networked (`network_mode: host`), part of Compose project `aiqadam-qa`, Compose file `/opt/apps/aiqadam-qa/deploy/docker-compose.qa.yml`; host port not enumerated (host-networked, no published-port entry recorded). — _source: `landscape/hosts/pro-data-tech-qa.md`, container table under "AiQadam application stack (aiqadam-qa)"; also `landscape/services.md` running-containers table_
- The container was up 9 days at the 2026-07-27 discovery time and was confirmed **healthy**, but it was **previously entirely undocumented** — the landscape's prior state only listed `oidc-stub` + `api` for this Compose project; Directus (plus web-next, authentik-server, authentik-worker, redis) was discovered as a pre-existing gap, not a new deployment. — _source: `landscape/hosts/pro-data-tech-qa.md`, "AiQadam application stack" intro and Change log entry for T-0126_
- The app's single env file for the whole `aiqadam-qa` stack is `/opt/apps/aiqadam-qa/deploy/.env` (owner `tvolodi:aiqadam-qa-secrets`, mode 640 as of T-0112) — this is the same file named in the task's exposure narrative and is where `DIRECTUS_TOKEN`/`DIRECTUS_ADMIN_TOKEN`/`DIRECTUS_ADMIN_PASSWORD` live. The landscape enumerates only two other named vars in this file (`WEB_BASE_URL`, `OIDC_REDIRECT_URI`) and two named secrets (`aiqadam-qa-jwt-signing-secret`, `aiqadam-qa-internal-api-token`, both name-only per `secrets-inventory.md` convention) — Directus's own admin token/password are **not** enumerated anywhere in the landscape's list of this file's contents. — _source: `landscape/hosts/pro-data-tech-qa.md`, "AiQadam application stack" bullet on the Env file; `shared/app-registry.md`, QA table row "App env file"_
- Access to the `deploy/.env` file on-host: group-readable by the `aiqadam-qa-secrets` group (members `tvolodi`, `deploy`); `deploy` (the CI user) also has this access, but has **no sudo grant** and is restricted to the forced `deploy.sh` command over SSH (`no-pty`, `command=` override) — i.e. `deploy` cannot get an interactive shell to hand-edit `.env`. Interactive edits would go through an operator (`tvolodi`/`viktor_d`/`binali_r`, all NOPASSWD sudo, all in `sshusers`) via SSH. — _source: `landscape/hosts/pro-data-tech-qa.md`, "CI/CD deploy user" and "Operator users" subsections_
- No documented Directus-specific rotation mechanism (UI vs REST `/users/me`/`/auth` vs CLI) exists anywhere in the landscape for either QA or prod. The only parallel documented rotation-adjacent precedent in the file is for **Authentik**, not Directus: "Admin access mechanism: `docker exec -i aiqadam-qa-authentik-server-1 ak shell` (Django management shell) ... no persisted Authentik admin credential exists in `secrets-inventory.md` for QA." No equivalent sentence exists for Directus. — _source: `landscape/hosts/pro-data-tech-qa.md`, Authentik container table row_
- No documented restart/reload requirement for Directus specifically after an env change. The landscape's only documented precedent for changing this stack's `.env` and needing a container action is T-0125 (2026-07-29): after editing `AUTHENTIK_ADMIN_URL` in this same `deploy/.env`, the fix required `docker compose up -d api` to recreate just the `api` container to pick up the env change — implying compose-level env vars here are not hot-reloaded and require an explicit container recreate. No equivalent has ever been exercised against `aiqadam-qa-directus-1`. — _source: `landscape/hosts/pro-data-tech-qa.md`, Change log entry for T-0125_
- **App-side credential-consumer question (blast-radius on the live app) — NOT documented.** Neither `landscape/hosts/pro-data-tech-qa.md`, `landscape/services.md`, nor `shared/app-registry.md` states which credential `aiqadam-qa-api-1` or `aiqadam-qa-web-next-1` use to read from Directus at runtime. `shared/app-registry.md`'s QA table (last substantively updated 2026-07-17, before Directus/Authentik/web-next were even known to be running) explicitly says of that era's scope: "OIDC login and Directus-CMS-backed routes are therefore non-functional in this environment by design; schema-valid placeholder OIDC/Directus env vars satisfy boot-time validation only" — but this predates T-0126's 2026-07-27 discovery that Directus-backed routes are now live in this environment ("the earlier 'out of scope for this environment' characterization for OIDC and Directus is superseded by this discovery"), so the app-registry.md text is stale on this specific point and should not be relied on for the current app-Directus relationship. — _source: `shared/app-registry.md`, QA table "Scope decision" row; `landscape/hosts/pro-data-tech-qa.md`, "AiQadam application stack" Scope bullet_
- Confirms the task's own framing: this is genuinely unconfirmed in the landscape, matching step 01's identified information gap verbatim ("Which credential `qa.aiqadam.org`'s running app container actually uses for its own Directus reads is unconfirmed"). — _source: `runs/2026-08-20-rotate-qa-directus-token-001/step-01-task-reader.md`_

### Stale or stub files encountered

- `landscape/hosts/pro-data-tech-qa.md` — `last_verified: 2026-07-29`, status `populated`. 22 days old as of today (2026-08-20) — **stale by the 30-day threshold is not yet crossed, but is approaching it** (8 days short of the 30-day flag). Not flagged as stale per the literal rule, but the Directus-relevant content within it (env-var enumeration, container discovery) is itself dated 2026-07-17/2026-07-27, i.e. actually older than the file's own `last_verified` stamp, which was bumped by an unrelated Authentik-URL fix (T-0125) that didn't touch Directus documentation at all.
- `landscape/services.md` — `last_verified: 2026-08-17`, status `populated`. Fresh (3 days old), well within threshold. Its pro-data-tech-qa section duplicates (does not add to) the Directus facts already covered above.
- `shared/app-registry.md` — `last_updated: 2026-07-17` (no `last_verified` frontmatter field — this file uses a different, non-landscape schema per its own header, so the landscape-reader's staleness rule technically doesn't apply to it, but it is 34 days old and, as noted above, is substantively out of date regarding Directus scope for QA).
- `landscape/secrets-inventory.md` — **does not exist in this checkout.** Per `landscape/README.md`: "Git-ignored — never committed. Operators must create this file locally." This is documented, expected repo state, not a stub/populate-status file with frontmatter to check — there is nothing to read, and per the landscape-reader's read-only-inventory-reference rule, no value would be quoted from it even if present.

### Gaps requiring live discovery

- Directus admin credential rotation mechanism: whether to rotate via Directus admin UI, REST (`/users/me` password change + regenerate static token, or `/auth` flows), or Directus CLI inside the container — not documented, must be determined live (e.g. `docker exec` into `aiqadam-qa-directus-1`, or its REST API, per Directus 11's own mechanism).
- Whether `aiqadam-qa-directus-1` needs a restart/recreate after the `.env` change to pick up new `DIRECTUS_TOKEN`/`DIRECTUS_ADMIN_TOKEN`/`DIRECTUS_ADMIN_PASSWORD` values, or whether Directus's own admin-user DB record must be separately updated (static tokens in Directus are typically stored in the `directus_users` table, not read fresh from env on every request) — the T-0125 precedent (`docker compose up -d api`) is suggestive by analogy for the compose-env-reload mechanic in general, but is not confirmatory for Directus's token-storage model specifically.
- Which credential (if any distinct from the admin token) `aiqadam-qa-api-1` and/or `aiqadam-qa-web-next-1` use for their own Directus reads — completely undocumented; must be discovered by reading the app's own env-var usage / source, or by inspecting `/opt/apps/aiqadam-qa/deploy/.env` var names live on host (without printing values — see Issues/risks below).
- Confirmation that `DIRECTUS_TOKEN` and `DIRECTUS_ADMIN_TOKEN` are the same secret under two names (task's own checklist item; previously observed during T-0136 but explicitly flagged by step 01 as not yet independently re-verified this run).
- Whether the old token, once rotated, needs revocation at the Directus DB/API level distinct from the `.env` overwrite (per step 01's Issues/risks: Directus may cache tokens or retain the old static token in the admin user record even after `.env` is overwritten) — no landscape fact confirms or refutes Directus 11's specific caching/storage behavior for static tokens.

## Issues / risks

- **No documented rotation mechanism means step 03/04 cannot design a fully mechanical plan from landscape alone** — the plan will need to include a live-discovery sub-step (e.g., reading Directus 11's admin API docs or probing the container) before the executor step, or the executor itself must do reconnaissance-then-act with the same output-hygiene discipline flagged by step 01 (never print matched secret lines; use `-o` capturing only variable names, never rely on `-v` exclusion to suppress a value).
- **App blast-radius is unconfirmed from landscape and must not be assumed zero.** T-0126 (2026-07-27) established Directus-backed routes are now live and no longer "out of scope," directly contradicting `shared/app-registry.md`'s older "non-functional by design" text for the same environment — any plan step that leans on the app-registry.md "Directus is out of scope" framing would be reasoning from stale data. Step 04 must treat the app's dependency on this credential as an open question requiring a live check (e.g., inspect `api`/`web-next` env-var references or source, or test app-facing Directus routes before/after rotation) rather than inferring low blast radius from the landscape alone.
- **Directus admin access precedent for this stack is thin.** The only sibling credential-management precedent recorded (Authentik) explicitly avoids persisting any credential in `secrets-inventory.md` and instead documents a `docker exec ... shell`-based admin path — if Directus follows a similar operational pattern (no persisted admin credential doc, DB-level user record), the executor should not expect an existing "how we rotate this" playbook to exist and should treat this as a first-time, carefully-verified procedure.
- `secrets-inventory.md`'s absence from this checkout is expected (git-ignored per README) and is not itself a landscape gap — but it does mean step 08's later update ("record rotation date") will most likely be creating a first entry for this secret rather than editing an existing dated entry, unless an operator's local, uncommitted copy already has one. This landscape-reader step made no attempt to read, infer, or reference any value from any local `secrets-inventory.md`, consistent with its read-only-inventory-reference rule — its non-existence in the repo tree is asserted from `landscape/README.md`'s documentation of the file, not from any workaround inspection of a local filesystem copy.

## Open questions (optional)

None — the landscape read itself is not blocked; the gaps identified above are exactly the kind of "must be discovered live" facts step 02's own role definition expects to surface for step 03/04, not blockers to finishing this step.
