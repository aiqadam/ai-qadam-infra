---
run_id: 2026-07-17-cicd-workflow-aiqadam-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-07-17T07:22:00Z
task_id: T-0113-github-actions-cicd-workflow-aiqadam-platform
inputs_read:
  - runs/2026-07-17-cicd-workflow-aiqadam-001/step-01-task-reader.md
  - workflows/cicd.md
  - tasks/T-0113-github-actions-cicd-workflow-aiqadam-platform.md
  - landscape/README.md
  - landscape/hosts/pro-data-tech-qa.md
  - landscape/hosts/pro-data-tech-prod.md
  - landscape/services.md
  - landscape/domains.md
  - landscape/secrets-inventory.md
  - shared/app-registry.md
  - shared/deploy-protocol.md
  - c:\Users\tvolo\dev\ai-dala\aiqadam (live repo probe: git remotes, branches, HEAD, package.json, .github/workflows/*)
artifacts_changed: []
next_step_hint: task-validator (step 03) must treat the discovery below as a hard input — the aiqadam repo already has .github/workflows/deploy.yml (Coolify-based CD) and ci.yml (advisory-only CI), neither of which references T-0112's four secrets. The task as written assumes greenfield ci-cd.yml authoring; step 03/04 must decide whether to add a NEW workflow file alongside the existing ones (recommended, lowest blast radius) or reconcile/replace. Also confirm with the user whether the pre-existing Coolify deploy.yml should be disabled, left alone, or left running in parallel — this is a scope question the task file does not address and was not visible to task-reader (step 01) since it required a live repo probe.
---

## Summary
Both target hosts are populated and current (`last_verified: 2026-07-17`) with T-0112's CI/CD deploy-user provisioning fully live: `pro-data-tech-qa` (95.46.211.230) and `pro-data-tech-prod` (95.46.211.224) each have a system user `deploy` (uid 999, `/bin/bash`, `deploybots` group in sshd `AllowGroups`) whose SSH key is locked to a single forced command — `/opt/apps/aiqadam-qa/deploy/deploy.sh` on QA, `/opt/apps/aiqadam-prod/deploy/deploy.sh` on prod — meaning the GitHub Actions workflow **cannot run arbitrary SSH commands**, only invoke that one placeholder script (currently just `docker compose -p <project> -f <compose-file> ps`), which T-0113 is expected to replace with real deploy logic. Compose layouts, domains, and secret names are all well-documented and current. However, a live probe of the actual app repo (`c:\Users\tvolo\dev\ai-dala\aiqadam`, confirmed `origin` = `https://github.com/aiqadam/ai-qadam-platform.git`, trunk branch = `main`) surfaced a landscape gap: the repo already ships `.github/workflows/deploy.yml` (a Coolify-based CD pipeline, unrelated to these two hosts) and `ci.yml` (explicitly documented as advisory-only, `continue-on-error: true`, per a 2026-06-29/07-03 override) plus 5 other workflow files — none of this is recorded anywhere in the landscape, and none of T-0112's four secret names appear in any existing workflow. This is a material fact step 03/04 needs and could not have gotten from the landscape alone.

## Details
### Relevant facts (sourced from landscape)

**Deploy user / forced command (identical pattern on both hosts):**
- User `deploy`, uid 999, gid 981 (primary group `deploy`), home `/home/deploy`, shell `/bin/bash`, system account, password-locked, no sudo grant — _source: `landscape/hosts/pro-data-tech-qa.md`, `landscape/hosts/pro-data-tech-prod.md`_
- Groups: `deploy`, `docker` (gid 986), `deploybots` (gid 982), plus `aiqadam-qa-secrets` (gid 980, QA) / `aiqadam-prod-secrets` (gid 980, prod) — _source: same_
- SSH admitted via `deploybots` added to sshd `AllowGroups` (now `sshusers deploybots` on both hosts) — _source: same_
- `authorized_keys` forced command, QA: `command="/opt/apps/aiqadam-qa/deploy/deploy.sh",no-port-forwarding,no-X11-forwarding,no-agent-forwarding,no-pty` — _source: `landscape/hosts/pro-data-tech-qa.md`_
- `authorized_keys` forced command, prod: `command="/opt/apps/aiqadam-prod/deploy/deploy.sh",no-port-forwarding,no-X11-forwarding,no-agent-forwarding,no-pty` — _source: `landscape/hosts/pro-data-tech-prod.md`_
- **Implication for the workflow file:** the SSH deploy step cannot run `git pull && docker compose up -d --build` (or anything else) directly over SSH — whatever command the GitHub Action sends, sshd will execute the forced command instead and ignore it. The workflow's job is therefore to (a) SSH in (any command string works, it's discarded) to trigger `deploy.sh`, and/or (b) SCP a new value (e.g. a git ref) somehow accessible to `deploy.sh` if the script needs to know what to deploy. **This is a design question for step 04, flagged as a gap below** — the placeholder script takes no arguments today.
- **Deploy script placeholder (both hosts, same content pattern):** `/opt/apps/aiqadam-qa/deploy/deploy.sh` and `/opt/apps/aiqadam-prod/deploy/deploy.sh`, mode 750, owner `deploy:deploy` — currently just a marker line + `docker compose -p <project> -f <compose-file> ps`. Explicitly documented as "to be replaced by T-0113's real CI/CD deploy logic" — _source: both host files_

**Compose / app directory layout:**
- QA: app checkout `/opt/apps/aiqadam-qa/` (git HEAD `dfd2a7c`, from `https://github.com/aiqadam/ai-qadam-platform.git`); compose project `aiqadam-qa`; compose file `/opt/apps/aiqadam-qa/deploy/docker-compose.qa.yml` (2 services: `oidc-stub`, `api`; `network_mode: host`); env file `/opt/apps/aiqadam-qa/deploy/.env` (mode 640, owner `tvolodi:aiqadam-qa-secrets` as of T-0112 — `deploy` has group-read) — _source: `landscape/hosts/pro-data-tech-qa.md`, `shared/app-registry.md`_
- Prod: app checkout `/opt/apps/aiqadam-prod/` (git HEAD `dfd2a7c`, pinned/detached, same commit as QA); compose project `aiqadam-prod`; compose file `/opt/apps/aiqadam-prod/deploy/docker-compose.prod.yml` (3 services: `postgres`, `oidc-stub`, `api`; `network_mode: host`); env file `/opt/apps/aiqadam-prod/deploy/.env` (mode 640, owner `tvolodi:aiqadam-prod-secrets` as of T-0112) — _source: `landscape/hosts/pro-data-tech-prod.md`, `shared/app-registry.md`_
- Both hosts' current deploys are on **detached-HEAD pinned commit `dfd2a7c`**, not tracking a moving branch — the executor for T-0114/T-0115 (out of scope here, but relevant context for how `deploy.sh` should work) will need to move this to a real `git pull`/checkout flow.
- Neither compose file builds/pulls images from a registry — the existing convention (T-0110/T-0111) is **local build on host** from the git checkout (`apps/api/Dockerfile`), matching the task's own recommended default. No GHCR or other registry is referenced anywhere in the landscape for this app.

**Domains:**
- QA: `qa-uz.aiqadam.org` → `95.46.211.230`, proxies to `127.0.0.1:3113`, Let's Encrypt TLS live, health endpoint `GET https://qa-uz.aiqadam.org/health` → 200 — _source: `landscape/domains.md`, `landscape/hosts/pro-data-tech-qa.md`_
- Prod: `aiqadam.org` (bare apex, no `www`) → `95.46.211.224`, proxies to `127.0.0.1:3115`, Let's Encrypt TLS live, health endpoint `GET https://aiqadam.org/health` → 200 — _source: `landscape/domains.md`, `landscape/hosts/pro-data-tech-prod.md`_
- Both domains are in the `aiqadam.org` Cloudflare zone (`bec8854d698d56ff17cf917367634100`), both DNS records unproxied (`proxied: false`) — not relevant to the workflow file itself but confirms no Cloudflare-side change is needed for T-0113.

**Secrets (names only, confirmed present per T-0112, not values):**
- `QA_SSH_DEPLOY_KEY`, `QA_SSH_HOST_KEY` — GitHub Actions repo secrets in `aiqadam/ai-qadam-platform`, corresponding to the QA `deploy` user's ed25519 CI key (`aiqadam-qa-deploy-ci`, fingerprint `SHA256:SLM2PY1Enq+oZ4nepJ5l499sPC9ulG1wc7Wi0ibUkZg`) — _source: `landscape/secrets-inventory.md`, `landscape/hosts/pro-data-tech-qa.md`_
- `PROD_SSH_DEPLOY_KEY`, `PROD_SSH_HOST_KEY` — same pattern for prod (`aiqadam-prod-deploy-ci`, fingerprint `SHA256:KLpw03147K4mknHrkZvoBv5PqDxWDAAugcc65IEGyUo`) — _source: `landscape/secrets-inventory.md`, `landscape/hosts/pro-data-tech-prod.md`_
- **Caveat:** T-0112 remains `status: in-progress` — the on-host provisioning is done and live-verified, but the task file itself notes the GitHub Actions secrets paste is a manual user action not yet confirmed. Step 01's handoff already flagged this ("T-0112 is recorded as done... task remains in-progress"). Both host landscape files independently repeat the same caveat. **Task-validator (step 03) should treat this as needing explicit confirmation** — if the secrets are not actually present in the GitHub repo, the workflow file can be authored but `deploy-qa`/`deploy-prod` will fail at runtime until the user pastes them in.

**deploy-protocol.md signal-file convention:**
- The protocol defines `tasks/deploy-request.md` in the *project* repo as a signal a project agent writes to request a deploy, which infra agents read to determine what ref to deploy for the `deploy-app` workflow (T-0114/T-0115-style tasks) — _source: `shared/deploy-protocol.md`_
- This convention is designed for the **existing manual/infra-orchestrator-driven `deploy-app` workflow** (human says "deploy X to test", orchestrator reads the signal file, runs 8 steps). It is not obviously designed for **CI-triggered automatic deploys** (push to `main` → GitHub Actions deploys immediately, no human-initiated infra run). T-0113's own task file explicitly leaves this open: wire the signal file in (CI writes it after a successful QA deploy) **or** explicitly declare it unused, per user preference. Live probe confirms **no `tasks/deploy-request.md` currently exists** in the aiqadam repo (`ls tasks` returned nothing, `cat tasks/deploy-request.md` returned nothing) — so today it's unused; there is no existing convention in-repo to preserve or break.
- **Recommendation surfaced for step 04, not decided here:** since deploy-qa in T-0113 is CI-triggered (not human-orchestrator-triggered), writing `tasks/deploy-request.md` post-deploy would function purely as a *record*, not a *request* (the deploy already happened) — a different semantic than the protocol currently documents (signal-file-as-request-before-action). This is worth flagging to the user rather than silently either wiring it in or skipping it.

**shared/app-registry.md:**
- Already documents both QA and prod AiQadam environments in full (compose files, containers, secrets by name, health endpoints, "Next milestone: T-0112 (deploy keys) and T-0113 (CI/CD workflow), then T-0114/T-0115 (first CI-driven deploy/promote)"). Confirms T-0113's own "What done looks like" item — "app-registry.md updated with a CI/CD section" — is additive: a **new** section, not a rewrite of the existing app entries — _source: `shared/app-registry.md`_

### Stale or stub files encountered
- `landscape/README.md` — no `last_verified` frontmatter field at all (file predates that convention or was never updated to add it). Content is stale: it lists only `hosts/ubuntu-16gb-nbg1-1.md` and `hosts/pro-data-tech-qa.md` in the file table (missing `hosts/pro-data-tech-prod.md` entirely), and describes `cloudflare.md`/`domains.md` as "stub — no domains/zones currently managed by this repo," which is now false (`domains.md` is `status: active`, `last_verified: 2026-07-13`, with three live subdomains). Not a blocker for this task — none of the specific facts needed came from README — but flagged as drift for step 08 or a future housekeeping task.
- All other landscape files read (`hosts/pro-data-tech-qa.md`, `hosts/pro-data-tech-prod.md`, `services.md`, `domains.md`) have `last_verified: 2026-07-17` or `2026-07-13`, well within 30 days — none stale.
- `secrets-inventory.md` has no frontmatter/`last_verified` field (by design — it's a flat gitignored list, not a dated landscape file per `landscape/README.md`'s own description). Content is internally consistent with the two host files' T-0112 notes.

### Gaps requiring live discovery
- **Confirmed via live probe (already resolved, reported here for step 03/04's benefit):** `origin` remote of `c:\Users\tvolo\dev\ai-dala\aiqadam` is confirmed `https://github.com/aiqadam/ai-qadam-platform.git` (there is also a stale `oldorigin` remote pointing to `https://github.com/tvolodi/aiqadam.git` — not relevant, superseded). Trunk branch is confirmed `main` (current checked-out branch, and the branch referenced by `origin/HEAD -> origin/main`). Both of these resolve open questions flagged by task-reader (step 01) and the task file itself.
- **New gap surfaced by live probe, NOT answerable from landscape:** the aiqadam repo already contains `.github/workflows/deploy.yml` — a Coolify-based continuous-deployment pipeline (triggers on push to `main`, calls `https://coolify.aiqadam.org/api/v1/deploy` for `web` and `api` apps using a `COOLIFY_TOKEN` secret) — and `.github/workflows/ci.yml`, whose header comment states CI is "ADVISORY ONLY... both jobs carry `continue-on-error: true`... does NOT block PR merge and does NOT fail the workflow run" per an explicit user override recorded 2026-06-29 and extended 2026-07-03. Five more workflow files exist: `content-quality.yml`, `deploy-web-next.yml`, `parity-check.yml`, `restic-drill-lint.yml`, `smoke.yml`, `supply-chain.yml`. None of these reference `QA_SSH_DEPLOY_KEY`, `PROD_SSH_DEPLOY_KEY`, `QA_SSH_HOST_KEY`, or `PROD_SSH_HOST_KEY` (confirmed via grep across all workflow files). This directly conflicts with two of T-0113's stated requirements: (1) the task assumes `ci-cd.yml` is a new/greenfield file, but there's already an active Coolify-based CD path deploying `web`/`api` on every push to `main` — is that a *different* app/environment than pro-data-tech-qa/prod (plausible — Coolify != pro-data.tech hosts), or a conflicting/legacy pipeline that should be disabled? Not knowable from the landscape or the task file; needs a user decision. (2) the task requires `build` to "fail the workflow on any failure (no deploy proceeds)," but the existing `ci.yml` was deliberately made advisory-only by prior explicit user direction — T-0113's new workflow should presumably NOT inherit that advisory pattern, but this is worth flagging so step 04 doesn't silently contradict a recent, deliberate prior user decision without calling it out.
- **Not answerable from landscape, needs step 03/04 or executor to check:** `package.json` confirms `pnpm test` (delegates to `turbo run test`), `pnpm typecheck`, `pnpm lint` (biome) all exist as scripts — so a real build/lint/test toolchain does exist (turbo + biome + pnpm 9.15.0, Node >=22). Whether `turbo run test` actually has meaningful test suites wired per-package (vs. no-op stubs) was not verified — this requires either running it or reading individual package configs, which is beyond a landscape read. Flagging per task's own "Open questions" item on test suite maturity.
- **Not in landscape:** whether a GitHub Environment named `production` (or any name) with required reviewers already exists in the `aiqadam/ai-qadam-platform` repo settings. This needs a `gh api repos/aiqadam/ai-qadam-platform/environments` check (or `gh` CLI equivalent) at execution time — outside landscape-reader's read-only file scope, and outside what the local git clone can reveal.
- **Not in landscape:** whether the four GitHub Actions secrets are actually present as repo secrets today (T-0112's own status is `in-progress` pending user's manual paste). `gh secret list` would confirm but wasn't run here (deliberately out of scope for a read-only landscape pass, and secret listing doesn't reveal values so would be safe, but confirming *presence* is arguably a step 03/04 concern, not step 02's).

## Issues / risks
- **Highest-priority finding for step 03:** the existence of `deploy.yml` (Coolify) and the advisory-only `ci.yml` in the target repo was invisible to task-reader (step 01) and to the landscape (no file in this repo tracks the app repo's own CI/CD state) — it only surfaced because this step probed the live clone. Step 03 (task-validator) should explicitly re-verify this against the task's assumptions before step 04 designs a solution, since "author `.github/workflows/ci-cd.yml`" reads very differently if it's the fourth deploy-relevant workflow file in that directory versus the first.
- The forced-command restriction on `deploy` (both hosts) means the workflow's SSH step is far more constrained than a generic "SSH and run a deploy command" — the actual git-ref-selection / build-trigger logic must live inside `deploy.sh` on each host (which the workflow will presumably need to update via `scp`/`rsync` before invoking it, or pass state some other way, since the forced command accepts no argument passthrough by default with plain OpenSSH `command=` restriction unless `SSH_ORIGINAL_COMMAND` is read inside the script). This is a solution-design concern for step 04, not resolved here, but the landscape fact (forced command, no args today) is the load-bearing constraint.
- T-0112's `in-progress` status (pending secrets paste) means step 04/06 may need to pause or explicitly confirm secret presence before the workflow can be validated end-to-end; this is a soft dependency, not a hard blocker for *authoring* the file.
- `landscape/README.md` drift (see Stale files above) is cosmetic to this task but should be corrected at some point — not urgent enough to block this run.

## Open questions (optional)
none — landscape is sufficiently populated and current to proceed to step 03. The Coolify/advisory-CI discovery is a **gap for step 03/04 to resolve with the user**, not a landscape blocker; verdict remains PASS with the gap flagged prominently above.
