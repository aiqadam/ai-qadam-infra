---
run_id: 2026-06-27-audit-hetzner-firewall-001
step: 03
agent: task-validator
verdict: PASS
created: 2026-06-27T00:00:00Z
task_id: T-0085-audit-hetzner-cloud-firewall-for-ubuntu-16gb-nbg1-1
inputs_read:
  - runs/2026-06-27-audit-hetzner-firewall-001/step-01-task-reader.md
  - runs/2026-06-27-audit-hetzner-firewall-001/step-02-landscape-reader.md
  - landscape/hosts/ubuntu-16gb-nbg1-1.md
  - landscape/secrets-inventory.md
  - workflows/discovery-host.md
  - workflows/discovery-cloudflare.md
  - shared/handoff-format.md
  - shared/verdicts.md
  - runs/2026-06-27-discovery-host-001/step-03-task-validator.md
  - runs/2026-05-12-discovery-cloudflare-001/step-03-task-validator.md
artifacts_changed: []
next_step_hint: Pass to executor-discovery (step 06). Steps 04 and 05 are skipped per discovery-host workflow frontmatter (`state_changing: false`, `skip_design_step: true`). The four probes (A token-verify, B firewalls enumerated, C server confirmation, D findings synthesis) from step-01's probe checklist are the working set. The structural template is `workflows/discovery-cloudflare.md`; the orchestrator must brief the executor that the equivalent of "only HTTP GET" applies to Hetzner (i.e., the executor's hard rule #1 — no state-changing commands — covers POST/PUT/PATCH/DELETE on `/firewalls`, `/servers`, and adjacent Hetzner endpoints, not just SSH).
---

## Summary

Validated: T-0085 (Hetzner Cloud Firewall audit for `ubuntu-16gb-nbg1-1`) is a well-formed, in-scope, read-only API discovery that resolves an explicit "status unknown — requires Hetzner API call against project 15130993" placeholder in the host's landscape file. All six checks pass; the token file is verified live (exists, 64 bytes, SHA-256 fingerprint `FBF81B3A1AB2F3A9BE3D3F30C47F32668EA25AE4FCD7363002A54C013CF03153`); the workflow may proceed directly to step 06 (executor-discovery), with steps 04 and 05 skipped per `discovery-host` frontmatter.

## Details

### Validation results

1. **Well-formed: PASS** — Six concrete, verifiable acceptance criteria defined in step-01, each tied to a named handoff file and a discoverable Hetzner API response: (1) token verify via `GET /v1/projects` returns project `ai-qadam`; (2) `GET /v1/firewalls` enumerates project-15130993 firewalls with full rule and `applied_to` detail; (3) `GET /v1/servers/145542849` confirms the server and reports Hetzner-side network config; (4) findings section explicitly answers "applied or not applied"; (5) `landscape/hosts/ubuntu-16gb-nbg1-1.md` receives a new Hetzner Cloud Firewall section (resolves Open Questions item #1 from the host file); (6) `landscape/secrets-inventory.md` gets the SHA-256 fingerprint housekeeping for the ai-qadam token if missing. Each criterion has a clear pass/fail signal; no vague-intent phrasing. Step 07 (execution-validator) has explicit, per-criterion checks.

2. **In-scope: PASS** — `discovery-host` is the right workflow shape for read-only enumeration even when the probe target is the Hetzner Cloud API rather than SSH-to-host. The workflow frontmatter (`state_changing: false`, `skip_design_step: true`) matches the operation exactly: no design step needed because the probe list is small and well-defined inline; no approval gate because nothing changes. The structural analog `workflows/discovery-cloudflare.md` (a read-only API enumeration against a different vendor) confirms the same `state_changing: false` shape is used for non-SSH discovery — T-0085 follows the same precedent, substituting Hetzner endpoints and the ai-qadam token.

3. **Not already done: PASS** — `landscape/hosts/ubuntu-16gb-nbg1-1.md` (populated today, 2026-06-27) carries "**Hetzner Cloud Firewall:** status unknown — out of scope for the discovery workflow (requires Hetzner API call against project 15130993)" in the Hardware & OS section, AND a `last_verified`-anchored entry in "Open questions" item #1: "**Hetzner Cloud Firewall ID:** unknown — requires Hetzner API call against project 15130993 …". The "What needs to happen" checklist item #2 explicitly defers this audit to a follow-on Hetzner-API workflow run after token-scope for project 15130993 is verified. The token was provisioned today; no prior run has used it. The status quo — placeholder "status unknown" — is the entire gap this run fills. The only Hetzner Cloud Firewall documented in the landscape is on `hetzner-prod` (project 12287574, firewall id 10145783), which is a different project and provides no protection to `ubuntu-16gb-nbg1-1`.

4. **No conflict with current state: PASS** — Pure read-only HTTP GET operations. No landscape fact forbids probing project 15130993. No landscape fact claims the firewall status is anything other than "unknown". The Hetzner Cloud Firewall id placeholder is the only firewall-shaped artifact in the landscape for this host, and it explicitly says "unknown — requires Hetzner API call" — i.e., the landscape author anticipated this exact run. No `discovery-hetzner.md` workflow file exists yet (and the task notes say "if the workflow proves useful, a dedicated file should be added"), but using `discovery-host`'s structural template with `discovery-cloudflare.md`'s inline probe pattern is a documented and valid fallback (step-01 explicitly cited this).

5. **Discoverable scope: PASS** — All required facts for the executor to act are present:
   - **Token file:** `C:\Users\tvolo\.config\ai-dala-infra\hetzner.ai-qadam.token` — verified live during this validation: `Test-Path` returns `True`, `Length` returns 64 bytes (consistent with a Hetzner Cloud API token: 64-character printable ASCII, no trailing newline), `Get-FileHash -Algorithm SHA256` returns fingerprint `FBF81B3A1AB2F3A9BE3D3F30C47F32668EA25AE4FCD7363002A54C013CF03153` (this is the value step 08 should record in `landscape/secrets-inventory.md`'s fingerprint slot for `hetzner-api-token:ai-dala-infra:ai-qadam-read-write`).
   - **Catalogued in secrets inventory:** the row exists with purpose, storage location (`C:\Users\tvolo\.config\ai-dala-infra\hetzner.ai-qadam.token`), ACL, used-by, rotation cadence, and provisioning date (2026-06-27). Only the SHA-256 fingerprint is a placeholder awaiting this run.
   - **Hetzner API endpoints:** documented at <https://docs.hetzner.cloud/#firewalls> — `GET /v1/firewalls`, `GET /v1/firewalls/{id}`, `GET /v1/servers/{id}`, `GET /v1/projects`. All GET. The endpoint list is stable and public.
   - **Per-server cross-reference:** the `applied_to` field on each firewall resource (or `GET /v1/firewalls/{id}/actions/get_applied_to`) is the authoritative answer to "is `ubuntu-16gb-nbg1-1` (server_id 145542849) protected by this firewall". The probe list in step-01 explicitly checks `applied_to` for server_id 145542849.
   - **The 64-byte file size is a useful live signal:** matches the documented Hetzner token format (a 64-char string of base62 chars). No further pre-flight API call is required from this step; the executor's probe A (`GET /v1/projects`) is the actual token-verify gate, and step 06 may emit `BLOCKED` if that probe fails.

6. **Workflow-specific rules respected: PASS** — `workflows/discovery-host.md` declares `state_changing: false` (no step-05 approval gate, per `workflows/_common-operations.md` cascade table) and `skip_design_step: true` (step 04 skipped; probe list lives in the workflow file or, in this run's case, is supplied inline by step-01 because the workflow file's probe checklist is SSH-targeted and does not match the Hetzner-API probe target). Step-01 derived a probe checklist (A token-verify, B firewalls enumerated, C server confirmation, D findings synthesis) that mirrors `workflows/discovery-cloudflare.md` probes A–G in shape; the orchestrator should brief the executor that this checklist replaces `workflows/discovery-host.md`'s SSH probes A–N for this run. The token is referenced by NAME (`hetzner-api-token:ai-dala-infra:ai-qadam-read-write`) only; the value is read at runtime via `Get-Content` (or `cat`/`curl -H "@file"`), never echoed, never pasted into a handoff. No secret value lands in `runs/2026-06-27-audit-hetzner-firewall-001/`.

### Specific concerns raised in the prompt

- **Is the executor-discovery agent's allowlist sufficient for Hetzner API probes?** PASS — the agent's tools include `bash`/`terminal` (sufficient for `curl` against `https://api.hetzner.cloud/v1/...`); `curl` is universally available on the management workstation (Windows OpenSSH build ships it; PowerShell `curl` is an alias for `Invoke-WebRequest` but the executor should call `curl.exe` explicitly to avoid that ambiguity). No new tools needed; the existing allowlist covers HTTP GET probes.
- **Does `state_changing: false` correctly skip steps 04 and 05?** PASS — confirmed by `workflows/discovery-host.md` frontmatter (`state_changing: false`, `skip_design_step: true`) and by the step bindings table ("Step 04 solution-designer: skipped", "Step 05 user-approval: skipped (state_changing: false)"). Per `workflows/_common-operations.md` cascade table, read-only workflows run steps 01–03 and 06–08.
- **Risk of accidental write endpoint invocation?** PASS — the probe list from step-01 is fully scoped to GET (`GET /v1/projects`, `GET /v1/firewalls`, `GET /v1/firewalls/{id}`, `GET /v1/servers/{id}`). The token is read+write-scoped at the Hetzner project level, but the executor-discovery subagent's hard rule #1 forbids state-changing commands; the equivalent rule for HTTP is "no POST/PUT/PATCH/DELETE on `/firewalls` or `/servers`". Step-01 explicitly stated this constraint; step-06 should refuse and emit `BLOCKED` if any non-GET verb appears in the plan. No risk of accidental mutation if the orchestrator briefs the executor clearly.

## Issues / risks

- **`landscape/secrets-inventory.md` file-level `last_verified` is stale (2026-05-26)** — the row for the ai-qadam token was added today but the file-level timestamp predates it. Not a blocker for this run; step 08 (landscape-updater) should bump the file-level `last_verified:` to 2026-06-27 when it lands the SHA-256 fingerprint housekeeping. Pre-existing drift, not introduced by this run.
- **`landscape/secrets-inventory.md` contains a literal password value** for `gitea:admin-password` (`eT96ulleryIpd38VJeQNRGm3lQ3qcUO3`), violating the file's own "Never put secret values in this file" rule. Pre-existing drift, out of scope; surface as a low-priority cleanup task (rotate the password, then remove from the file).
- **First Hetzner-API run in the project.** No prior run has probed a Hetzner Cloud API endpoint. The executor-discovery subagent's hard rule #4 (Cloudflare API: only HTTP GET requests) is the structural model but worded for Cloudflare; the orchestrator must brief the subagent that the equivalent rule for Hetzner applies. The four probes A–D from step-01 are the working set; the executor should not invent additional probes that require non-GET verbs.
- **No dedicated `workflows/discovery-hetzner.md` exists yet.** T-0085's Notes say: "if the workflow proves useful, a dedicated `discovery-hetzner.md` workflow file should be added." This run will use `workflows/discovery-host.md` as the workflow binding and `workflows/discovery-cloudflare.md` as the inline structural template. If subsequent Hetzner-API runs are needed (Hetzner snapshot backups audit per `landscape/hosts/ubuntu-16gb-nbg1-1.md` Open Questions item #2, or Hetzner Cloud Firewall apply as a follow-on state-changing task), a dedicated workflow file should be created and this run's handoffs cited as the precedent.
- **Token file existence verified, but token revocation not verified.** A defensive HTTP GET against `https://api.hetzner.cloud/v1/projects` would confirm the token is still active (not revoked at the Hetzner side). Optional — the executor's probe A will perform this check; if the token has been revoked, probe A fails and step 06 emits `BLOCKED`. Cheap insurance exists (one curl call), but step 03 does not require it. Not a blocker.
- **`applied_to` verification is mandatory for finding accuracy.** Hetzner Cloud Firewalls are project-scoped resources; the `applied_to` field of a firewall resource is the authoritative list of servers the firewall protects. The executor must explicitly look up server_id `145542849` in each firewall's `applied_to` list and report per-firewall whether `ubuntu-16gb-nbg1-1` is covered. Reporting "firewalls exist in project ai-qadam" without resolving whether THIS server is covered is incomplete and would fail step-07.
- **Default-exposure language is mandatory if no firewall is applied.** If the audit finds NO Hetzner Cloud Firewall applied to `ubuntu-16gb-nbg1-1`, step 08 must use precise wording: "Hetzner Cloud servers with no Cloud Firewall applied are reachable on all ports from the public internet, blocked only by the host firewall (UFW, currently allow 22/80/443 v4+v6) plus fail2ban (maxretry=3, bantime=600s)." The landscape must NOT characterize this as "low risk"; this is a finding to surface to the user and likely triggers a follow-on state-changing task to apply a Cloud Firewall.
- **SSH password auth still enabled on the host** (per `landscape/hosts/ubuntu-16gb-nbg1-1.md` Access section: `PasswordAuthentication yes`, `PermitRootLogin yes`). This is adjacent context — UFW allows 22 globally, fail2ban is the only current mitigation. The audit should surface this as compounding the case for an outer-layer Cloud Firewall restricting source IPs, but should not attempt to fix it (out of scope; that is T-0086 territory or a follow-on sshd hardening workflow).

## Open questions (optional)

- (For orchestrator before step 06) Confirm the executor-discovery subagent is briefed that the equivalent of hard rule #4 ("only HTTP GET requests") applies to Hetzner endpoints (`/v1/firewalls`, `/v1/servers`, `/v1/projects`). The existing rule is Cloudflare-specific; the equivalent for Hetzner should be stated explicitly in the step-06 invocation.
- (For orchestrator before step 06) Confirm probe A's "token verify" surface: `GET /v1/projects` (returns a list of projects visible to the token; project `ai-qadam` with id 15130993 should appear) versus `GET /v1/projects/15130993` (direct lookup). Both work; either is acceptable as the token-verify gate. Step-01 suggested `GET /v1/projects` for the list (analogous to Cloudflare's `/user/tokens/verify`); step-06 may refine to `GET /v1/projects/15130993` if more direct.
- (For step 06) If `applied_to` is not present on a firewall resource for some reason (e.g., API pagination edge case, or the field is named differently in Hetzner's current API version), the fallback is `GET /v1/firewalls/{id}/actions/get_applied_to`. Document the path taken so step 07 can verify.
- (For step 08 landscape-updater) Where to place the new "Hetzner Cloud Firewall" section in `landscape/hosts/ubuntu-16gb-nbg1-1.md` — inline replacement of the "Hetzner Cloud Firewall: status unknown" line in "Hardware & OS", or a new top-level section between "Hardware & OS" and "Access"? Step-01 recommends inline replacement unless a dedicated section adds clarity; either is acceptable. Mirror the wording style used for `hetzner-prod`'s Network section (`Hetzner Cloud Firewall: firewall-1 (id=10145783) applied to this server.`).
- (For orchestrator after step 08) T-0085 has `kind: task` and `status: in-progress`. After a successful audit, the orchestrator decides whether to transition T-0085 to `done` (with `outcome: succeeded`) if firewalls are found and documented, or to `pending` if a follow-on state-changing task must be created to apply a firewall. Step 08 only writes the landscape.
- (For step 08 landscape-updater) Also update the host file's Change log with a 2026-06-27 row referencing `2026-06-27-audit-hetzner-firewall-001`. The T-0082 History section in the same file is also a reasonable place to record that the Hetzner Cloud Firewall audit has completed.
