---
run_id: 2026-07-24-fix-deploy-qa-permission-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-07-24T00:00:00Z
task_id: T-0124-fix-deploy-qa-permission-denied
inputs_read:
  - runs/2026-07-24-fix-deploy-qa-permission-001/step-01-task-reader.md
  - landscape/README.md
  - workflows/cicd.md
  - landscape/hosts/pro-data-tech-qa.md
  - landscape/services.md
artifacts_changed: []
next_step_hint: proceed to step 03 task-validator; live SSH discovery (id deploy, ls -la on the checkout, git config, .last-deployed-commit) is required before step 04 can design a fix — landscape only documents state as of T-0113 (2026-07-17), which is the leading hypothesis, not confirmed current state.
---

## Summary
Both in-scope landscape files are current (`pro-data-tech-qa.md` last_verified 2026-07-17, `services.md` last_verified 2026-07-23 — neither stale) and both are `status: populated`, not stubs. The landscape confirms the task's hypothesis is plausible but explicitly documents it as an "unplanned" fix bolted on during T-0113: the `deploy` user (uid 999) was added as a secondary member of the `tvolodi` group to gain write access to the `tvolodi:tvolodi`-owned checkout at `/opt/apps/aiqadam-qa/`, relying entirely on directory/file group-write bits (`775`/`755`) rather than a dedicated ownership scheme — precisely the kind of grant that is fragile against any subsequent file recreated with different ownership/mode. The landscape has no record of anything touching this host between T-0113 (2026-07-17) and today (2026-07-24), so a week of undocumented drift is the most likely explanation, but this cannot be confirmed from files alone.

## Details
### Relevant facts (sourced from landscape)
- App checkout is at `/opt/apps/aiqadam-qa/`, owned `tvolodi:tvolodi`, directory modes `775`/`755` — _source: `landscape/hosts/pro-data-tech-qa.md`_ (line ~85, "CI/CD deploy user" subsection)
- `deploy` user (uid 999, gid 981, shell `/bin/bash`, `useradd --system`) has groups: `deploy` (primary, gid 981), `docker` (gid 986), `deploybots` (gid 982), `aiqadam-qa-secrets` (gid 980) — _source: `landscape/hosts/pro-data-tech-qa.md`_ ("CI/CD deploy user" subsection, bullet "Groups")
- **Unplanned T-0113 fix, quoted verbatim:** "the app checkout (`/opt/apps/aiqadam-qa/`, including `.git/` and `deploy/`) is owned `tvolodi:tvolodi` mode `775`/`755` — `deploy` had no write access to run `git fetch`/`checkout` until added as a secondary member of the `tvolodi` group (`sudo usermod -aG tvolodi deploy`), which grants write via the existing group-write bits (no file ownership or mode changed)." — _source: `landscape/hosts/pro-data-tech-qa.md`_
- A matching `git config --global --add safe.directory /opt/apps/aiqadam-qa` was added to `deploy`'s `~/.gitconfig` to satisfy git's ownership-mismatch check — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Both the group-add and the safe.directory config are noted as "additive and reversible" (`sudo gpasswd -d deploy tvolodi` to undo) — this is the only rollback path the landscape records; it is not directly relevant to fixing the current permission-denied error but matters if step 04 needs a way to cleanly back out the current (evidently insufficient) grant before applying a different one.
- Deploy script: `/opt/apps/aiqadam-qa/deploy/deploy.sh` (mode 750, owner `deploy:deploy`) — does `git checkout --detach <ref>` after validating the ref, **never runs `git clean`** (hard rule in script's own header comment, not a technical control) — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Deploy script logic: records pre-deploy HEAD to `.last-deployed-commit.previous`, checks out, writes new HEAD to `.last-deployed-commit`, then `docker compose -p aiqadam-qa -f docker-compose.qa.yml up -d --build` — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Last confirmed-working deploy history: T-0113's own live rehearsal (2026-07-17) was a **self-deploy of the already-running pinned commit** `dfd2a7c479c18e9acea5b3e0f53e19aca3f777bb` — health check 200, marker files correct, containers `Up (healthy)`. This was a no-op checkout (same ref in and out), so it does **not** prove the group-write grant survives a checkout that actually overwrites tracked files with different content — a materially different code path from what `deploy-qa` needs to do now. — _source: `landscape/hosts/pro-data-tech-qa.md`_
- `deploy/.env` ownership is `tvolodi:aiqadam-qa-secrets` mode 640 (changed from `tvolodi:tvolodi` 600 by T-0112 to grant `deploy` group-read) — this file is untracked/gitignored, not part of the `git checkout --detach` overwrite path, so it is not implicated in the `package.json`/`pnpm-lock.yaml` unlink error, but confirms the pattern of "grant via group membership on top of tvolodi-owned files" used throughout this host's CI/CD setup — _source: `landscape/hosts/pro-data-tech-qa.md`_
- T-0113 status at hand-off was **"in-progress (PR #15 open, not yet merged)"** — the landscape's own `last_verified_note` frontmatter still shows this as unresolved-in-repo state as of 2026-07-17; no landscape entry confirms PR #15 (or #44/#51 mentioned in the task) closed cleanly against this specific deploy-user permission setup after that date. — _source: `landscape/hosts/pro-data-tech-qa.md` frontmatter_
- Public endpoint for the AiQadam QA stack is `https://qa-uz.aiqadam.org` (renamed mid-run from `qa.aiqadam.org` during T-0110 "to route around an app-level tenant-parsing 400") — **this differs from the task's stated target `https://qa.aiqadam.org`** used throughout step 01's acceptance criteria (health check, register endpoint). — _source: `landscape/hosts/pro-data-tech-qa.md`_ and `landscape/services.md`_
- `services.md` confirms the same checkout path and Compose project (`aiqadam-qa`, compose file `/opt/apps/aiqadam-qa/deploy/docker-compose.qa.yml`, containers `aiqadam-qa-oidc-stub-1` + `aiqadam-qa-api-1`), last known deployed git HEAD `dfd2a7c` as of 2026-07-13/17 — no entry shows a git HEAD advance since then, consistent with the task's claim that QA is pinned since PR #44. — _source: `landscape/services.md`_
- No `sudo`/root grant exists for `deploy` (confirmed absent, no `/etc/sudoers.d/90-deploy` file) — any permission fix must work within `deploy`'s actual group memberships and file ACLs; it cannot rely on `deploy` self-elevating. — _source: `landscape/hosts/pro-data-tech-qa.md`_
- No mention anywhere in either landscape file of ACLs (`setfacl`) being used on this host for the `aiqadam-qa` checkout — the only access-control mechanism on record is Unix group-write bits plus the `usermod -aG` grant. — _source: `landscape/hosts/pro-data-tech-qa.md`_ (absence noted; not stated explicitly, inferred from the full "CI/CD deploy user" and "Security posture" sections containing no ACL reference)

### Stale or stub files encountered
- None. `landscape/hosts/pro-data-tech-qa.md` — last_verified 2026-07-17, status populated (7 days old, well under the 30-day staleness threshold). `landscape/services.md` — last_verified 2026-07-23, status populated (1 day old).

### Gaps requiring live discovery
- Exact current `ls -la` output for `/opt/apps/aiqadam-qa/package.json` and `/opt/apps/aiqadam-qa/pnpm-lock.yaml` — owner, group, and mode bits. The landscape only documents the *directory*-level ownership/mode (`tvolodi:tvolodi`, `775`/`755`) as of 2026-07-17; individual file-level state after any `pnpm install` or partial deploy since then is undocumented.
- Current `id deploy` output — which groups the `deploy` user actually holds today. The landscape states `deploy` was added to `tvolodi`'s group on 2026-07-17, but does not confirm this membership persisted (e.g., not reverted by a later unlogged `gpasswd -d`, not lost to a home-directory/user re-provisioning).
- Whether `deploy`'s `~/.gitconfig` still contains the `safe.directory /opt/apps/aiqadam-qa` entry — if lost, `git` operations as `deploy` would fail with a dubious-ownership error distinct from (but possibly compounding) a permission-denied unlink error.
- Whether any files in the checkout were touched/recreated by a process running as a *different* user (e.g., `tvolodi` running a manual `pnpm install` or partial/interrupted deploy) since T-0113's rehearsal — this would explain per-file ownership drift even with the directory-level grant intact, exactly the task's leading hypothesis.
- Current value of `/opt/apps/aiqadam-qa/deploy/.last-deployed-commit` — needed to confirm how stale QA actually is relative to `main` (task claims pinned at PR #44's `af30beb`; landscape's last recorded HEAD is `dfd2a7c`, an different/older commit — the two are not reconciled in the landscape and must be checked live).
- Confirmation of which real public hostname is authoritative today: landscape says `qa-uz.aiqadam.org`, task's acceptance criteria (per step 01) say `qa.aiqadam.org`. This must be resolved via live discovery (DNS/nginx vhost check) before the execution-validator (step 07) can meaningfully hit a health endpoint — using the wrong hostname would produce a false negative or false positive independent of the permission fix itself.
- Whether PR #15 (T-0113's real-deploy-logic PR, "open, not yet merged" as of 2026-07-17) has since merged — affects whether the `deploy.sh` script currently on the host matches what the landscape describes, or is still the placeholder/an intermediate version.
- No landscape record exists of any deploy attempt, successful or failed, between 2026-07-17 and today — the entire failure history the task describes (every push since PR #44 failing) postdates the landscape's last_verified date and is invisible to this repo's records; it can only be confirmed via the GitHub Actions run logs in `aiqadam/ai-qadam-platform` (outside this repo) and live SSH inspection.
- Whether an ACL-based approach (`setfacl`) is already in use anywhere on this host as precedent, or whether a fix here would be introducing a new access-control pattern — undocumented either way; relevant to step 04's choice between "fix the group-write gap again" vs. "adopt a more durable mechanism (e.g., default ACLs, or changing deploy.sh to run `chown`/`git config core.sharedRepository`)."

## Issues / risks
- The landscape explicitly frames the 2026-07-17 `usermod -aG tvolodi deploy` grant as a workaround bolted on mid-run ("unplanned... no file ownership or mode changed") rather than a designed access-control scheme — this is a plausible root cause of recurring fragility, consistent with the task's hypothesis, but the landscape cannot confirm whether the *current* failure is a repeat of the same gap or a new, different drift (e.g., group membership lost entirely vs. individual file mode/ownership overridden by a later process).
- Hostname mismatch between landscape (`qa-uz.aiqadam.org`) and the task's stated acceptance-criteria hostname (`qa.aiqadam.org`) is unresolved and must be flagged to the task-validator (step 03) — proceeding to design/execute a health check against the wrong hostname would produce misleading verification results.
- The landscape's last known deployed commit (`dfd2a7c`) does not match the task's stated "pinned at PR #44's `af30beb`" — either the landscape is stale on this one specific fact (despite last_verified being only 7 days old) or the task's framing is imprecise; this discrepancy should be resolved via live `.last-deployed-commit` read, not assumed.
- This host runs `deploy` with real forced-command SSH access and no sudo; any permission-fix design (step 04) must specify exactly which commands run as which user (root/operator via sudo, vs. `deploy` itself) — the landscape confirms `deploy` cannot self-remediate permission issues on files it doesn't own.

## Open questions (optional)
none — verdict is PASS. The gaps above are expected live-discovery items for this task, not blockers to producing this landscape summary; step 01 already anticipated live SSH discovery as necessary. Recommend the solution-designer's plan (step 04) include an explicit discovery sub-step (SSH `ls -la`, `id deploy`, `git config --get safe.directory` as `deploy`, `cat .last-deployed-commit`) before any remediation commands, and that step 03/04 explicitly reconcile the `qa.aiqadam.org` vs `qa-uz.aiqadam.org` and `af30beb` vs `dfd2a7c` discrepancies noted above.
