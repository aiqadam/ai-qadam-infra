---
run_id: 2026-07-27-fix-authentik-scope-mappings-qa-001
step: 03
agent: task-validator
verdict: PASS
created: 2026-07-27T15:27:21Z
task_id: T-0126-fix-authentik-scope-mappings-on-qa
inputs_read:
  - runs/2026-07-27-fix-authentik-scope-mappings-qa-001/step-01-task-reader.md
  - runs/2026-07-27-fix-authentik-scope-mappings-qa-001/step-02-landscape-reader.md
  - tasks/T-0126-fix-authentik-scope-mappings-on-qa.md
  - workflows/infrastructure.md
artifacts_changed: []
next_step_hint: solution-designer MUST treat "an authentik-server container exists on pro-data-tech-qa, reachable via docker exec + ak shell or the REST API" as an unverified hypothesis, not a premise. The plan's first concrete step must be a read-only discovery/reproduction phase (docker ps / docker compose ls across all projects on the host, confirm container name+project, confirm REST API reachability at https://auth.qa.aiqadam.org) with an explicit decision point: if no Authentik service is found running on the host, halt and re-route to BLOCKED (task's premise false, needs a real discovery task per landscape/domains.md's own flagged recommendation) rather than proceeding to patch anything. Also plan should independently check T-0125's AUTHENTIK_ADMIN_URL env var live (docker exec ... env | grep AUTHENTIK_ADMIN_URL on the api container) before relying on final sign-in verification, since a wrong admin URL would cause the live-verification acceptance criterion to fail for an unrelated reason.
---

## Summary
Validated with conditions: T-0126 is well-formed and in-scope, but its premise that a live, reachable Authentik service exists on `pro-data-tech-qa` is not a landscape fact — it is contradicted by the one landscape file that documents what runs on that host — so the task is only sound if its existing "reproduce first" acceptance criterion is explicitly hardened into a real discovery gate before any patch mechanism is committed to.

## Details
### Validation results

1. Well-formed: PASS — The task names a concrete, verifiable end state: QA's OAuth2 provider has the three managed `openid`/`email`/`profile` scope mappings attached, confirmed by both an API-level check and a live registration/sign-in round-trip against `qa.aiqadam.org` producing no 401. Acceptance criteria are specific and checkable, not aspirational.

2. In-scope: PASS — Patching a Docker-hosted service's config via its REST API/`docker exec`, on a managed host, is squarely `workflow: infrastructure` territory (Docker/Compose changes on the server). No workflow mismatch.

3. Not already done: PASS — Neither `landscape/hosts/pro-data-tech-qa.md` nor `landscape/services.md` records any scope-mapping state (because neither records an Authentik service at all — see check 4). Nothing in the landscape indicates this has already been fixed. The task's own Why section documents a live repro (401 persisting after the code-fix deploy) as the trigger.

4. No conflict with current state: **FAIL, but not fatal — see conditions.** `landscape/hosts/pro-data-tech-qa.md` documents exactly two Compose projects on this host (`ai-qadam-test`, `aiqadam-qa`), and the `aiqadam-qa` project's containers are `aiqadam-qa-oidc-stub-1` (a static `nginx:alpine` stub explicitly annotated "real OIDC login is out of scope for this environment") and `aiqadam-qa-api-1`. There is no `authentik-server` container, no separate Authentik Compose project, documented anywhere. The task's central premise — a live Authentik OAuth2 provider is running and reachable at `auth.qa.aiqadam.org`, in a container named `authentik-server`, "part of the `aiqadam-qa` compose stack," "running unchanged for 9 days" — is not a landscape fact. It is narrative carried over from an unrelated app-repo local-dev fix (PR #81), corroborated only by a DNS A record created out-of-band on 2026-07-18 by work this repo doesn't own or track. This is a real conflict (an authoritative landscape file affirmatively describes the OIDC surface on this host as a non-functional stub), not merely a gap — it must be treated as such rather than waved through as "unknown but probably fine."

5. Discoverable scope: PASS, conditionally — The task's own first acceptance criterion already requires reproducing the bug live before any patch, which is the right shape for this situation. That is sufficient to proceed *if* solution-designer explicitly elevates "does an Authentik service exist on this host at all, and where" to a first-class discovery step with a real halt/re-route branch — not folded silently into "confirm current property_mappings state" as the landscape-reader's handoff prose does. As written, the task's acceptance criteria and Notes assume the *mechanism* (`docker exec` into `authentik-server`, or the REST API) is correct and only the *data* (property_mappings) is in question. That assumption is exactly what's unverified. The gap is discoverable (a `docker ps`/`docker compose ls` sweep resolves it in seconds), so this does not need to go back to step 01/02 or to the user — but it does need to be made explicit before solution-designer commits to a plan, per this run's instructions.

6. Workflow-specific rules respected: PASS — Idempotency: the task's own Notes state the PATCH is additive/idempotent (re-attaching already-attached mappings is a no-op); no non-idempotency concern. Backup-before-destructive: not applicable — no config file is overwritten or data deleted, only a property-mapping list is extended via API. Verify in two places: satisfiable and already called for — acceptance criteria include both a host/API-level check (property_mappings state) and an externally-observable check (real sign-in round-trip against `qa.aiqadam.org`), matching the rule's intent.

## Issues / risks
- **Premise risk (primary):** the task assumes an `authentik-server` container exists, is part of the `aiqadam-qa` stack, and has been "running unchanged for 9 days." None of this is corroborated by any landscape file. If solution-designer builds a plan around this exact container name/location without a discovery step, the plan will simply fail at execution when `docker exec authentik-server ...` finds no such container — or worse, could accidentally target the wrong container if names are approximate.
- **T-0124/T-0125 status risk:** T-0126's Why section treats T-0124 (deploy permission fix) and T-0125 (`AUTHENTIK_ADMIN_URL` fix) as resolved. Per landscape-reader, both are genuinely still `in-progress`, stalled at step-04 with no executor or validator ever run. T-0124's resolution is plausibly self-evident (the triggering deploy of `cc43257` did succeed per GitHub Actions log, which is external evidence independent of this repo's workflow trail) — low residual risk. T-0125 is a bigger concern: if `AUTHENTIK_ADMIN_URL` on QA's `api` container is still pointing at prod's Authentik instance, T-0126's own final acceptance criterion (live sign-in round-trip against `qa.aiqadam.org`) could fail for a completely unrelated reason (wrong admin URL, not missing scope mappings), producing a confusing false-negative that could send whoever runs verification down the wrong debugging path. This doesn't block T-0126 from proceeding, but the plan should check this env var live, early, so a T-0125 failure isn't misattributed to this task's own fix.
- **Out-of-band service risk:** the `auth.qa.aiqadam.org` DNS record and whatever stands behind it were created outside this repo's tracked workflow. Whoever did that setup may have used non-standard conventions (container naming, admin bootstrap, network placement) that a solution built purely on the PR #81 local-dev precedent won't anticipate. Discovery should not assume the QA Authentik deployment mirrors local-dev's structure.
- **Escalation path if discovery finds nothing:** if the first discovery step finds no Authentik service running anywhere on `pro-data-tech-qa` (contradicting even the DNS-record-based assumption), that is a different problem than "scope mappings need patching" — it would mean the task's entire premise is false and this should re-route to BLOCKED with a recommendation to file the host-level Authentik discovery task that `landscape/domains.md` already flagged as missing, rather than solution-designer improvising a deployment plan under this task's identity.

## Open questions (optional)
none — the task can proceed to solution-designer under the conditions above; nothing here requires user input to resolve, since the missing facts are discoverable read-only on the host itself and the task's own acceptance-criteria shape already anticipates a reproduce-first sequence. Verdict is PASS rather than BLOCKED because the fix is procedural (harden the discovery step, add an explicit halt branch) and within solution-designer's normal authority to specify.
