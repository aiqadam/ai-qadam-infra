---
run_id: 2026-07-27-fix-authentik-scope-mappings-qa-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-07-27T00:00:00Z
task_id: T-0126-fix-authentik-scope-mappings-on-qa
inputs_read:
  - runs/2026-07-27-fix-authentik-scope-mappings-qa-001/step-01-task-reader.md
  - tasks/T-0126-fix-authentik-scope-mappings-on-qa.md
  - tasks/T-0124-fix-deploy-qa-permission-denied.md
  - tasks/T-0125-fix-authentik-admin-url-on-qa.md
  - landscape/hosts/pro-data-tech-qa.md
  - landscape/services.md
  - landscape/secrets-inventory.md
  - landscape/README.md
  - landscape/domains.md
  - landscape/cloudflare.md
  - tasks/_index.md
  - runs/2026-07-24-fix-deploy-qa-permission-001/ (dir listing only)
  - runs/2026-07-24-fix-authentik-admin-url-001/ (dir listing only)
artifacts_changed: []
next_step_hint: task-validator must treat Authentik-on-QA as an undocumented service — the host/services landscape files contain zero facts about it (no container name, no admin URL path, no provider pk, no scope-mapping state). All of T-0126's technical assumptions (authentik-server container, docker exec + ak shell mechanism, REST API at auth.qa.aiqadam.org) come only from the task file's own narrative/precedent (PR #81 on the app repo) and from DNS/Cloudflare records, not from any host-level discovery in this repo. Recommend task-validator either accept this as a live-discovery-first task (first AC already requires reproducing the bug live) or flag BLOCKED for a scoped discovery sub-step before solution-designer commits to a mechanism. Also: T-0124 and T-0125 are NOT resolved — both are genuinely still in-progress (stalled at step-04, no executor/validator/landscape-update ever ran), contradicting the past-tense "resolved"/"complete" language in T-0126's own Why section.
---

## Summary

The landscape has **no record at all** of an Authentik service running on `pro-data-tech-qa`. `landscape/hosts/pro-data-tech-qa.md` (last_verified 2026-07-17, populated) documents exactly two Docker Compose projects on that host — `ai-qadam-test` (postgres) and `aiqadam-qa` (`aiqadam-qa-oidc-stub-1`, a static `nginx:alpine` OIDC-discovery stub explicitly marked "real OIDC login is out of scope for this environment," plus `aiqadam-qa-api-1`) — and neither `services.md` nor the host file mentions an `authentik-server` container, an Authentik admin URL, or any provider/scope-mapping state. The only landscape trace of Authentik-on-QA is indirect: `landscape/domains.md` and `landscape/cloudflare.md` both record a Cloudflare A record `auth.qa.aiqadam.org → 95.46.211.230` (Cloudflare record comment: "pro-data-tech-qa Authentik"), created 2026-07-18 by **separate, out-of-band work not tracked by any task file in this repo**, discovered only incidentally during a 2026-07-19 pre-cutover DNS zone-diff safety check, and explicitly flagged there as needing "a host-level discovery task... to document this service properly" — which has never been filed or run. Everything T-0126 asserts about the live Authentik setup (container name `authentik-server`, `docker exec ... ak shell` minting pattern, REST API shape, current provider `property_mappings` state) is carried over by narrative/precedent from the *unrelated* local-dev/app-repo fix in PR #81, not from any fact this repo has verified on QA. Access to the host itself is well documented and current: SSH as `root@95.46.211.230` (break-glass, key-only) or as operators `tvolodi`/`viktor_d`/`binali_r` (all sudo-capable, `docker` group members), Docker 29.6.1 / Compose v5.3.1 installed and active. No Authentik admin API token for QA exists anywhere in `secrets-inventory.md`. Separately, T-0124 and T-0125 — both referenced by T-0126 as effectively concluded — are **not** closed: `tasks/_index.md` still lists both `status: in-progress`, and their respective runs (`2026-07-24-fix-deploy-qa-permission-001`, `2026-07-24-fix-authentik-admin-url-001`) each stop at step-04 (solution-designer) with no step-05 onward, meaning no executor ever ran, nothing was verified live, and no landscape update ever landed for either.

## Details

### Relevant facts (sourced from landscape)

- `pro-data-tech-qa` (95.46.211.230) access: SSH break-glass `root` (key-only, provider key), or operators `tvolodi`/`viktor_d`/`binali_r` (all in `sshusers` + `sudo` + `docker` groups, NOPASSWD sudo, live-verified for `tvolodi`). Management-workstation SSH alias `pro-data-tech-qa` defaults to user `tvolodi`. — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Docker Engine 29.6.1 / Compose plugin v5.3.1 / containerd installed and active on the host; `docker exec` is available to any operator in the `docker` group without sudo. — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Two Compose projects run on this host: `ai-qadam-test` (postgres only) and `aiqadam-qa` (`aiqadam-qa-oidc-stub-1` — a static `nginx:alpine` OIDC discovery stub, and `aiqadam-qa-api-1` — the NestJS API). **No `authentik-server` container, no Authentik Compose project, and no third project of any kind is documented on this host.** — _source: `landscape/hosts/pro-data-tech-qa.md`, `landscape/services.md`_
- The `aiqadam-qa-oidc-stub-1` container is explicitly documented as a "permanent fixture," not a real OIDC/Authentik provider — "real OIDC login is out of scope for this environment." This is a direct tension with T-0126's premise that a live Authentik OAuth2 provider exists and is reachable at `auth.qa.aiqadam.org`. — _source: `landscape/hosts/pro-data-tech-qa.md`_
- A Cloudflare DNS A record `auth.qa.aiqadam.org → 95.46.211.230` exists, comment "pro-data-tech-qa Authentik," created 2026-07-18T04:40 UTC by work this repo did not perform and has no task file for. A sibling record `qa.aiqadam.org → 95.46.211.230` (comment "migrated from qa-uz") was created the same time, also out-of-band. Both are "user-confirmed expected" but otherwise undocumented. — _source: `landscape/domains.md`, `landscape/cloudflare.md`_
- `landscape/domains.md` itself flags: "Not otherwise documented in `hosts/pro-data-tech-qa.md` — a host-level discovery task may be warranted to document this service properly." No such discovery task exists in `tasks/_index.md` as of this run. — _source: `landscape/domains.md`_
- `secrets-inventory.md`'s "AiQadam QA — pro-data-tech-qa" section lists only `aiqadam-qa-jwt-signing-secret`, `aiqadam-qa-internal-api-token`, and `aiqadam-qa-deploy-ssh-key`. **No Authentik admin API token, Authentik admin password, or Authentik bootstrap credential for QA appears anywhere in the file.** — _source: `landscape/secrets-inventory.md`_
- `tasks/_index.md` shows T-0124 and T-0125 both still `status: in-progress` (not `done`), same as T-0126. — _source: `tasks/_index.md`_
- T-0124's own run (`2026-07-24-fix-deploy-qa-permission-001`) has artifacts only through step-04 (solution-designer) — no step-05 (approval), step-06 (executor), step-07 (validator), or step-08 (landscape-updater) exist. The permission-denied fix described in T-0126's Why section as already resolved was therefore never confirmed executed by this repo's own workflow trail. — _source: dir listing, `runs/2026-07-24-fix-deploy-qa-permission-001/`_
- T-0125's own run (`2026-07-24-fix-authentik-admin-url-001`) likewise has artifacts only through step-04 — no execution or verification ever ran for the `AUTHENTIK_ADMIN_URL` env-var fix either. — _source: dir listing, `runs/2026-07-24-fix-authentik-admin-url-001/`_
- Despite the above, `deploy.sh`'s design (regex-validated git ref, `git fetch`+`git cat-file -e` check, rollback markers, `deploy` user with `tvolodi`-group write access via T-0113) is documented as "confirmed working end-to-end via a live rehearsal" as of 2026-07-17 — i.e., the deploy mechanism itself is sound; what's unconfirmed is whether the *specific* permission-denied bug T-0124 was opened for is actually fixed. — _source: `landscape/hosts/pro-data-tech-qa.md`_
- `landscape/README.md` confirms the standard landscape-editing rule: files are authoritative, edited by landscape-updater (step 08) or manual correction; `secrets-inventory.md` is gitignored, never committed, must exist locally. — _source: `landscape/README.md`_
- `pro-data-tech-qa.md`'s own `last_verified_note` (2026-07-17) is itself about T-0113 (deploy.sh replacement) — the file has not been updated since, so it cannot reflect anything from the 2026-07-18 out-of-band Authentik/qa.aiqadam.org DNS work, T-0124, or T-0125. — _source: `landscape/hosts/pro-data-tech-qa.md`_

### Stale or stub files encountered

- None of the files read are stale by the 30-day rule: `hosts/pro-data-tech-qa.md` last_verified 2026-07-17 (10 days old), `services.md` last_verified 2026-07-23 (4 days old), `domains.md` and `cloudflare.md` last_verified 2026-07-23 (4 days old). All `status: populated`/`active` (not stub).
- No file is a stub in the relevant scope. However, functionally, the landscape's coverage of "Authentik on QA" is stub-equivalent — the service exists in reality (per DNS + task narrative) but has zero lines of description in any landscape file outside a one-line DNS-record comment.

### Gaps requiring live discovery

- **Whether Authentik actually runs on `pro-data-tech-qa` at all, and if so how**: container name, image/version, Compose project name and file path, port bindings, whether it's part of the `aiqadam-qa` project or a separate one. Nothing in the landscape confirms this beyond a DNS record comment.
- **The Authentik admin access mechanism on QA**: task assumes `docker exec ... ak shell` (mirroring local-dev/PR #81) works identically on QA, but this has never been verified against QA's actual deployment — if QA's Authentik was set up "out of band" (as its DNS record was), its container/access pattern could differ.
- **Current provider `pk` and `property_mappings` state** for the QA OAuth2 provider — task's own first acceptance criterion requires reproducing this live; landscape has no cached copy.
- **Whether an Authentik admin credential already exists outside `secrets-inventory.md`** (e.g., in `credentials.md`, which this reader was not directed to check, or set up by whoever did the 2026-07-18 out-of-band work) — worth a targeted check before assuming a token must be minted from scratch.
- **Real status of T-0124's permission-denied fix and T-0125's `AUTHENTIK_ADMIN_URL` fix** — both tasks' own workflow runs stopped at step-04 with no execution ever recorded in this repo. T-0126's Why section treats both as resolved/settled based on narrative claims of live testing, but no landscape file or run artifact in this repo corroborates that testing happened under a completed, validated workflow. If T-0125's fix (setting `AUTHENTIK_ADMIN_URL` to `https://auth.qa.aiqadam.org` in `docker-compose.qa.yml`) was never actually applied, the QA `api` container may still be defaulting to prod's Authentik URL — worth a live `docker exec ... env | grep AUTHENTIK` check before or alongside T-0126's own work, since T-0126's live-verification acceptance criterion (registration/sign-in round trip against `qa.aiqadam.org`) would silently fail for the *wrong* reason (wrong admin URL, not missing scope mappings) if T-0125 is in fact still outstanding.

## Issues / risks

- **Primary risk**: solution-designer may be tempted to copy the `docker exec authentik-server ak shell` mechanism verbatim from the app-repo's local-dev precedent (PR #81) without confirming a container by that name exists on QA. If Authentik on QA turns out to be deployed differently (different container name, external Authentik-as-a-service, or a Compose project not yet in `services.md`), that command will simply fail — this should be the very first thing the executor checks (`docker ps` / `docker compose ls` on the host), and the plan should not hardcode the container name as fact.
- T-0126's Why section states "QA's Authentik container (`authentik-server`, part of the `aiqadam-qa` compose stack) has been running unchanged for 9 days" — this specific claim (container name + which Compose project it belongs to) is **not corroborated by any landscape file**. `landscape/services.md`'s `aiqadam-qa` project table lists exactly two containers (`oidc-stub`, `api`), not three. This should be treated as an unverified assumption, not a landscape fact, until the executor confirms it live.
- T-0124 and T-0125 being genuinely unresolved (not just "in-progress" as a formality) means the QA environment's actual state is less certain than T-0126's narrative implies — e.g., if T-0125's env-var fix was never applied, live verification of T-0126's own fix could fail even after correctly patching the property_mappings, producing a confusing false-negative for whoever runs the final registration/sign-in check.
- No Authentik admin token for QA exists in the gitignored `secrets-inventory.md`; per the task's own Notes, the executor is expected to mint one live via `ak shell` rather than requesting a new long-lived credential — this is consistent with what the landscape shows (nothing pre-provisioned), so no contradiction there, just confirmation that the "no token recorded" premise in the task file is accurate.
- The `auth.qa.aiqadam.org` / `qa.aiqadam.org` DNS records and whatever service stands behind them were created by out-of-band work this repo doesn't own or track — coordinate-before-assuming-exclusive-control caution (already noted in `cloudflare.md` for the wider zone) applies here too; the executor should not assume it has unilateral license to reconfigure anything under `auth.qa.aiqadam.org` beyond the specific property-mapping PATCH the task describes.

## Open questions (optional)

none — landscape is populated and current enough to proceed; the gaps above are exactly the kind of live-discovery items the task's own first acceptance criterion (reproduce the bug via docker exec/REST API before patching) already anticipates. Verdict is PASS, not BLOCKED, because the task itself is structured as reproduce-then-fix and the missing facts are all things the executor is expected to establish live — but task-validator and solution-designer should treat the "authentik-server / 9 days unchanged / part of aiqadam-qa stack" claims as unverified narrative, not landscape fact, and should explicitly account for T-0124/T-0125's real (unresolved) status rather than the "apparently resolved" framing carried over from the task file.
