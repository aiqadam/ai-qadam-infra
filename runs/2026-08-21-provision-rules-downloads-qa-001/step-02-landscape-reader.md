---
run_id: 2026-08-21-provision-rules-downloads-qa-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-08-21T06:20:00Z
task_id: T-0141-provision-rules-source-file-downloads-qa
inputs_read:
  - runs/2026-08-21-provision-rules-downloads-qa-001/step-01-task-reader.md
  - landscape/README.md
  - landscape/hosts/pro-data-tech-qa.md
  - landscape/services.md
  - landscape/secrets-inventory.md
  - shared/app-registry.md
  - workflows/deploy-app.md
artifacts_changed: []
next_step_hint: Landscape is fresh (2026-08-20, T-0136/T-0137/T-0138) and rich on credentials, hygiene rules, and DB/network topology — but silent on Directus's host port, the `directus` database, the public Directus origin, and the FR-CMS-008 surface. Those are live-discovery items for the solution-designer, not blockers.
---

## Summary

The committed landscape is current (`last_verified: 2026-08-20`, one day old) and unusually well-populated for this host, because T-0136/T-0137/T-0138 all executed against this exact Directus instance yesterday. It authoritatively fixes the credential (`DIRECTUS_ADMIN_TOKEN` in `/opt/apps/aiqadam-qa/deploy/.env`, rotated 2026-08-20), the container identity and pinned version (`aiqadam-qa-directus-1`, `directus/directus:11`, 11.17.4, `network_mode: host`), the precedent that both target scripts have already been run successfully against this instance once, and the output-hygiene rules that two secret-exposure incidents produced. It is, however, **silent on four facts the two scripts need**: Directus's host port, the name of the Directus database, the public Directus origin the rendered download links must resolve to, and anything at all about the FR-CMS-008 surface (`portal-content/`, `public-documents`, `PUBLIC_ASSET_FOLDER_ID`, the anonymous asset grant). I am read-only and cannot probe the host, so those are flagged below for the solution-designer to discover live rather than guessed at here.

## Details

### Relevant facts (sourced from landscape)

**Host and access**

- Target host is `pro-data-tech-qa`, IPv4 `95.46.211.230`, Ubuntu 26.04, provider pro-data.tech (not Hetzner) — _source: `landscape/hosts/pro-data-tech-qa.md`_
- SSH alias `pro-data-tech-qa` exists in `C:\Users\tvolo\.ssh\config` but **configures `User tvolodi`**; prior runs deliberately used `ssh -i ... root@95.46.211.230` explicitly to avoid landing as `tvolodi`. Operator key for `tvolodi` is the `ai-dala-infra` ed25519 key; the provider RSA key (`SHA256:1X5RtbilgvvakpD5wTENNyKK9Lkoc9sOXoAxeuy9DL0`, workstation path `C:\Users\tvolo\.ssh\pro-data.tech-qa-instance_rsa.ppk`, OpenSSH format despite the extension) is the root break-glass anchor — _source: `landscape/hosts/pro-data-tech-qa.md`_
- `tvolodi` (uid 1001) is in `sudo`, `sshusers`, `docker` (gid 986), and `aiqadam-qa-secrets` (gid 980) — so `tvolodi` can run `docker` without sudo **and** has group-read on `deploy/.env` — _source: `landscape/hosts/pro-data-tech-qa.md`_
- The app checkout `/opt/apps/aiqadam-qa/` is owned `tvolodi:tvolodi`, mode `775`/`755` — _source: `landscape/hosts/pro-data-tech-qa.md`_ (relevant: the `scp` prerequisite writes a new subdirectory here, and `tvolodi` owns it, so no privilege escalation is needed for the copy)
- UFW is active, deny-by-default, with only 22/80/443 open. No new port needs opening for this task — everything happens over SSH or through the existing nginx vhost — _source: `landscape/hosts/pro-data-tech-qa.md`, `landscape/services.md`_

**QA Directus — what IS documented**

- Container `aiqadam-qa-directus-1`, image `directus/directus:11`, Compose project `aiqadam-qa`, compose file `/opt/apps/aiqadam-qa/deploy/docker-compose.qa.yml`, `network_mode: host`, status `Up (healthy)` — _source: `landscape/services.md`, `landscape/hosts/pro-data-tech-qa.md`_
- **Pinned version confirmed 11.17.4** (image digest `sha256:eb326f679ae847c0a776f93b972761dc2ebe84980e0b9d274a6bc31cd62809f7`), established live by T-0137 — _source: `landscape/hosts/pro-data-tech-qa.md` (Change log, 2026-08-20)_
- **Host port: recorded as "not enumerated."** Both the host file's container table and `services.md`'s container table give Directus's host ports as `not enumerated` — _source: `landscape/hosts/pro-data-tech-qa.md`, `landscape/services.md`_ (see Gaps)
- The reserved loopback range for test apps on this host is `127.0.0.1:3110-3119`; documented occupants are `3112` (postgres), `3113` (api), `9999` (oidc-stub) — _source: `shared/app-registry.md`_ (this is the range a Directus port would fall inside, but the landscape does not assign one)
- Directus is one of four containers that reach Postgres over `127.0.0.1:3112` under `network_mode: host`, and `pg_hba.conf` rates that path `trust` — meaning Directus's own DB connection has never actually been password-authenticated. Recorded by T-0138 as an incidental finding, not a new issue — _source: `landscape/hosts/pro-data-tech-qa.md`_

**Credential — fully pinned down by the landscape**

- `DIRECTUS_ADMIN_TOKEN` lives in `/opt/apps/aiqadam-qa/deploy/.env` on `pro-data-tech-qa`; last rotated **2026-08-20** (T-0137), reason: transcript exposure during T-0136 — _source: `landscape/secrets-inventory.md`_
- It is the **canonical, compose-wired** Directus admin static token: the `directus` service consumes it directly as its `ADMIN_TOKEN` bootstrap var, and the `api` service receives it via compose interpolation `DIRECTUS_TOKEN: ${DIRECTUS_ADMIN_TOKEN:?...}` — confirmed empirically by a SHA-256 digest match during T-0137 — _source: `landscape/hosts/pro-data-tech-qa.md` (Change log), `landscape/secrets-inventory.md`_
- `DIRECTUS_TOKEN` also exists as a flat key in the same `.env`; at T-0137's rotation time it resolved to the *same* Directus identity (`admin@aiqadam.org`) despite a different literal string. Both keys now hold the identical value. **Implication for this run:** either key would authenticate, but `DIRECTUS_ADMIN_TOKEN` is the one the task names and the canonical one to read — _source: `landscape/secrets-inventory.md`_
- `.env` is mode `640`, owner `tvolodi:aiqadam-qa-secrets` — readable by `tvolodi` and by the `deploy` CI user, not world-readable — _source: `landscape/hosts/pro-data-tech-qa.md`_
- A `.env` backup from T-0137 (`deploy/.env.pre-T0137.<timestamp>.bak`, 1563 B) exists on-host and **contains old secret values by design** — worth not `cat`-ing during this run — _source: `landscape/hosts/pro-data-tech-qa.md`_

**Directus admin mechanics established live yesterday (directly reusable)**

- `PATCH /users/me` rotates the static token / password live; DB-row fields are read per-request — **no Directus restart is needed** for admin-credential changes. Conversely, `ADMIN_TOKEN`/`ADMIN_PASSWORD` env vars are first-run bootstrap-only. Not needed for T-0141 itself, but it establishes that Directus admin operations against this instance are HTTP-API-driven and restart-free — _source: `landscape/hosts/pro-data-tech-qa.md` (Change log, T-0137)_
- **`aiqadam-qa-api-1` needs a `docker compose up -d --no-deps api` recreate to pick up any `.env` change** (Docker env vars are not hot-reloaded). T-0141 changes no env var, so this should NOT be needed — but if the designer's plan ends up touching `.env`, this is the required follow-on — _source: `landscape/hosts/pro-data-tech-qa.md` (Change log, T-0137)_
- `web-next` holds **zero** `DIRECTUS*` env vars (confirmed live during T-0137) — so whatever origin the rendered download link uses, it is not coming from a `DIRECTUS_*` env var on `web-next` — _source: `landscape/hosts/pro-data-tech-qa.md` (Change log, T-0137)_

**Precedent: both target scripts have already been run against this instance**

- T-0136 (2026-08-20, run `2026-08-20-seed-content-documents-qa-001`) ran `infrastructure/directus/bootstrap.sh` **to completion against QA for the first time ever**, creating `content_pages` and `content_documents` plus their PUBLIC read grants, and confirmed idempotent no-op on all ~77 pre-existing collections — _source: `landscape/hosts/pro-data-tech-qa.md`_
- The **one** non-no-op side effect was a dated (2026-05-25), already-decided cleanup block labelled `F-S2.12 — drop F-S2.8.x operator_invites.* email-routing fields`, which dropped 7 legacy fields from `operator_invites`. Confirmed benign — live `apps/api` code already accounts for the drop at `apps/api/src/modules/admin-invites/admin-invites.controller.ts:51`. **Having been applied once, it should be a no-op on this run** — _source: `landscape/hosts/pro-data-tech-qa.md`_
- T-0136 also ran `infrastructure/directus/seed-content-documents.sh`, seeding exactly the 5 governance rows this task is about: `manifesto`, `charter-v0-1`, `kazakhstan-mou`, `global-board-polozhenie-v1`, `soglashenie-v1`. Verified live: `/rules` lists all 5, `/rules/charter-v0-1` renders full content, superseded labels on exactly `global-board-polozhenie-v1` and `soglashenie-v1`, `/about`/`/history`/`/partners` unaffected (all 200) — _source: `landscape/hosts/pro-data-tech-qa.md`_
- **Direct relevance:** the `content_documents` rows already exist. This run is adding a `source_file` column, a folder, a grant, and 5 file attachments *to rows that are already there* — a genuinely additive delta on top of a state the landscape confirms. It also means the seed script's row-upsert half is expected to be a no-op and only its upload/attach half should do work.
- **Key lesson recorded from T-0136, directly applicable:** every 403 across six failed attempts that day was Directus accurately reporting a collection that *did not exist yet*. RBAC was never broken; the Administrator policy always had genuine `admin_access: true` bypass-all. Directus's 403 message reads "...you don't have permission **or it does not exist**" — the second half is often the true one. If this run hits a 403, the landscape's own guidance is to check existence before concluding permissions — _source: `landscape/hosts/pro-data-tech-qa.md`_

**Output hygiene — a hard project rule, sourced from two real incidents**

- Two transcript-exposure incidents occurred on 2026-08-20 against this exact host and credential family (T-0137 for the Directus token, T-0138 for the Postgres password). The recorded mechanism of the first: a `grep -B/-A` combined with a `-v` exclusion **failed to suppress matched secret lines**. Both were local-transcript-only, no external exposure — _source: `landscape/hosts/pro-data-tech-qa.md` (Change log), `landscape/secrets-inventory.md`_
- The T-0137 run explicitly records "No secret value was printed, logged, or written anywhere in this run" as an achieved property — that is the bar for this run too — _source: `landscape/hosts/pro-data-tech-qa.md`_

**Network topology (learned by T-0138, relevant if any DB-level verification is contemplated)**

- `ai-qadam-test-db-1` sits on user-defined bridge network `ai-qadam-test_default`, gateway `172.18.0.1`, container IP `172.18.0.2` — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Genuine DB connectivity verification requires a throwaway client attached to that **named bridge network**, connecting by container name (`-h ai-qadam-test-db-1`). `--network container:ai-qadam-test-db-1` does **not** work — it shares the target's own namespace, making `172.18.0.1` the gateway rather than a valid peer, and produces "connection refused" rather than an auth result. This exact mistake was in T-0138's *approved* plan and had to be corrected mid-run — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Databases documented inside that container: `aiqadam_test` (original) and `aiqadam_qa` (added by T-0110). User `aiqadam` — _source: `landscape/services.md`, `shared/app-registry.md`_

**Public endpoint / regression surface**

- Sole live nginx vhost on this host is `qa.aiqadam.org`, proxying `443` → `127.0.0.1:3113` (the api container). `qa-uz.aiqadam.org` was retired out-of-band; its DNS still resolves here but has no vhost or cert — _source: `landscape/services.md`, `landscape/hosts/pro-data-tech-qa.md`_
- `auth.qa.aiqadam.org` is referenced as the externally-reachable route to Authentik, which is also `network_mode: host` with "no published ports" — establishing that this host **does** front host-networked containers with additional nginx vhosts that the vhost inventory does not fully enumerate — _source: `landscape/hosts/pro-data-tech-qa.md`_
- Health endpoint: `GET https://qa.aiqadam.org/health` → 200, `{"status":"ok","service":"api",...}`. Note `deploy-app`'s step-07 rule says to read the app's Health endpoint from `shared/app-registry.md` — but the registry's QA row still names the **retired** `https://qa-uz.aiqadam.org/health`. The correct current URL is the `qa.aiqadam.org` one — _source: `landscape/services.md` vs. `shared/app-registry.md`_ (see Issues)

**Workflow-mandated registry check (`deploy-app` "Script-first execution model")**

- `shared/app-registry.md` has **no `Scripts` table** for the `aiqadam` app — neither the QA nor the prod section contains one. The workflow says: if a script exists for the operation, run it; if the cell says "not yet created," fall back to a manual sequence and note that a script should be created. Here there is no table at all. **However**, the operation *is* script-driven — the two scripts live in the app repo (`infrastructure/directus/*.sh`), which is exactly where the workflow wants operational knowledge to live. The spirit of the rule is satisfied; the registry is simply not recording these scripts — _source: `shared/app-registry.md`, `workflows/deploy-app.md`_
- `shared/app-registry.md` is materially stale in several respects beyond the health URL — see Issues.

### Stale or stub files encountered

- `landscape/hosts/pro-data-tech-qa.md` — `last_verified: 2026-08-20`, `status: populated`. **Fresh (1 day).** No staleness flag.
- `landscape/services.md` — `last_verified: 2026-08-17`, `status: populated`. 4 days old, within the 30-day threshold. But its `pro-data-tech-qa` container detail is dated 2026-07-27 (T-0126) and predates yesterday's three runs; the host file is the more current source for anything Directus-related.
- `landscape/secrets-inventory.md` — `last_verified: 2026-08-20`, `status: populated`. **Fresh (1 day).** Git-ignored by design, present in this checkout.
- `shared/app-registry.md` — `last_updated: 2026-07-17`. **35 days old**, past the 30-day threshold, and demonstrably out of date (details in Issues). Not a `landscape/` file so the staleness rule applies only by analogy, but the `deploy-app` workflow makes it authoritative for ports/paths/health endpoints, so its staleness matters.

### Gaps requiring live discovery

These are facts the solution-designer needs and the committed landscape does **not** contain. I am read-only and cannot probe the host, so I am flagging rather than guessing:

1. **Directus's host port.** Recorded as "not enumerated" in both container tables. Every `curl` in both scripts needs a concrete base URL. The reserved range `127.0.0.1:3110-3119` is documented with `3112`/`3113`/`9999` assigned, which brackets the answer without giving it. *(The orchestrator's pre-flight has separately established this as `3119`. That is a live fact, not a landscape fact — it should be written into the landscape at step 08, since its absence is exactly what makes this a gap today.)*
2. **The Directus database name.** The landscape documents `aiqadam_test` and `aiqadam_qa` inside `ai-qadam-test-db-1` and says nothing about a Directus-specific database. Whether Directus shares `aiqadam_qa` or has its own DB in that cluster is unrecorded. Not strictly needed to run the two scripts (both are HTTP-API-driven, not SQL), but needed for any pre-change dump/rollback and for step 08's landscape write-back. *(Pre-flight has established a separate `directus` database exists in that cluster — again a live fact absent from the committed landscape.)*
3. **The public Directus origin.** AC-6 requires the rendered link to resolve to the *public* Directus host, not the internal `directus:8055` docker alias. The landscape records exactly one QA vhost (`qa.aiqadam.org` → api on 3113) and an incidental `auth.qa.aiqadam.org`. It does not say whether a Directus vhost exists, nor which env var composes the public asset URL. Since `web-next` was confirmed to hold zero `DIRECTUS*` env vars, the origin likely comes from the `api` container or a build-time value — but this is inference, not landscape. **The designer must determine, live, what public URL an anonymous browser will actually hit for `/assets/<id>`, and whether it is reachable from outside the host at all.** If Directus is host-networked on a loopback-only port with no vhost, AC-4 and AC-6 may not be satisfiable without an nginx change — which would be a scope expansion this task did not anticipate.
4. **The entire FR-CMS-008 surface is absent from the landscape.** No mention anywhere of `portal-content/`, `public-documents`, `PUBLIC_ASSET_FOLDER_ID`, `source_file`, or any anonymous `directus_files` grant (grep across `landscape/` returns zero hits). So the landscape offers **no evidence that any part of the target state already exists** — consistent with the task's premise that this is unprovisioned. It also means none of the "not already done" question can be answered from the landscape alone; it rests on the pre-flight's live findings.
5. **Whether the QA checkout is at the FR-CMS-008 merge commit.** The landscape's most recent recorded checkout HEAD is `b5250071` (2026-07-29, T-0125); `shared/app-registry.md` still says `dfd2a7c` (2026-07-13). Both are far behind. The landscape cannot confirm freshness. *(Pre-flight established the checkout is at `84cfd4d` with the FR-CMS-008 code present — and specifically that a `git merge-base --is-ancestor` check against the pre-squash branch SHA gives a **false negative**, because that SHA doesn't exist in main's history after a squash-merge. The grep-for-actual-code check is the reliable one. Worth recording, as the landscape has no note about this failure mode.)*
6. **Which SSH identity to use for `scp`.** The landscape documents the identities but flags that the `pro-data-tech-qa` alias silently resolves to `User tvolodi`, and that prior runs used explicit `-i ... root@...` to avoid that. For this task, `tvolodi` is arguably the *better* target — it owns `/opt/apps/aiqadam-qa/` and is in `aiqadam-qa-secrets` — but the designer should state the choice explicitly rather than let the alias decide.
7. **No pre-change backup convention is established for Directus schema.** The host has a strong backup-before-change culture (`.env.pre-T0137.*.bak`, `/var/backups/pre-T0099.*`, `/etc/default/ufw.bak`), but nothing about dumping Directus schema or the `directus` database before a `bootstrap.sh` run — T-0136 apparently took none. `deploy-app` requires the designer's plan to include an exact rollback command. The designer will need to construct one; the landscape offers no precedent to copy.

None of these gaps make the task impossible to design safely — they are all resolvable by live inspection at design time, and several are already resolved by the orchestrator's pre-flight. Hence `PASS`, not `BLOCKED`.

## Issues / risks

- **`shared/app-registry.md` is stale and actively misleading in three places.** Its QA row states the compose file has "2 services: `oidc-stub`, `api`" (actually 7 since at least 2026-07-27), names the health endpoint as `https://qa-uz.aiqadam.org/health` (that vhost is retired), and pins the checkout at `dfd2a7c` (superseded at least twice). It also asserts as a "Scope decision" that "Directus-CMS-backed routes are NOT deployed here… non-functional in this environment by design" — which the host file explicitly supersedes ("as of 2026-07-27, Directus-CMS-backed routes and Authentik-backed OIDC login are both live in this environment"). **A downstream agent that trusts the registry over the host file will design against a environment that no longer exists.** The host file wins. Worth a follow-up task to reconcile the registry; out of scope for T-0141's `affects:` list, which names only the host file.
- **`deploy-app`'s step-07 health-check rule points at the stale registry.** The workflow instructs the execution-validator to look up the Health endpoint in `shared/app-registry.md` and "do NOT assume `/api/health`" — but the registry's value is the dead `qa-uz` hostname. The validator must use `https://qa.aiqadam.org/health`, per `landscape/services.md` and the host file.
- **`deploy-app` is a slightly awkward fit for this task, though not a wrong one.** Its core machinery — build image, `docker compose up --force-recreate`, rollback by re-tagging an image, Phase 0 local→GitHub→host code sync — is all no-op here. Nothing is built, no container is recreated, no app code moves. What actually happens is: copy 5 binary files onto the host, then run two idempotent scripts already present in the checkout. The workflow's genuinely applicable requirements (script-first execution, an explicit rollback command, a health check at step 07) all still bind. The designer should state Phase 0 as an explicit no-op rather than silently skipping it, and should not force-fit the image-tag rollback model to a schema+content change.
- **The `scp` prerequisite is state-changing and is not one of the two named scripts.** It writes a new untracked directory into `/opt/apps/aiqadam-qa/`. Two knock-on effects the designer should handle: (a) `deploy.sh` never runs `git clean` (a hard rule enforced only by a comment in its own header, not by a technical control), so an untracked `portal-content/` should survive future deploys — but that safety rests on a comment; (b) the files exist in no repository, so a host rebuild silently regresses the feature. Step 08 should record this dependency in the landscape.
- **The source directory on the workstation contains far more than the 5 target files.** `c:\Users\tvolo\dev\ai-dala\aiqadam\portal-content\20260819\` holds 15 files plus a `pdf/` subdirectory (PDFs, PPTXs, an HTML, a `.doc`, and a `factsheet` `.docx` that is *not* one of the 5). A blanket `scp -r` would push ~13 MB of unrelated binaries onto the host. The designer should copy the **5 named files only**. Their exact names, as the seed script expects them: `AI Qadam Manifesto.docx`, `AI Qadam Charter v0 1.docx`, `AI_Qadam_Kazakhstan_MoU-2105 (3).docx`, `AI Qadam Global Board Положение (2).docx`, `AI Qadam Soglashenie v1 (2).docx`.
- **Three of the five filenames are hostile to shell quoting** — they contain spaces, parentheses, and (in one case) Cyrillic characters. Copying from a Windows workstation to a Linux host adds a filename-encoding dimension. If a filename lands even slightly wrong on the host, the seed script's non-fatal skip behavior means the run reports success while silently uploading nothing. The designer must specify exact quoting and a post-copy verification that asserts **5 files with byte-exact expected names**, not merely "the directory exists."
- **Two Directus admin tokens exist in `.env` and are currently identical.** Reading the wrong key would still authenticate today, which makes a key-name mistake invisible now and a latent failure after any future desync. The task names `DIRECTUS_ADMIN_TOKEN`; the designer should read exactly that key.
- **A `.env` backup containing the OLD secret values sits on-host** (`deploy/.env.pre-T0137.<timestamp>.bak`). Any glob-based or directory-wide inspection of `deploy/` risks surfacing it. Combined with the standing no-`grep -B/-A`-with-`-v` rule, the designer should specify precise, single-key extraction rather than any pattern-scan of the `deploy/` directory.
- **The landscape has no Directus rollback precedent.** `bootstrap.sh` is additive by design, and T-0136 proved it idempotent across ~77 collections on this instance — but "additive and idempotent" is a property of the script, not a rollback plan. The designer must author the rollback from scratch (most plausibly: drop the `source_file` field + relation, delete the `public-documents` folder and its uploaded files, revoke the anonymous grant), and should note that reverting the *grant* is the security-relevant half.

## Open questions

- **Is Directus publicly reachable at all, and at what origin?** This is the one gap that could turn into a scope change rather than a discovery item. AC-4 and AC-6 both require an *anonymous, external* HTTP 200 on `/assets/<id>`. The landscape records exactly one QA vhost, pointing at the api on `3113`. If no nginx vhost fronts Directus, an anonymous external request cannot reach it, and satisfying those two ACs would require an nginx change that T-0141 does not scope, did not blast-radius, and whose `affects:` list does not anticipate. The designer must resolve this **before** planning execution, and escalate as a scope question if no public route exists.
- **Should `shared/app-registry.md`'s stale AiQadam QA row be corrected as part of this run, or filed separately?** It contradicts the host file on Directus's very existence in this environment. T-0141's `affects:` names only `landscape/hosts/pro-data-tech-qa.md`, which argues for a separate follow-up task — but leaving a registry that the `deploy-app` workflow treats as authoritative asserting "Directus is not deployed here" is a trap for the next run.
