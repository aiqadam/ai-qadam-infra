---
run_id: 2026-08-17-prepare-letflow-host-001
step: 03
agent: task-validator
verdict: PASS
created: 2026-08-17T04:20:00Z
task_id: T-0134-prepare-ubuntu-16gb-nbg1-1-for-letflow
inputs_read:
  - runs/2026-08-17-prepare-letflow-host-001/step-01-task-reader.md
  - runs/2026-08-17-prepare-letflow-host-001/step-02-landscape-reader.md
  - workflows/infrastructure.md
  - tasks/T-0134-prepare-ubuntu-16gb-nbg1-1-for-letflow.md
  - shared/handoff-format.md
  - shared/verdicts.md
artifacts_changed: []
next_step_hint: solution-designer (step 04) should explicitly state, with rationale, its decisions on (a) sshd-hardening depth — literal two-directive reading vs. the fuller sshusers/AllowGroups/cipher-hardening pattern from T-0093/T-0102, (b) whether the DOCKER-USER/MASQUERADE after.rules append is needed given DEFAULT_FORWARD_POLICY is already ACCEPT (resolve via live inspection, not the 51-day-old snapshot), (c) the 80/443 Hetzner firewall source CIDR (0.0.0.0/0 + ::/0 assumed — state this explicitly), and (d) what, if anything, is written to secrets-inventory.md (most likely a documentation-only entry for the already-referenced ai-dala-infra management key, or an explicit no-op) — none of these require user input, all are within step 04's normal design mandate. Also plan step 07's "externally observable" firewall verification as a TCP-level reachability probe (SYN/connect success vs. filtered), not an HTTP probe, since no service will be listening on 80/443 after this task (app deploy is out of scope).
---

## Summary
Validated: T-0134 passes all six checks — the task names a concrete, verifiable end state, the infrastructure workflow is the right fit, none of the target state is already in place, nothing in the change contradicts an explicit landscape fact, no critical unknown blocks design (the gaps step 02 flagged are legitimate design decisions or live-discovery items, not missing facts), and the workflow's idempotency/backup/dual-verification rules are all satisfiable for this task. Verdict: PASS.

## Details
### Validation results

1. **Well-formed: PASS** — "What done looks like" lists five checklist items, each a concrete, checkable end state: Docker Engine + Compose plugin installed; Hetzner Cloud Firewall allows inbound 80/443; sshd hardened with two named directives; `role:` frontmatter updated; a `services.md` section added for the host. All five are independently verifiable by an executor/validator without further interpretation. The one soft spot — "matching T-0093/T-0102's precedent" language alongside only two named directives, when the actual precedent shipped ~10 directives plus an `AllowGroups sshusers` group-creation step — is a tension between contextual framing and the literal acceptance criteria, not a defect in the criteria themselves; the literal checklist remains the authoritative "done" definition and is unambiguous on its own. Handled under check 5 below as a design decision, not a well-formedness failure.

2. **In-scope: PASS** — The infrastructure workflow's "When this workflow applies" section explicitly lists Docker/Compose changes, OS package install, and firewall rules — all three are exactly what T-0134 requires. One notable wrinkle: the workflow file's own bullet reads "Any change to a managed host (currently: `landscape/hosts/hetzner-prod.md`)," which reads as if only `hetzner-prod` is in scope. This is stale documentation, not a real restriction — precedent tasks T-0093 (`pro-data-tech-qa`) and T-0102 (`pro-data-tech-prod`) already used this identical workflow against hosts other than `hetzner-prod`, and this repo's landscape (per step 02) manages a multi-host fleet. Flagged in Issues/risks as a doc-freshness note for a future landscape/workflow cleanup task, not a blocker to this run.

3. **Not already done: PASS** — Per step 02's landscape summary: `role:` is still `unassigned`; Docker/Compose/nginx are all not installed; the Hetzner Cloud Firewall has no 80/443 rule; sshd is still at cloud-init defaults (`PasswordAuthentication yes`, `PermitRootLogin yes`); `services.md` has no "Docker present" entry for this host. Every target end state in the checklist is currently unmet.

4. **No conflict with current state: PASS** — The landscape file's own "Open questions" section states role assignment is the explicit gate blocking UFW/sshd/deployment work on this host — T-0134 is that gating decision being exercised, not a violation of it. No other explicit landscape fact contradicts the change: `DEFAULT_FORWARD_POLICY="ACCEPT"` was pre-set "for Docker parity," anticipating exactly this kind of task; server protection flags (`delete`/`rebuild`) are unrelated to package/firewall/sshd config and unaffected; the project's local-disk-only backup policy is compatible with workflow rule 2 (a local config backup before sshd/UFW changes, no Hetzner snapshot needed).

5. **Discoverable scope: PASS** — Reviewing each of step 02's five flagged gaps on its own merits rather than deferring to step 02's framing:
   - **sshd-hardening pattern depth** (literal 2-directive vs. full T-0093/T-0102 pattern with `sshusers`/`AllowGroups`/cipher hardening): This is a genuine design choice, but it is resolvable by the designer using documented precedent and stated rationale — it does not require user input. Either choice is defensible (literal criteria satisfy the letter of "done"; full pattern satisfies "parity... matching precedent" from the Why section) and step 04 is positioned to pick one and justify it. Not a blocker.
   - **UFW/Docker `after.rules` interaction**: Explicitly a live-discovery item — the designer's plan should include a step to inspect current `iptables`/`after.rules` state before deciding whether the DOCKER-USER/MASQUERADE append (needed on `pro-data-tech-prod`) is also needed here, rather than assuming from the stale snapshot. This is exactly the kind of item check 5 anticipates being "flagged for live discovery" rather than resolved now. Not a blocker.
   - **Hetzner firewall 80/443 source CIDR**: Not stated verbatim in the task, but the inference to `0.0.0.0/0`/`::/0` is low-risk and well-supported (the host will carry public Letflow traffic per the Why section, and the existing UFW rule is already world-open on the same ports). The designer should state this assumption explicitly rather than leave it implicit, but it does not need to be kicked back to the user — it's a standard, unsurprising default for a public web host firewall rule, and NEEDS_APPROVAL at step 05 (this change is firewall-rule-change, on the "always requires approval" list) gives the user a natural checkpoint to object if they intended a narrower CIDR.
   - **Unexplained `secrets-inventory.md` `affects:` entry**: No acceptance criterion names a secret, and no existing entry for this host exists in that file today. This is most plausibly explained by step 02's own reading: the host's landscape file already claims its SSH public key lives in `secrets-inventory.md`, but no such entry currently exists — this task, which formally assigns the host's role, is a reasonable place to add that documentation entry (inventory only, no secret values, consistent with the workflow's `secrets-inventory.md` handling rule). This is designable as either "add a documentation-only entry for the existing `ai-dala-infra` key" or an explicit no-op with a stated reason — either resolves it without user input. Not a blocker, but the designer must not silently ignore it; it should be addressed one way or the other in the plan.
   - **51-day-stale host landscape file**: This affects confidence in specific facts (apt posture, effective sshd config, firewall rule set), not the availability of facts needed to design a plan. The landscape provides enough structure to design against (known baseline, known precedent, known target state), and workflow rule 1 (idempotency) plus standard operating practice already call for the executor to re-verify live state as a precondition. This is a live-reconfirmation task for step 04/06 to build in, not a missing-fact blocker.

   None of the five gaps constitute a critical unknown that makes the task undesignable, and none require user input — all are either ordinary design choices with defensible defaults, or explicitly deferred to live discovery during planning/execution, consistent with how this workflow is meant to operate.

6. **Workflow-specific rules respected: PASS**
   - *Idempotency (rule 1)*: Satisfiable — package install, firewall-rule-add, and sshd drop-in file writes are all naturally idempotent operations if the designer specifies presence/content checks (e.g., "install Docker only if absent," "add firewall rule only if not already present," "write drop-in with fixed target content") rather than blind appends.
   - *Backup before destructive changes (rule 2)*: Satisfiable — sshd config changes via new drop-in files under `/etc/ssh/sshd_config.d/` are additive, not overwrites, but the designer/executor can still capture the current `sshd -T` output and any UFW/`after.rules` state as a pre-change snapshot, satisfying the spirit of the rule even where no existing file is being destructively overwritten.
   - *Verify in two places (rule 3)*: Satisfiable, with one nuance worth flagging for step 04/07: since app deployment is out of scope, nothing will be listening on 80/443 after this task completes, so the "externally observable behavior" check for the firewall change cannot be an HTTP probe — it should be a TCP-level reachability check (connection reaches the host and is refused/reset by the OS, vs. being silently dropped/filtered by the firewall) to distinguish "firewall now permits" from "nothing is listening yet," the latter being expected and correct at this task's boundary. For sshd hardening, dual verification is straightforward (host-side `sshd -T`; external — a live SSH connection attempt confirming key-auth still works and password-auth is now rejected).

## Issues / risks
- The infrastructure workflow file's "When this workflow applies" bullet still names `landscape/hosts/hetzner-prod.md` as the parenthetical "currently" example host, which is stale relative to this repo's actual multi-host fleet (already evidenced by T-0093/T-0102 precedent on other hosts). Does not block this run; worth a documentation-cleanup task separately.
- The task's Notes section already flags Docker install, the firewall rule change, and sshd hardening as likely `NEEDS_APPROVAL` per `shared/approval-protocol.md` — consistent with this validation; step 04 should expect to produce a `NEEDS_APPROVAL` (not auto-approved `PASS`) handoff given medium blast radius and these being on the "always requires approval" list.
- `secrets-inventory.md`'s role in this task remains a judgment call for step 04 to make explicit (documentation entry vs. no-op) rather than something resolvable from the landscape alone — flagged, not blocking.
- The 51-day-old host landscape snapshot means step 04's plan should build in live-state reconfirmation (sshd effective config, Hetzner firewall rule list, UFW/`after.rules` state) as an early step rather than trusting the snapshot for exact current values.

## Open questions (optional)
none — verdict is PASS, not BLOCKED.
