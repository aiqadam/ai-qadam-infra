---
run_id: 2026-08-17-prepare-letflow-host-001
step: 02
agent: landscape-reader
verdict: PASS
created: 2026-08-17T03:05:00Z
task_id: T-0134-prepare-ubuntu-16gb-nbg1-1-for-letflow
inputs_read:
  - runs/2026-08-17-prepare-letflow-host-001/step-01-task-reader.md
  - landscape/README.md
  - workflows/infrastructure.md
  - landscape/hosts/ubuntu-16gb-nbg1-1.md
  - landscape/services.md
  - landscape/secrets-inventory.md
  - tasks/T-0134-prepare-ubuntu-16gb-nbg1-1-for-letflow.md
  - tasks/T-0093-harden-sshd-on-pro-data-tech-qa.md
  - tasks/T-0102-harden-sshd-on-pro-data-tech-prod.md
  - shared/handoff-format.md
  - shared/verdicts.md
artifacts_changed: []
next_step_hint: task-validator (step 03) and solution-designer (step 04) should treat the sshusers/AllowGroups question and the Docker-UFW-coexistence question below as the two live design decisions this run must resolve; secrets-inventory.md's role in this task remains unconfirmed and should be settled explicitly rather than defaulted.
---

## Summary
`ubuntu-16gb-nbg1-1` (46.225.239.60, Hetzner project "ai-qadam", CX43) is a clean, network-hardened-but-application-empty Ubuntu 26.04 host: Hetzner Cloud Firewall and host UFW both already allow only TCP 22 from the management workstation IP (`178.89.57.135/32`) at the cloud layer, UFW additionally pre-allows 80/443 locally and already carries `DEFAULT_FORWARD_POLICY="ACCEPT"` in anticipation of Docker, and fail2ban's sshd jail is active — but Docker, Compose, and nginx are all **not installed**, no Compose projects exist, and sshd is still at cloud-init defaults (`PasswordAuthentication yes`, `PermitRootLogin yes`, no `AllowGroups` filter, no `sshusers` group, single operator user `tvolodi`). `role:` is still `unassigned` and the landscape file's own "Open questions" section explicitly gates UFW/sshd/deployment decisions on this role assignment — which is exactly what T-0134 now resolves. The landscape file was last verified 2026-06-27 (51 days ago, stale by this role's 30-day threshold); `services.md`'s entry for this host is more recent (2026-07-27) and corroborates the same not-installed state. No secrets-inventory.md entry exists yet for this host despite the host file's claim that its SSH public key lives there.

## Details
### Relevant facts (sourced from landscape)
- Host identity: `46.225.239.60`, Hetzner server id `145542849`, project id `15130993` ("ai-qadam"), CX43 (8 vCPU / 16 GiB RAM / 150 GiB disk, 143 GiB free), Ubuntu 26.04 "Resolute Raccoon", kernel `7.0.0-22-generic`. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`_
- `role: unassigned` in frontmatter; "Open questions" explicitly states role assignment is the gate for UFW/sshd/deployment work — this task is that decision. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`_
- Hetzner Cloud Firewall `ai-qadam-mgmt-ssh` (id `11204449`) is applied to this server with a single inbound rule: TCP 22 from `178.89.57.135/32` only; outbound is Hetzner default allow-all. No 80/443 rule exists yet — this is the firewall change T-0134 requires. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`, "Hetzner Cloud Firewall" section_
- Host UFW is already active: deny-inbound default, allow 22/80/443 (v4+v6), `DEFAULT_FORWARD_POLICY="ACCEPT"` — explicitly set "for Docker parity with hetzner-prod" even though Docker isn't installed yet, so **no UFW rule change should be needed for Docker itself**, unlike the DOCKER-USER/MASQUERADE after.rules append that pro-data-tech-prod needed (T-0106). — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`, "Network" section_
- sshd is at cloud-init defaults: `PermitRootLogin yes`, `PasswordAuthentication yes` (via drop-in `50-cloud-init.conf`), `PubkeyAuthentication yes`, `MaxAuthTries 6`, `LoginGraceTime 120`, `X11Forwarding yes`, `ClientAliveInterval 0`, `UseDNS no`. No `AllowUsers`/`AllowGroups`. No project-managed drop-ins installed. This is "deferred item #4" in the host's "What needs to happen" list. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`, "Access" and "What needs to happen" §4_
- Only local users are `root`, `nobody`, `tvolodi` (uid 1000, sudo group, passwordless sudo via `/etc/sudoers.d/90-tvolodi`). No `sshusers` group exists on this host (unlike `pro-data-tech-qa`/`pro-data-tech-prod`, which both have a `sshusers` group with `root` as a transitional member plus multiple named operators). Root cannot currently SSH in at all (`/root/.ssh/` empty), so `PermitRootLogin prohibit-password` vs `yes` has no practical effect today, but is still the stated acceptance criterion. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`, "Access" section_
- fail2ban 1.1.0-9 already installed and active (T-0084, 2026-06-27); sshd jail maxretry=3/bantime=600s/findtime=600s; `ignoreip` includes the management workstation IP `178.89.57.135`. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`, `landscape/services.md`_
- Docker: **not installed**. nginx: **not installed**. No Compose projects on disk, no docker-proxy ports bound. Only stock cloud-image systemd units run (ssh, chrony, unattended-upgrades, qemu-guest-agent, cloud-init stages, snapd, apparmor, systemd-resolved, ufw, fail2ban). No crontabs, no `app-backup.timer`, no certbot timer. — _source: `landscape/services.md`, `## ubuntu-16gb-nbg1-1` section_
- Backups: no Hetzner snapshot option (project-wide policy: local-disk-only backups, no paid Hetzner add-ons); no application-level backup tooling yet (none needed — no app data exists). — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`, `landscape/README.md`_
- Server protection flags already enabled: `protection.delete=true`, `protection.rebuild=true`. — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`_
- apt posture (as of 2026-06-27): 13 pending upgrades (normal for a fresh image), unattended-upgrades active, `-updates` channel not subscribed (security/ESM only). — _source: `landscape/hosts/ubuntu-16gb-nbg1-1.md`_
- **sshd-hardening precedent (T-0093 on `pro-data-tech-qa`, T-0102 on `pro-data-tech-prod`, both `done`):** both installed two drop-ins under `/etc/ssh/sshd_config.d/` — `40-disable-password.conf` (`PasswordAuthentication no`, `KbdInteractiveAuthentication no`) and `40-ai-dala-infra.conf` (`PermitRootLogin prohibit-password`, `MaxAuthTries 3`, `LoginGraceTime 30`, `X11Forwarding no`, `ClientAliveInterval 300`, `ClientAliveCountMax 2`, `AllowGroups sshusers`, hardened KexAlgorithms/Ciphers/MACs dropping SHA-1/CBC/3DES/RC4) — both required creating a `sshusers` group with `root` as a transitional sole member *before* setting `AllowGroups sshusers`, to avoid lockout. Both validated with `sshd -t` before reload, verified session survival, and preserved the original provider/break-glass key. — _source: `tasks/T-0093-harden-sshd-on-pro-data-tech-qa.md`, `tasks/T-0102-harden-sshd-on-pro-data-tech-prod.md`_
- **T-0134's literal acceptance criteria name only two directives** (`PasswordAuthentication no`, `PermitRootLogin prohibit-password`) — narrower than the full ~10-directive drop-in T-0093/T-0102 actually shipped, and narrower than the `AllowGroups sshusers` + group-creation step those precedents required. — _source: `tasks/T-0134-prepare-ubuntu-16gb-nbg1-1-for-letflow.md`, "What done looks like"_
- Docker-install precedent (`pro-data-tech-prod`, T-0106): Docker CE 29.6.1 + Compose plugin v5.3.1 from the official Docker apt repo (keyring method), operator user(s) added to the `docker` group, and a UFW `after.rules` DOCKER-USER/MASQUERADE coexistence block appended — the latter was needed there because that host's UFW forward policy had to be flipped; `ubuntu-16gb-nbg1-1` already has `DEFAULT_FORWARD_POLICY="ACCEPT"` pre-set, so this append step may not be required here (design decision, not yet confirmed). — _source: `landscape/services.md`, `## pro-data-tech-prod` → Docker section_
- Naming convention for Compose deployments on other hosts: project directory under `/opt/apps/<app>/`, Compose file at `deploy/docker-compose.<env>.yml`, env file at `deploy/.env` mode 600, per-app entry added to `landscape/services.md` "Running Compose projects" table — useful precedent for the not-yet-created follow-on letflow deploy task, though app deployment itself is out of scope for T-0134. — _source: `landscape/services.md`, `## pro-data-tech-qa` / `## pro-data-tech-prod` sections_
- `secrets-inventory.md` currently has **no section at all** for `ubuntu-16gb-nbg1-1` or for the shared management SSH key (`ai-dala-infra`) — even though the host's own landscape file states "Full public key in `../secrets-inventory.md`". No acceptance criterion in T-0134 names a new secret. — _source: `landscape/secrets-inventory.md`, `landscape/hosts/ubuntu-16gb-nbg1-1.md` line "SSH key (management workstation)"_

### Stale or stub files encountered
- `landscape/hosts/ubuntu-16gb-nbg1-1.md` — `last_verified: 2026-06-27`, `status: populated`. 51 days old as of today (2026-08-17) — **stale** by this role's 30-day threshold. Not a stub (content is fully populated), but given this run will make live changes (Docker install, firewall rule, sshd hardening), the ~7-week-old snapshot of apt posture, effective sshd config, and firewall rules should be reconfirmed live before/while the plan executes rather than assumed unchanged.
- `landscape/secrets-inventory.md` — has **no frontmatter and no `last_verified` field at all** (unlike every other landscape file), so its freshness cannot be assessed by date. It is git-ignored per project convention (operators maintain it locally), which may explain the missing frontmatter, but it means this role's staleness check cannot apply to it — flagged as a format gap rather than a content gap.
- `landscape/services.md` — `last_verified: 2026-07-27`, `status: populated`. 21 days old — within the 30-day threshold, not stale.

### Gaps requiring live discovery
- **sshd hardening scope decision:** whether to replicate the full `sshusers`-group + `AllowGroups` + hardened-KEX/Cipher/MAC pattern used on `pro-data-tech-qa`/`pro-data-tech-prod` (T-0093/T-0102), or apply only the two directives T-0134 literally names (`PasswordAuthentication no`, `PermitRootLogin prohibit-password`). The task's Notes call this "matching T-0093/T-0102's precedent," which argues for the fuller pattern, but this host currently has no `sshusers` group and only one operator user (`tvolodi`) — the designer must decide and state the rationale; this isn't resolvable from the landscape alone.
- **Docker/UFW coexistence:** whether the DOCKER-USER + MASQUERADE `after.rules` append that `pro-data-tech-prod` needed (T-0106) is also needed on `ubuntu-16gb-nbg1-1`, given this host's UFW already has `DEFAULT_FORWARD_POLICY="ACCEPT"` pre-set. Requires either live inspection of current `iptables`/`after.rules` state or a decision to add the block defensively regardless.
- **Live reconfirmation of current state before changing it:** given the 51-day-old landscape snapshot, the executor (or designer's plan) should re-probe current sshd effective config, current Hetzner Cloud Firewall rule set, and current UFW state at the start of execution rather than assume the 2026-06-27 snapshot is still 100% accurate — standard idempotency practice per `workflows/infrastructure.md` rule 1, but worth calling out explicitly given the staleness.
- **Hetzner Cloud Firewall 80/443 source scope:** T-0134 says "allow inbound TCP 80/443" but does not state a source CIDR. Since this will eventually carry public Letflow application traffic, the implied intent is `0.0.0.0/0` (and the equivalent `::/0` for IPv6), by analogy with the existing UFW rule already being world-open on 80/443 — but this is an inference, not a landscape fact, and the designer should confirm/state it explicitly rather than assume silently.
- **Why `landscape/secrets-inventory.md` is in scope for T-0134:** no acceptance criterion names a new secret, and no existing entry for this host's SSH key or for Docker exists in that file today (see gap above). The designer/executor should determine live whether this task actually needs to add anything there (e.g., documenting the existing `ai-dala-infra` management key now that the host has a role, or a Docker registry credential if a private registry is used) or whether it's a no-op for this run and the `affects:` entry is simply anticipatory/inaccurate.
- **`letflow-queue` service context:** the task's "Why" references an already-deployed `letflow-queue` service at `queue-test.ai-dala.com`, but no landscape file in this repo's scope documents it — it most likely lives in the sibling `ai-dala-infra` repo (this repo split off from it per `landscape/README.md`'s bootstrap note) and is out of this run's landscape entirely. Not a blocker for T-0134 (host-level prep only), but downstream steps should not assume facts about it are available here.

## Issues / risks
- `landscape/hosts/ubuntu-16gb-nbg1-1.md` is 51 days stale against a 30-day threshold; this run will make destructive/security-relevant changes (Docker install, firewall rule, sshd hardening) based partly on that snapshot. Recommend the executor re-verify live state (sshd -T, Hetzner firewall rule list, UFW status) as a precondition step rather than trusting the snapshot outright — consistent with `workflows/infrastructure.md`'s idempotency rule but worth surfacing explicitly given the age.
- `secrets-inventory.md` has no `last_verified`/frontmatter at all, so this role cannot apply its normal staleness check to it; combined with the fact that it has no entry at all for this host, its relevance to T-0134 is genuinely unclear (see gap above) rather than merely stale.
- This host currently lacks the `sshusers` group / multi-operator pattern used elsewhere in the fleet; a literal reading of T-0134's two-directive acceptance criteria (without `AllowGroups`) would leave it structurally different from `pro-data-tech-qa`/`pro-data-tech-prod`, which may or may not be acceptable — flagged for the designer, not resolved here per this role's "do not form a plan" constraint.
- None of the above rise to the level of blocking design — the landscape is populated (not stub), the host is in a known clean state (no existing services to regress), and the gaps are scoping/precedent decisions the solution-designer is positioned to make, not missing facts that make the task undesignable.
