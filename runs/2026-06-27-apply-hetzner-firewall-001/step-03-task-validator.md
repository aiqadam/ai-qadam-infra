---
run_id: 2026-06-27-apply-hetzner-firewall-001
step: "03"
agent: task-validator
verdict: PASS
created: 2026-06-27T00:00:00Z
task_id: T-0086-apply-hetzner-cloud-firewall-for-ubuntu-16gb-nbg1-1
inputs_read:
  - runs/2026-06-27-apply-hetzner-firewall-001/step-01-task-reader.md
  - runs/2026-06-27-apply-hetzner-firewall-001/step-02-landscape-reader.md
  - tasks/T-0086-apply-hetzner-cloud-firewall-for-ubuntu-16gb-nbg1-1.md
  - tasks/.promotions/T-0086-promotion-2026-06-27.md
  - shared/handoff-format.md
  - shared/verdicts.md
  - shared/approval-protocol.md
  - workflows/infrastructure.md
  - runs/2026-06-27-install-fail2ban-001/step-03-task-validator.md
  - runs/2026-05-13-install-rustdesk-server-001/step-03-task-validator.md
  - tasks/_index.md
artifacts_changed: []
next_step_hint: solution-designer (step 04) — design the Hetzner Cloud Firewall create+apply body and the protection-flag call. The step-04 verdict MUST be `NEEDS_APPROVAL` (not `PASS`) because firewall rule changes are explicitly enumerated under `shared/approval-protocol.md` §"Always requires `NEEDS_APPROVAL`" regardless of blast/reversibility ratings. Lock in: (1) firewall name (default `ai-qadam-mgmt-ssh`, user may rename at approval), (2) protection-flag call shape — combined `{delete: true, rebuild: true}` call + two GET verifications (matches Hetzner API convention; fewer round-trips than two separate POST calls), (3) IPv4-only inbound scope with documented "IPv6 remains unrestricted at cloud layer" assumption, (4) Hetzner default allow-all outbound (no explicit `rules.outbound` array), (5) create+apply separation: `POST /v1/firewalls` with rules (no `applied_to`) followed by `POST /v1/firewalls/{id}/actions/apply_to_resources` once the create response is verified — this decouples the create from the apply so the executor can sanity-check the JSON before binding to the server. Surface the pre-flight `api.ipify.org` re-verification and the same-day `GET /v1/firewalls?project_id=15130993` re-check as hard executor gates (not designer decisions).
---

## Summary

Task T-0086 passes all six validation checks: it is well-formed with concrete, verifiable acceptance criteria; the `infrastructure` workflow is the correct fit and its 8-step shape binds cleanly; the task is not redundant with prior work and has no conflict with the current landscape state; all facts required to design a solution are present in landscape plus the audit follow-up run; and every workflow-specific rule is satisfiable. The task is ready for solution design. Per `shared/approval-protocol.md` §"Always requires `NEEDS_APPROVAL`", the firewall change at step 04 must emit `verdict: NEEDS_APPROVAL` (not auto-approve) regardless of `estimated_blast_radius: medium` / `estimated_reversibility: full` — the orchestrator should expect a step-05 user-approval file before the executor may mutate Hetzner state.

## Details

### Validation results

1. **Well-formed: PASS** — The task names a concrete, verifiable end state across 11 acceptance criteria: a specific firewall named `ai-qadam-mgmt-ssh` (or user-discretion name) in Hetzner project `15130993`, an inbound rule `TCP 22 from 178.89.57.135/32` (explicit, NOT `0.0.0.0/0`), no other inbound rules, applied to server `145542849`, two server protection flags enabled (`delete=True`, `rebuild=True`), a live `Test-NetConnection 46.225.239.60 -Port 22` post-apply probe returning True, landscape updates to `landscape/hosts/ubuntu-16gb-nbg1-1.md` "Hetzner Cloud Firewall" section and a confirm-only touch of `landscape/services.md`, plus `tasks/_index.md` re-sort (already satisfied by promotion). Every criterion is checkable with a specific command, file diff, or API call. The acceptance criteria are more concrete than the typical "make security better" — they specify the exact CIDR, the exact port, the exact flag values, the exact API probe. No vague intent. Frontmatter is complete and consistent: `kind: task`, `status: in-progress` (valid for execution per the task-reader contract), `workflow: infrastructure`, `priority: P1`, `estimated_blast_radius: medium`, `estimated_reversibility: full`, `created_by: 2026-06-27-audit-hetzner-firewall-001`, `executed_by_runs: [2026-06-27-apply-hetzner-firewall-001]`, `affects: [landscape/hosts/ubuntu-16gb-nbg1-1.md, landscape/services.md]`, `related: [T-0085-...]`, `blocks: []`, `blocked_by: []`. History entries are present and consistent (`created` → `promoted` → `status → in-progress`).

2. **In-scope: PASS** — `workflows/infrastructure.md` §"When this workflow applies" explicitly enumerates **"firewall rules"** as a trigger. Adding a Hetzner Cloud Firewall to a managed host (`ubuntu-16gb-nbg1-1`, listed in `landscape/hosts/`) is squarely within scope. The state-changing nature (Hetzner Cloud API mutations: `POST /v1/firewalls`, `POST /v1/firewalls/{id}/actions/apply_to_resources`, `POST /v1/servers/{id}/actions/change_protection`) means the workflow is correctly classified as state-changing and the human approval gate applies. Step bindings (01 task-reader → 02 landscape-reader → 03 task-validator → 04 solution-designer → 05 user-approval → 06 executor-infra → 07 execution-validator → 08 landscape-updater) apply cleanly to this run as evidenced by the existing step-01 and step-02 handoffs.

3. **Not already done: PASS** — Per the landscape summary in step-02 (`landscape/hosts/ubuntu-16gb-nbg1-1.md` "Hetzner Cloud Firewall" section): **project `ai-qadam` (`15130993`) contains zero Hetzner Cloud Firewalls**, verified same-day (2026-06-27) by the T-0085 audit (run `2026-06-27-audit-hetzner-firewall-001`). The task's `executed_by_runs:` list contains only the current run; no other workflow run has created a firewall in this project. The pattern reference (prod `firewall-1`, id `10145783`) is in a different project (`12287574`) and does not pre-apply to this host. No no-op risk.

4. **No conflict with current state: PASS** — Layering and conflict analysis:
   - **No existing Cloud Firewall applies to `ubuntu-16gb-nbg1-1`** — audit-confirmed zero firewalls in project `15130993`.
   - **No other pending task modifies this host's cloud firewall** — `tasks/_index.md` scan confirms T-0086 is the only row referencing `ubuntu-16gb-nbg1-1`'s cloud firewall or protection flags. Sibling tasks T-0083 (UFW, `done`) and T-0084 (fail2ban, `done`) are host-layer and orthogonal to the cloud-layer firewall. T-0082 (inventory, `in-progress`) is the discovery parent task that T-0086 is a child follow-on of; T-0082 does not block T-0086 and T-0086 does not block T-0082.
   - **No conflict between the proposed firewall rules and host-level state.** UFW allows 22/80/443 (v4+v6) at the host layer. The proposed cloud firewall will allow inbound 22 from `178.89.57.135/32` only. The two layers are non-conflicting and stacked: cloud layer is the outer filter (more restrictive), host layer still applies for traffic that does pass through. UFW's 80/443 allow rules will become effectively dead at the cloud layer (which blocks 80/443 inbound), but that is the intended defense-in-depth posture and does not cause functional conflict — the host has no role yet, so no inbound 80/443 is expected.
   - **No conflict with token scope.** The Hetzner token `hetzner-api-token:ai-dala-infra:ai-qadam-read-write` is project-scoped to `15130993` only. This run only touches resources in `15130993` (firewall create + apply) and on `145542849` (server is in `15130993`; protection-flag call is project-agnostic via `/v1/servers/{id}`). Token cannot touch project `12287574` (prod) — and prod is not being touched. No cross-project bleed.
   - **No conflict with other concurrent runs against this host.** Other 2026-06-27 runs against this host (`discovery-host-001`, `configure-ufw-001`, `install-fail2ban-001`, `audit-hetzner-firewall-001`) are all `done` per the index; none is in-progress in parallel with this run. The `runs/` directory confirms no concurrent step files exist for this host. No interleaving risk.

5. **Discoverable scope: PASS** — All landscape facts required to design a solution either exist or are precisely characterized as live-discovery gates owned by downstream steps. Specifically:
   - **Server identity, project, IP, OS** — all in `landscape/hosts/ubuntu-16gb-nbg1-1.md` (fresh, today).
   - **Token metadata + scope** — in `landscape/secrets-inventory.md`; fingerprint verified active 2026-06-27.
   - **Management workstation context** (outbound IP `178.89.57.135/32`, SSH config alias, identity file) — in landscape; IP last live-verified 2026-06-27 via `api.ipify.org` during T-0084.
   - **Pattern reference** (prod `firewall-1` id `10145783`, project `12287574`, server `112603990`) — in `landscape/hosts/hetzner-prod.md` "Network" section.
   - **Hetzner API endpoint documentation** — the three endpoints (`POST /v1/firewalls`, `POST /v1/firewalls/{id}/actions/apply_to_resources`, `POST /v1/servers/{id}/actions/change_protection`) are part of the published Hetzner Cloud API and are documented in Hetzner's public API reference; no live schema discovery needed at design time beyond reading the docs once.
   - **Live-discovery gaps owned by downstream steps** (none blocking):
     - Step 04 (solution-designer) must consult Hetzner Cloud API docs once to lock in the exact JSON body shape for `applied_to[]` (`{type: "server", server: {id: <id>}}`), the protection-flag call body shape (`{delete: true, rebuild: true}` — single call acceptable per Hetzner API), and the `description` field optionality. All three are documented in Hetzner's public API reference; no real discovery risk.
     - Step 06 (executor-infra) must perform three hard pre-flight checks immediately before any POST: (a) `GET /v1/firewalls?project_id=15130993` to confirm still zero firewalls in project (defense-in-depth, same-day re-check of the audit), (b) `curl https://api.ipify.org` to re-verify management-workstation outbound IP is still `178.89.57.135` (ISP rotation risk), (c) record both results + timestamps in handoff. Hard abort on discrepancy.
   - **No critical unknowns remain.** The remaining "open questions" (firewall name finality, protection-flag call shape, IPv6 scope, outbound policy, create+apply separation pattern) are all **design decisions** for step 04 to lock in explicitly — they are not blocking gaps and the default resolutions are recommended in step-02's "Open questions" section.

6. **Workflow-specific rules respected: PASS** — Per `workflows/infrastructure.md`:
   - **Idempotency required.** Step 04 must design the executor's plan to be safe to re-run. Recommended pattern (per step-02): create + apply as separate API calls, so a re-run after partial failure can either (a) delete the orphaned firewall first via `DELETE /v1/firewalls/{id}` then recreate, or (b) detect the existing firewall via `GET /v1/firewalls/{id}` and re-apply rules. The executor's runbook must include an explicit pre-POST "check current state, abort or continue" decision branch. The acceptance criteria note explicitly: "running this task twice would create two firewalls" — step 04 should design a pre-flight that checks for an existing firewall named `ai-qadam-mgmt-ssh` (or whatever name is committed) and either reuses it or halts for user decision. Documented for step-04.
   - **Backup before destructive changes.** This run **creates** a new firewall; it does not modify or delete an existing one. The only destructive operations are (a) deleting the orphan firewall if a re-run finds one (idempotency case), and (b) reverting the protection flags if the user requests rollback after apply. Both are documented in step-04's design as guard-rail operations, not as primary deliverables. The existing `landscape/hosts/ubuntu-16gb-nbg1-1.md` itself captures the pre-apply state implicitly (via `last_verified: 2026-06-27` and the existing "Hetzner Cloud Firewall: NONE applied" text), satisfying the workflow rule's intent.
   - **Verify in two places.** Step 04's design must include: (a) **host-side verification** via `GET /v1/firewalls/{id}` and `GET /v1/servers/145542849` to confirm the API mutations landed, and (b) **externally-observable verification** via `Test-NetConnection 46.225.239.60 -Port 22` from the management workstation (must return True after apply). Both verifications are listed in the acceptance criteria; step-04's design must thread them into the executor's runbook with explicit success criteria and a negative-space test note (a probe from a non-management IP cannot be performed without a second external source, but the rule-set itself encodes the negative expectation).

### Open question resolutions for step 04 (carried from the task brief)

The task brief explicitly requested the validator lock these resolutions. The validator confirms each recommendation is sound:

1. **Firewall name:** `ai-qadam-mgmt-ssh` is a sensible default (encodes project + purpose + protocol scope). User may override at the approval step. Step 04 must commit a name in the design handoff and surface it in the approval gate verbatim. **RECOMMENDED:** keep default unless user says otherwise.

2. **Protection flag calls:** Hetzner `POST /v1/servers/{id}/actions/change_protection` accepts both flags in one call body (`{delete: true, rebuild: true}`). This matches the Hetzner API convention, halves the number of state-mutating POST calls, and makes the "verify each flag post-set" wording of the acceptance criteria satisfiable via a single `GET /v1/servers/145542849` whose response shows both `protection.delete` and `protection.rebuild` as `true`. **RECOMMENDED:** combined call + single GET verification (the GET response confirms both flags at once).

3. **Outbound rules:** Hetzner default is allow-all. No explicit `rules.outbound` array needed in the create body — Hetzner will default to allow-all whether the field is omitted or set to `[].` **RECOMMENDED:** omit `rules.outbound` from the create body (matches task acceptance criteria "Hetzner default (allow all) — no customization needed").

4. **IPv6 scope:** Management workstation outbound is IPv4-only (`178.89.57.135` is an IPv4 address). The task's specified inbound source is `178.89.57.135/32` (IPv4 CIDR). The audit's "default exposure" language mentions IPv6, but the task is scoped to IPv4 only. IPv6 inbound remains unrestricted at the cloud layer (matching the prod `firewall-1` implicit IPv4-only posture — historical inbound additions on prod are all IPv4 CIDRs). **RECOMMENDED:** IPv4-only firewall; document the explicit "IPv6 inbound remains unrestricted at the cloud layer until a role is assigned and v6 policy is set" assumption in the step-04 design handoff and surface it in the approval gate.

5. **Create + apply separation:** Recommended pattern is `POST /v1/firewalls` with rules (no `applied_to` in the body) → validate the create response → `POST /v1/firewalls/{id}/actions/apply_to_resources` to bind to server `145542849`. This decouples create from apply so the executor can sanity-check the JSON one more time before binding to the host. Lower lockout risk than putting `applied_to` in the create body and rolling forward on any error. The task's "token" line in the T-0086 Notes section references both endpoints (`POST /v1/firewalls` and `POST /v1/firewalls/{id}/actions/apply_to_resources`), suggesting this is the intended pattern. **RECOMMENDED:** separate-action apply.

6. **Pre-flight sequence for step 06 (executor):** Must include, in order: (a) `GET /v1/firewalls?project_id=15130993` to confirm zero firewalls; (b) `curl https://api.ipify.org` to re-verify management-workstation outbound IP; (c) record both results + timestamps in handoff. Hard abort on discrepancy. **NOT a step-04 design decision — a step-06 runbook requirement, but step-04 must surface this as an explicit executor gate.**

7. **Idempotency / re-run safety:** Step 04 should design the executor's runbook to detect an existing firewall named `ai-qadam-mgmt-ssh` (or committed name) before POSTing the create. If one exists, halt and ask the user (do not auto-delete). This is a step-04 design requirement, not a validator blocker.

## Issues / risks

- **Lockout risk (HIGH if mishandled, MEDIUM overall with mitigations in place).** If the SSH allow rule omits `178.89.57.135/32` (typo, IP mismatch after ISP rotation, etc.), the host becomes unreachable from the management workstation. **Mitigations are well-designed:** (a) pre-flight `api.ipify.org` re-verification immediately before POST, with hard-abort on mismatch; (b) Hetzner Cloud Console provides manual rollback (operator can delete the firewall via console); (c) KVM-over-IP console is available as a last-resort fallback; (d) the create+apply separation pattern lets the executor verify the rule set BEFORE binding to the server. The remaining residual risk is acceptable for the value gained (closing cloud-layer exposure).
- **Approval gate is mandatory (not auto-approve).** Per `shared/approval-protocol.md` §"Always requires `NEEDS_APPROVAL`", firewall rule changes **always** require explicit human sign-off. Step 04 MUST emit `verdict: NEEDS_APPROVAL` regardless of the task's `medium/full` blast/reversibility rating. The orchestrator must expect a step-05 file with `verdict: APPROVED` before invoking the executor.
- **Token scope is narrow but sufficient.** Token is `project_id 15130993` read+write. A typo in the project_id or a call to the wrong resource id will return 404/403. Executor must include the `project_id=15130993` filter in every call and verify the firewall id's `project_id` in the create response before issuing the apply action.
- **IPv6 exposure remains.** Under the recommended IPv4-only design, IPv6 inbound remains unrestricted after this run completes. This is an explicit documented decision; the IPv6 gap will close when the host is assigned a role and a deliberate v6 policy is set. **NOT a task-blocker; documented for the user in the approval gate.**
- **Re-run safety (idempotency).** Running this task twice without an idempotency guard would create two firewalls in project `15130993`. Step 04's design must include a pre-POST firewall-exists check; documented as a step-04 design requirement.
- **`PasswordAuthentication yes` on the host (cloud-init default).** This is a separate sshd-hardening task not in T-0086's scope. The cloud-layer firewall allow rule is restrictive enough that password auth is not the dominant risk, but defense-in-depth is incomplete until a sibling task disables password auth on this host. **Informational; not blocking; not in T-0086's scope.**
- **Landscape `last_verified: 2026-06-27` drift for sibling files** — `landscape/hosts/hetzner-prod.md` is 32 days old (just over the 30-day threshold). Pre-existing drift, not blocking for this run (prod is not being modified). Flagged for a future landscape-updater refresh.
- **`gitea:admin-password` row in `landscape/secrets-inventory.md` contains a literal password value** (`eT96ulleryIpd38VJeQNRGm3lQ3qcUO3`). Pre-existing convention violation. Out of scope for this run. Flagged for a future cleanup.
- **Hetzner Cloud API documentation is the single source of truth for body shapes.** Step 04 must read the published Hetzner Cloud API docs once and lock in the exact JSON body shapes. No live schema exploration during execution.

## Open questions

- (For step 04 solution-designer) Firewall name finality — surface `ai-qadam-mgmt-ssh` in the approval gate verbatim; user may override in their APPROVE response.
- (For step 04 solution-designer) Protection-flag call shape — recommend combined `{delete: true, rebuild: true}` call; document the rationale (Hetzner API convention, fewer round-trips) in the design handoff.
- (For step 04 solution-designer) IPv6 inbound scope — recommend IPv4-only firewall with explicit "IPv6 inbound remains unrestricted until role assignment" documentation; surface in approval gate.
- (For step 04 solution-designer) Outbound policy — recommend omitting `rules.outbound` from the create body (Hetzner default allow-all); document explicitly.
- (For step 04 solution-designer) Create + apply separation — recommend the separate-action pattern (create with rules, then apply via `apply_to_resources` action) for lower lockout risk and better verification windows.
- (For step 04 solution-designer) Idempotency / re-run safety — design the executor's pre-POST to detect an existing firewall named `ai-qadam-mgmt-ssh` (or committed name) and halt with a user prompt rather than auto-deleting or creating a duplicate.
- (For step 06 executor-infra) Pre-flight sequence MUST be: (a) `GET /v1/firewalls?project_id=15130993` → confirm zero firewalls (or detect existing and halt); (b) `curl https://api.ipify.org` → re-verify management-workstation outbound IP is `178.89.57.135`; (c) record both results + ISO-8601 timestamps in handoff BEFORE any POST. Hard-abort on discrepancy.
- (For step 06 executor-infra) Post-apply verification sequence MUST be: (a) `GET /v1/firewalls/{id}` → confirm `project_id=15130993`, rules match design, `applied_to` includes server `145542849`; (b) `GET /v1/servers/145542849` → confirm `protection.delete=true` AND `protection.rebuild=true`; (c) from management workstation: `Test-NetConnection 46.225.239.60 -Port 22` → True.
- (For step 08 landscape-updater) Update `landscape/hosts/ubuntu-16gb-nbg1-1.md` "Hetzner Cloud Firewall" section to reflect new firewall id, name, rule list, `applied_to`, and protection flags. Update "Hardware & OS" one-line summary. Bump `last_verified:` to 2026-06-27. Add a change-log row for `2026-06-27-apply-hetzner-firewall-001`. Re-status the "What needs to happen" item #2 to ✅ done. Remove T-0086 from "Open tasks affecting this host" after task-08 close-out. Mirror the prod change-log row format from the 2026-05-26 Gitea-SSH firewall modification entry, substituting project 15130993 and server 145542849.

## Verdict rationale

**PASS.** Task T-0086 is well-formed with concrete, verifiable acceptance criteria; the `infrastructure` workflow is the correct fit (firewall rules are explicitly enumerated in scope); the task is not redundant with prior work and has no conflict with the current landscape state (zero firewalls in project 15130993, no conflicting tasks, no concurrent runs, host-layer protections orthogonal to cloud-layer); all facts required to design a solution exist in landscape plus the audit follow-up; and every workflow-specific rule is satisfiable with the recommended design pattern (create+apply separation, pre-flight IP re-verification, two-place post-apply verification). The only material design decision that must be made at step 04 is the call shape of the protection-flag endpoint (combined vs separate); both are valid per Hetzner API, and the combined-call recommendation matches API convention. The validation surfaces no `BLOCKED` condition and no `FAIL` condition. The task is ready for solution design. The orchestrator should expect step-04 to emit `verdict: NEEDS_APPROVAL` and a step-05 user-approval file before the executor may mutate Hetzner state.